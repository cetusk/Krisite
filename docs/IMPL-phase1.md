# Krisite — Phase 1 実装ノート

対象: **Phase 1（出力抽出の最小検証）** — **CP3 到達時点**（2026-08-26）。

## 0. この文書の位置づけ

`CLAUDE.md`「文書の役割分担」のとおり、ここには **なぜそう作ったか** だけを残します。
コードを読めば分かること・`git log` で分かることは書きません。

**CP1 / CP2 / CP2.5 / CP3 をすべて通過しました。** 残りは §7.5 の一覧です。

仕様は第8版です。CP2.5（斜面）が新設され、CP2 の位置づけが訂正されました。

Phase 0 の実装ノートは `IMPL-phase0.md`。そちらの §7「Phase 1 への申し送り」も参照してください。

---

## 1. 実装の地図

| モジュール | 責務 | **そこだけが持つ不変条件** |
|---|---|---|
| `geom/`（追加分） | 平面の同一判定・全順序、軸平行平面、同次点の投影向き | 平面 ID の同値関係はここが定義する |
| `mesh/tri_mesh.hpp` | 入力メッシュと符号付き体積 | 向きは「外から見て CCW」= 外向き法線 |
| `mesh/topology.hpp` | §10.1 の位相検査 | **幾何を一切見ない。**純粋に組合せ的 |
| `octree/uniform_grid.hpp` | 固定深度の一様分割 | セル境界は 2 の冪の格子座標。深度は実行時 |
| `csg/plane_table.hpp` | 幾何平面 → `PlaneId` | 同一平面（向きを問わない）は同一 ID |
| `csg/faces.hpp` | 入力を平面ごとの多角形に併合 | 各辺は**別の平面**を持つ隣接面と共有される |
| `csg/fragment.hpp` | 凸多角形の断片と平面分割 | 頂点 = `support ∩ edge[i-1] ∩ edge[i]` |
| `csg/raycast.hpp` | 点の内外判定 | **判定点は境界上に無い**（呼び出し側の契約） |
| `csg/boolean.hpp` | §4 のパイプライン | 分割平面は**両メッシュの全平面** |

---

## 2. 設計判断と根拠

### 2.1 入力三角形を「平面ごとの多角形」に併合した ★

**これが Phase 1 で最初に必要になった、仕様に明示されていない構造上の判断です。**

立方体の 1 面を 2 三角形のまま扱うと、その**対角辺**が問題になります。対角辺は三角形分割の
産物であって幾何的な特徴ではなく、**2 平面の交線として表せません**（両側の三角形が同一平面
なので交線が定まらない）。したがって対角辺上に生まれる交点は「3 平面の交点」の枠に収まらず、
§3.3 の頂点表現から外れます。

併合すれば多角形の各辺は必ず**別の平面を持つ隣接面**と共有されるので、

- 辺 = 2 平面の交線
- 頂点 = 3 平面の交点
- クリップで生まれる頂点 = （支持平面, 辺の平面, 切断平面）

となり、**表現がクリップについて閉じます。**§3.1 の「立方体の 1 面は 2 三角形ですが、
平面は 1 枚です。統合しないと、後段で同じ点が別の平面3つ組から生まれ、§5 の同一性判定に
不要な負荷がかかります」は、この話だと読みました。

- **代案 A**: 三角形のまま扱い、入力辺を特別な辺種として持つ
  → クリップで生まれる頂点が 3 平面の交点にならず、`intersect3` が使えない。
    §3.3 の `Vertex` 表現も破綻する
- **代案 B**: 対角辺の両側に「同一平面」を割り当てて交線を退化させる
  → `intersect3` の前提（3 平面が一点で交わる）が破れる

**Phase 1 の制約**: 併合後の多角形が**凸**であることを要求します。§4.3 が「凸なので扇でよい」
と書いているのはこの前提です。コーパス（立方体・四面体・量子化した回転立方体）はすべて凸面
なので成立しますが、**一般のメッシュでは非凸の平面パッチがあり得ます**（§7 に申し送り）。

### 2.2 断片は「支持平面 + 辺の平面」で持つ

頂点を明示的に持たず、`edge[i]` だけを持ちます。頂点は必要になったときに `intersect3` で
復元します。

- **利点**: クリップが**平面 ID の列の操作**になる。頂点の同一性が平面3つ組で自然に決まり、
  §5.3 の第1段のキーがそのまま得られる
- **代償**: 頂点を使うたびに `intersect3`（262 ns、`IMPL-phase0` §5.2）が走る。
  Phase 1 は性能を問わないので許容。キャッシュは Phase 2 の候補

### 2.3 クリップの辺保持規則

保持する辺の判定を「端点の少なくとも一方が**真に**内側」にしています。

素朴に「端点の一方が 0 以上」とすると、頂点がちょうど切断平面に乗るときに**面積 0 の辺**を
拾い、頂点が重複した退化多角形ができます。凸多角形では保持される辺が巡回的に連続するので、
その連続区間の末尾に切断平面を 1 枚足せば閉じます。

この規則は 4 通りの符号配置（両端内側 / 出る / 入る / 両端外側）と、頂点が平面に乗る場合を
手で検算してから実装しました。テストの `test_split_through_vertex` がこの境界を突きます。

#### 2.3.1 開始位置の探し方が誤っていた ★ CP2.5 の唯一のバグ

保持辺の開始位置を「**直前が非保持である最初の保持辺**」で探していました。
**全辺が保持される場合に開始点が見つからず、その側を丸ごと落とします。**

三角形を 1 頂点だけ外側に切ると起きます。符号が $(+,+,-)$ のとき、3 辺はいずれも
「端点の少なくとも一方が真に内側」を満たすので全辺が保持され、残るべき四角形が消えます。
負側（三角形）は全辺保持にならないので残り、**片側だけが欠ける**という壊れ方をします。

**軸平行な立方体を軸平行な平面で切るかぎり断片は常に長方形なので、この配置は
一度も現れません。**（長方形は $(+,+,-,-)$ になり、両端が外側の辺が必ず 1 本ある。）
CP1 / CP2 のコーパスがすべて軸平行だったため、**このバグは 2 つのチェックポイントを
素通りしました。**

正しくは、開始位置を**脱出辺**（$s_i k > 0$ かつ $s_{i+1} k \le 0$。凸多角形では一意）の
次にある最初の保持辺として求めます。全辺保持の場合も含めて一様に決まります。

### 2.4 セル割り当ては AABB による保守的判定

三角形の AABB とセルの**閉領域**が重なるかだけを見ます。

- **取りこぼさない**（AABB は三角形を含むため）。取りこぼすと §4.2 の破綻が起きる
- **過剰割り当てはある**が、クリップ結果が空になるだけで無害
- 厳密な三角形-箱交差（分離軸定理）は Phase 2 の最適化候補

「取りこぼさないこと」が正しさに効き、「過剰でないこと」は性能にしか効かない、という
非対称性があるので、Phase 1 では安全側に倒しました。

### 2.5 分類の代表点は「相手の**境界上**に無い断片の頂点」★ CP2 で改めた

§6.1 は「キャスト元の断片は入力頂点を含むものを優先」「断片ごとの重心は $18b{+}36$ ビット
規模になるので避けよ」と言っています。実装では**断片の頂点そのもの**を使います。

**CP1 では条件を「相手の全平面に対して符号が非零」としていました。これは強すぎました。**

断片の頂点は、その断片を切り出した平面の上に必ず載ります。相手の平面が切断に使われて
いれば、頂点は必ずその平面の上にあります。ケース 2 では**四角形の 4 辺のうち 3 辺が
相手の平面上**という断片が現れ、4 頂点すべてが条件を落として停止しました。

必要なのは「$\partial B$ の外にあること」だけです。**平面上にあることと境界上にあることは
別です。** 相手の平面上でも、その平面上の面の外側なら境界からは外れています。
そこで `csg/raycast.hpp` に `point_on_boundary` を足し、条件をこれに置き換えました。

判定は厳密です。三角形の平面上にあるかを `side` で見て、載っていれば**法線成分が非零の
軸**へ投影して `orient2d_h` 3 本で内外を見ます。新しい述語は要りません。

**頂点の分類が断片全体の分類と一致することは保証されます。** $\partial B$ は $B$ の平面の
和集合に含まれ、断片は $B$ の平面配置のセルなので、$\partial B$ は断片の**内部を横切りません**。
頂点から内部へ引いた線分は途中で $\partial B$ を跨がないので、頂点が境界上に無ければ
断片と同じ側です。

- **代案**: 重心（7 リム規模）→ §6.1 が明示的に避けよと言っている
- **代案**: 入力頂点だけを使う → 入力頂点を持たない断片が classification できない

### 2.5.1 それでも足りず、対角線の中点に落とした

条件を緩めてもなお、**4 頂点すべてが $\partial B$ 上**という断片が残りました。ケース 2 の

    A = [-2^{b-1}, 0]^3、B の面 x = -2^{b-2} 上の断片（y \in [-2^{b-2}, 0], z \in [-2^{b-1}, 0]）

がそれです。この断片は A の内部にありますが、4 辺のうち 3 辺が A の面に載るため、
4 頂点すべてが $\partial A$ 上になります。相対内部の点が要ります。

**凸多角形の対角線の中点は相対内部にあります。** 相対内部は相手の平面配置のセルの内側
なので、$\partial B$ から必ず外れます。したがって**頂点数 4 以上なら必ず決まります。**

問題は幅です。$v_i = (X_i : W_i)$ の中点は

$$ m = (X_0 W_1 + X_1 W_0 \;:\; 2 W_0 W_1) $$

で、これを `HPointD` に詰めるには `kHomoXyz` / `kHomoW` を広げることになり、
**構成点の表現そのものが太ります**（すべての述語に波及）。

そこで **$m$ を構成しません。** `side_value` も `orient2d_h_value` も同次座標について
**線形**なので、被符号値 $F$ について

$$ F(m) = W_1 F(v_0) + W_0 F(v_1) $$

が厳密に成り立ちます。$m$ の $w = 2W_0W_1$ の符号は $\mathrm{sign}(W_0)\mathrm{sign}(W_1)$ です。
`geom::HMidPointD` は両端点を持つだけで、述語 2 本だけを新しい幅で足しました（§4）。

### 2.5.2 三角形の断片には対角線が無い — 3 頂点の重心に落とす

**予告どおり起きました。** CP2.5 のケース 2T で、3 頂点すべてが $\partial B$ 上の三角形
断片が出ます（4 辺のうち 3 辺が相手の平面上、という 2.5.1 と同じ形の三角形版）。

**凸多角形の 3 頂点が張る三角形の内部は、多角形の相対内部に含まれます。**
したがって共線でない 3 つ組を選べば必ず境界から外れます。全 3 つ組を試すので、
非退化な断片なら必ず決まります。

重心も構成しません。同じ線形性で
$$ F(c) = W_1W_2 F(v_0) + W_0W_2 F(v_1) + W_0W_1 F(v_2) $$
と評価します（`widths.hpp` bits::kTriSide の導出）。

代表点の探索は **頂点 → 対角線の中点 → 3 頂点の重心** の 3 段です。安いものから
試すので、実測では中点までで決まる断片がほとんどです（CP2.5 で重心の発火は 0 件）。

### 2.6 レイキャストの記号的摂動を投影だけに限定できる理由

2.5 により**判定点は必ず $\partial B$ の外**にあります（`point_on_boundary` で確かめます）。したがって

$$N \cdot p + d \ne 0$$

が保証され、**前方交差の判定は退化しません。**摂動が要るのは投影後の内外判定だけです。

摂動はレイ原点を実座標で $(0, \varepsilon, \varepsilon^2)$ ずらしたものとして扱います。
投影の向きが 0 になったとき、

$$\mathrm{orient2d} \to O + (b_y - a_y)\varepsilon^2 - (b_z - a_z)\varepsilon$$

なので、$\varepsilon$ の項 $-(b_z - a_z)$、それも 0 なら $\varepsilon^2$ の項 $(b_y - a_y)$ で
符号が決まります。両方 0 なら投影された辺が退化していて、それは三角形の法線の $x$ 成分が
0（レイが平面に平行）の場合なので、先に弾いています。

**記号的摂動の適用範囲を狭めたのは意図的です。** 摂動は正しさの検証が難しいので、
必要な箇所を構造的に減らしました。

### 2.7 位相検査器を出力より先に作った

`mesh/topology.hpp` を `csg/` より前に実装しました。検査器が無いと CP1 の合否を判定できず、
§10.5 の変異テストも検出力を持ちません。

そして**検査器自身を、位相が既知の構成で検証**しました。とくに次の 2 つが重要です。

| 構成 | 何を保証するか |
|---|---|
| 頂点だけを共有する 2 立方体 | 辺は壊れず向きも整合するが**頂点多様体が落ちる**（ケース 11b の $\cup$） |
| **T 字接合** | 幾何的に隙間ゼロでも**辺多様体が落ちる** |

後者が成り立たないと §10.5 の**変異 3 が素通り**します。§4.3.1 が「T 字接合は幾何的に
隙間が無いため、緩い検査だと素通りします」と警告しているのはこの点です。

検査器は幾何を一切見ません。純粋に組合せ的なので、頂点座標の表現（`IPoint` か構成点か）に
依存せず、§10.2.1 の深度不変性がそのまま乗ります。

### 2.8 出力メッシュは `TriMesh` ではない

出力の頂点は構成点（有理数）なので `mesh::TriMesh`（`IPoint` 頂点）には入りません。
`csg::BoolMesh`（`HPointD` 頂点）を別に定義しました。

位相検査は三角形の添字だけで行えるので、`check_topology(BoolMesh::triangles)` がそのまま
使えます。2.7 で検査器を幾何非依存にしておいたことがここで効いています。

---

### 2.9 断片の同一性は「辺の平面 ID」では決まらない ★ CP2 で改めた

CP1 では断片の正準キーを **(支持平面, 辺の平面 ID 列の正準回転)** にしていました。
**これは誤りです。**

支持平面 $p$ に対し、**相異なる 2 平面が $p$ と同一の交線を持ち得ます。**
例えば $p: z=0$ に対して $q_1: x=0$ と $q_2: x+z=0$ はどちらも $p$ 上で直線 $x=0$ を与えます。
同じ幾何辺が、切る順序によって $q_1$ とも $q_2$ とも記録されます。

そこで**縫合を分類より先に済ませ**、断片の同一性を**大域頂点 ID の集合**で判定するように
変えました。第2段（値ベースの併合）を通した ID は正準なので、平面 ID の別名に影響されません。
凸多角形は頂点集合で一意に定まるので、順序を捨てても領域は復元できます。

`csg/boolean.hpp` の冒頭の手順が「4 → 5」の順になっているのはこのためです。

### 2.10 共平面重複は「符号 0」ではなく「同一領域の対」で検出する ★

§4.3.2 は「$\partial B$ 上に載る断片は符号 0 になります」と書いていますが、
**逆は成り立ちません。** 符号 0 は「相手の平面上」であって「相手の境界上」ではありません。
$B$ の面の外側にある同一平面上の断片も符号 0 になります（2.5 と同じ混同）。

正しい判定は「$B$ の面がその領域を覆っているか」です。そして

- $B$ の面の境界辺は、それぞれ $B$ の平面に載っている（2.1 の併合の帰結）
- 分割は $B$ の全平面で行う（§4.3.1）

ので、**平面 $p$ 上の断片は $B$ の面に丸ごと含まれるか丸ごと外れるか**のどちらかです。
したがって $\partial B$ が覆っているなら、$B$ 側にも**同一領域の断片が必ず存在します**。

そこで「同じ (支持平面, 頂点 ID 集合) の組に両 owner の断片が居る」ことを共平面重複の
定義にしました。向きは `Fragment::flipped` の一致で分かります（同じ支持平面どうしなので）。

選択規則は正則化ブール（§2.3）から決まります。

| | $\cup$ | $\cap$ | $A \setminus B$ |
|---|---|---|---|
| 同方向 | A のみ | A のみ | 両方落とす |
| 逆方向 | 両方落とす | 両方落とす | A のみ |

同方向の面は「両者の外側が同じ側」なので $\cup$ と $\cap$ の境界に残り、$A \setminus B$ で消えます。
逆方向の面は「一方の外側が他方の内側」なので $\cup$ と $\cap$ では内部面になって消え、
$A \setminus B$ では $A$ の表面として残ります。

**副次的に、`seen` による重複除去のバグも直りました。** CP1 の重複キーには owner が
入っていなかったので、A の断片と B の断片が同一領域を占めると**片方が「重複」として
黙って捨てられていました**。共平面重複そのものが消えていたわけです。

## 3. 仕様に書かれていない判断

| 箇所 | 判断 | 根拠 |
|---|---|---|
| 入力の平面併合 | 平面ごとの多角形に統合する（2.1） | 対角辺が 2 平面の交線として表せない |
| 併合後の凸性 | Phase 1 の前提として要求する | §4.3「凸なので扇でよい」の前提 |
| セル割り当て | AABB による保守的判定（2.4） | 取りこぼしと過剰の非対称性 |
| 分類の代表点 | 相手の境界上に無い断片頂点、無ければ対角線の中点（2.5） | 重心を避けつつ境界を外す |
| 中点の扱い | 点を構成せず両端点で持つ（2.5.1） | 構成点の幅を広げずに済む |
| 出力の三角形化 | 断片の頂点順のまま扇 | 併合時の CCW 順が保たれる |
| `Difference` の向き | 採用した B の断片は頂点順を反転 | $A \setminus B$ の境界では B の法線が逆になる |
| 断片の同一性 | (支持平面, **縫合後の大域頂点 ID 集合**) をキー（2.9） | 辺の平面 ID は正準でない |
| 共平面重複 | 同一領域に両 owner の断片が居ることで検出（2.10） | 符号ベクトルの 0 だけでは境界上か判らない |

---

## 4. 新規述語のビット幅

§7 の手順どおり、**先に導出して `geom/widths.hpp` に定数を足してから**実装しました。

| 述語 | 上界 | 実測 (b=21) | リム | 用途 |
|---|---|---:|---|---|
| 平面の小行列式 `5b+9` | 114 | 106 | 2 | 平面の比例判定・全順序 |
| 平面の全順序 `6b+11` | 137 | — | 3 | 実際は `5b+8` までしか届かない（下記） |
| 軸平行平面のオフセット `b+1` | 22 | — | `kOffset` に内包 | セル面 |
| 四面体の体積x6 `3b+1` | 64 | 63 | 1 | 入力の向き検査 |
| 入力メッシュの体積x6 `3b+1+16` | 80 | — | 2 | 三角形 $2^{16}$ 枚までの総和 |
| `orient2d_h` `8b+17` | 185 | 170 | 3 | レイキャスト（同次点版） |

**上界超過はゼロです。**

`kPlaneOrder = 6b+11` は 24 ビット（b=21）保守的です。先頭非零成分 $i$ は法線に限られる
ため（$i$ が $d$ なら $a=b=c=0$ で、それは退化三角形の平面）、実際に到達しうる最大は
`kPlaneMinor` と同じ $5b+8$ です。安全側なのでそのまま採っています。

`orient2d_h` は**キャスト元が同次点のときだけ**必要です。入力頂点（`IPoint`）から撃つなら
既存の `orient2d`（$2b+3$）で済みます。

### 4.1 CP2 で足した 2 本（中点の述語）

2.5.1 の代表点フォールバックのために追加しました。**中点を構成せず**、
被符号値の線形性を使って両端の値の重み付き和で評価します。

| 述語 | 導出 | 理論 (b=21) | 実測 | リム |
|---|---|---:|---:|---:|
| `side(plane, 中点)` | $\mathtt{kHomoW} + \mathtt{kSide} + 1 = 15b+33$ | 348 | 317 | 6 |
| `orient2d_h(中点)` | $\mathtt{kHomoW} + \mathtt{kOrient2dH} + 1 = 14b+30$ | 324 | 297 | 6 |
| `side(plane, 重心)` | $2\,\mathtt{kHomoW} + \mathtt{kSide} + 2 = 21b+46$ | 487 | 443 | 8 |
| `orient2d_h(重心)` | $2\,\mathtt{kHomoW} + \mathtt{kOrient2dH} + 2 = 20b+43$ | 463 | 423 | 8 |

$+1$ は 2 項の和、$+2$ は 3 項の和のぶんです。**上界超過はありません。**

比較のために書いておくと、中点を `HPointD` として素直に構成すると
$x,y,z$ が $13b+27$、$w$ が $12b+25$ になり、**構成点の型そのものを広げる**ことになります。
`side` は $15b+32$、`cmp_h` は $25b+53$ に膨らみ、Phase 0 の述語全体に波及します。
線形性を使えば、影響はこの 2 本に閉じます。

---

## 5. チェックポイントの結果

### 5.1 CP1 — ケース 1（一般位置）× union × 深度 0, 2

ケース 1（一般位置の立方体 2 個）× union × 深度 0 と 2。

| 深度 | V | E | F | χ | C | g | 断片 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 0 | 44 | 126 | 84 | 2 | 1 | 0 | 48 |
| 2 | 170 | 504 | 336 | 2 | 1 | 0 | 192 |

辺多様体・頂点多様体・向きの整合・退化三角形なし、すべて通過。
**§10.2.1 の深度不変性も成立**（$(C, g_{\text{total}})$ が一致）。断片が 4 倍に増えたうえで
位相が保たれているので、分割が効いた状態での不変性です。

#### §5.4 の数値（ケース 1）

| 測る値 | 深度 0 | 深度 2 |
|---|---:|---:|
| 構成点の総数 | 44 | 170 |
| 平面3つ組が相異なるのに値が一致した頂点 | 0 | 0 |
| **1 点に集まる平面の最大枚数** | **3** | **3** |
| 領域数（符号ベクトル） | 16 | 16 |

**一般位置では 4 平面同時交差は起きません。**最大 3 枚で、第1段が全頂点を捕まえ、
第2段の出番はゼロでした。総当たり（併合後の各頂点 × 全平面で `side` を評価）で
測っているので過小評価ではありません。

**5 枚以上が出るかは CP2（ケース 5）で初めて分かります。**

---

### 5.2 CP2 — ケース 5（面がセル境界と一致）× union × 深度 0〜3 ★

ケース 5（面がセル境界と完全一致）× union × 深度 0〜3。**通過しました。**

| 深度 | V | E | F | χ | C | g | 断片 | 重複断片 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 0 | 44 | 126 | 84 | 2 | 1 | 0 | 48 | 0 |
| 1 | 44 | 126 | 84 | 2 | 1 | 0 | 48 | 12 |
| 2 | 44 | 126 | 84 | 2 | 1 | 0 | 48 | 36 |
| 3 | 170 | 504 | 336 | 2 | 1 | 0 | 192 | 144 |

辺多様体・頂点多様体・向きの整合・退化なし、すべて通過。深度不変性も成立。

**深度 1 と 2 で断片が増えないのは仕様どおりの挙動です。** ケース 5 の面は深度 1 の境界
($0$) とも深度 2 の境界 ($\pm 2^{b-2}$) とも一致し、それらは `PlaneTable` で入力平面と
**同じ ID** になります。§4.3.1 の大域分割が既にその平面で切っているので、
深度を上げても**新しい切断面が 1 枚も増えません**。増えるのは重複割り当てだけです。
実際に新しい切断が入るのは深度 3（$\pm 2^{b-3}$ の奇数倍）からです。→ §6.7

#### §5.4 の数値（ケース別・深度別、union）

| # | 構成 | 深度 | 断片 | 重複断片 | 構成点 | 併合後 | 値併合 | **最大枚数** | うち mesh |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 一般位置 | 0/1/2/3 | 48/108/192/366 | 0/0/0/0 | 46/100/184/358 | 同左 | 0 | **3** | 3 |
| 2 | 面が完全共平面 | 0/1/2/3 | 40/40/56/224 | 0/8/40/160 | 37/37/51/208 | 同左 | 0 | **3** | 3 |
| 5 | セル境界と一致 | 0/1/2/3 | 48/48/48/192 | 0/12/36/144 | 46/46/46/184 | 同左 | 0 | **3** | 3 |
| 5T | セル角を斜面が通る | 0/1/2/3 | 34/34/48/154 | 0/12/19/72 | 40/40/70/183 | 32/32/56/146 | 8/8/14/37 | **4/4/6/6** | 4 |
| 6 | 1 格子ずれ | 0/1/2/3 | 48/108/192/432 | 0 | 46/100/184/418 | 同左 | 0 | **3** | 3 |
| 8 | 同一の立方体 | 0/1/2/3 | 12/48/48/192 | 0/0/48/192 | 8/26/26/98 | 同左 | 0 | **3** | 3 |
| D | 交わらない 2 個 | 0/1/2/3 | 32/60/96/308 | 0 | 36/64/100/312 | 同左 | 0 | **3** | 3 |

「最大枚数」はセル面込み、「うち mesh」は入力メッシュ由来の平面だけ。
総当たり（併合後の全頂点 × 表の全平面で `side` を評価）なので過小評価ではありません。

#### 分かったこと ★

**軸平行な立方体だけのコーパスでは、1 点に集まる平面は原理的に 3 枚を超えられません。**

1 軸につき点を通れる平面は高々 1 枚（平行な 2 平面は交わらない）で、軸は 3 本だけだからです。
そして**セル面が立方体の面と一致しても枚数は増えません。** 両者は同一の幾何平面なので
`PlaneTable` が同じ ID に併合し、頂点の平面3つ組が一意に定まるためです。

つまり **ケース 5 は、仕様が期待した「§5.2 の 4 平面同時交差」を突いていません。**
突いているのは §4.2 の重複割り当てのほうです（深度 3 で 144 件）。

そこで**斜面**を持つ対（ケース 5T）を作って確かめました。四面体の面 $x+y+z=0$ を
原点（深度 1 以降のセル角、かつ A の角）に通すと、原点を通る平面は
$x=0$, $y=0$, $z=0$, $x+y+z=0$ の **4 枚**になります。

- **最大枚数 4**（セル面込みで 6）を検出 → **計測は 4 枚以上を検出できます**
- **第2段が発火（値併合 8〜37 件）** → §5.3 の第2段は**必要です**。
  平面3つ組が相異なるのに値が一致する頂点が実際に出ます

**検出できない計測器で「最大 3 枚」と報告しても何も言っていません。** 5T はその番人です。

**ただしケース 5T の位相は通りません**（§7.2）。斜面を持つ入力では現状の実装が
非多様体を出します。**これは CP2 の問いではなく CP3（ケース 7 / 9）の問題で、
この改訂の前から同じです**（旧実装は同じ入力で停止します）。合否には使っていません。

#### 結論

- **CP2 は通過**。§11 の「通れば EMBER 続行の判断が立つ」に該当します
- **5 枚以上は出ませんでした。** 軸平行では 3 枚、斜面を入れると 4 枚（セル面込み 6 枚）
- §5.3 の第2段は**斜面があるときに必要**。軸平行だけなら第1段で足ります
- 4 平面同時交差の発生率は「発散する」水準ではありません（§11 の中止条件 3 には該当せず）

---

### 5.3 CP2.5 — 斜面ケース（2T / 4T / 5T）× 3 演算 × 深度 0〜3 ★

**通過しました。** §11 の「通れば EMBER 続行の判断が立ちます」に該当します。

深度 0 が先決という指摘のとおりで、**落ちていたのは縫合ではなく `clip_edges`** でした
（2.3.1）。深度 0 には継ぎ目が無いので、これは CP1 クラスの失敗です。

| ケース | ∪ | ∩ | $A\setminus B$ | 最大枚数（mesh のみ） | 値併合 / 構成点 | 有効セル 0→3 |
|---|---|---|---|---:|---|---|
| 2T 共平面接触を斜面で | 通過 | 通過 | 通過 | **6**（3） | 83 / 189 = **43.9%** | 1 → 7 → 14 → 52 |
| 4T 四面体 2 個が頂点共有 | §9.3 で対象外 | 通過（空） | 通過 | **9**（**6**） | 18 / 193 = 9.3% | 1 → 2 → 14 → 76 |
| 4T′ 頂点共有 + 体積も重なる | **通過** | **通過** | §9.3 の扱い（扇 3 個。7.8） | **9**（**6**） | 32 / 225 = 14.2% | 1 → 1 → 8 → 48 |
| 5T セル角を斜面が通る | 通過 | 通過 | 通過 | **6**（4） | 72 / 234 = 30.8% | 1 → 7 → 23 → 120 |

**4T′ は 4T の $\cup$ が除外されることの穴埋めです**（第8版 §9.1）。4T は両者が別々の
八分儀にあるため共有頂点まわりに扇が 2 つできますが、4T′ は同じ側に重ねるので扇が
1 つにつながり $\cup$ が多様体になります。これで**高多重度の同時交差が 3 演算すべてで
検証されます。**

深度不変性 $(C, g_{\text{total}})$ もすべて成立。

**§5.4 の問い「5 枚以上が出るか」の答えは YES です。** 4T / 4T′ で mesh 平面のみ 6 枚、
セル面込みで 9 枚に達します。四面体の頂点に 3 枚ずつ集まるという仕様の予測どおりでした。

**そして併合は局所に閉じます。** 併合グループの空間的な広がりは全ケース・全深度で
1 以下、つまり 1 セルとその面・辺・頂点隣接に収まります（第8版 §5.4）。
発生率が 44% でも大域整列は不要で、**Phase 3 は「セル並列 + 境界併合」で組めます。**

**第2段は必須です。** 値併合の割合が 9〜44% で、無視できる水準ではありません。
§11 の中止条件 3（発生率が深度とともに発散）には該当しません — 深度を上げても
割合はおおむね一定です（5T: 27% → 27% → 30% → 31%）。

#### ケース 4T の $\cup$ は §9.3 の扱いにしています

四面体 2 個が**頂点だけ**を共有するので、$\cup$ の出力は非多様体になります
（$C=2$、頂点まわりに扇が 2 つ）。ケース 11b と同型で、**正則化では解決しません**。
§9.3 は 11b について書かれていますが、同じ理由が 4T にも当てはまるので同じ扱いに
しました。**§9.3 の適用範囲を 4T にも広げる旨、仕様への明記をお願いします**（§7.7）。

なお **$\cap$ が空になるのは正しい結果です**（頂点接触の正則化交差は空）。
空メッシュは合法なので（§2.3）、位相検査は個別フラグではなく `ok()` で見ます。

### 5.4 §10.3 体積の恒等式（GMP）★

**位相が通っただけでは「正しい立体か」は分かりません。** 辺が 2 面に接し向きが整合して
いても、選んだ断片が違えば別の立体になります。そこで §10.3 を実装しました。

$$|A \cup B| + |A \cap B| = |A| + |B|, \qquad |A \setminus B| = |A| - |A \cap B|$$

**全 9 ケース × 深度 0〜3 × 3 演算で厳密に成立**（10945 検証）。深度によって体積が
変わらないことも確かめています。

出力の頂点は有理数で、共通分母が三角形ごとに違うので `mpq` で足し合わせます。
入力側は整数座標なので `mesh::signed_volume6` から出ます（**別経路どうしの突き合わせ**）。

---

### 5.5 CP3 — 全 18 ケース × 3 演算 × 深度 0〜3 ★

**通過しました**（1503 検証）。§9.3 の除外は **3 構成**だけです。

#### §5.4 の数値（ケース別の最大値）

| ケース | 構成 | 最大枚数 | うち mesh | 値併合 |
|---|---|---:|---:|---:|
| 1 | 一般位置 | 3 | 3 | **0** |
| 2 | 面が完全共平面 | 3 | 3 | **0** |
| 3 | 辺が一致（共線） | 3 | 3 | **0** |
| 4 | 頂点が一致 | 3 | 3 | **0** |
| 5 | 面がセル境界と一致 | 3 | 3 | **0** |
| 6 | セル境界から 1 格子ずれ | 3 | 3 | **0** |
| **7** | **量子化した回転立方体** | **6** | **4** | **87** |
| 8 | 同一の立方体 | 3 | 3 | **0** |
| **9** | **四面体 2 個（一般位置）** | **6** | 3 | **18** |
| 10 | 一方が他方を完全に含む | 3 | 3 | **0** |
| 11a | 面接触のみ | 3 | 3 | **0** |
| 11b | 辺接触のみ | 3 | 3 | **0** |
| 12 | セル境界をまたぐ大立方体 | 3 | 3 | **0** |
| **2T** | 共平面接触を斜面で | **6** | 3 | **83** |
| **4T** | 四面体 2 個が頂点を共有 | **9** | **6** | **18** |
| **4T′** | 頂点共有 + 体積も重なる | **9** | **6** | **32** |
| **5T** | セル角を斜面が通る | **6** | **4** | **72** |
| D | 交わらない 2 立方体 | 3 | 3 | **0** |

**軸平行なケースは 12 件すべてが例外なく「3 枚・値併合 0」です。**
斜面を持つ 6 件だけが 4 枚以上になります。§9.1 の構造的な主張が、
コーパス全体で確認できました。

**ケース 4（頂点が一致）も 3 枚止まり**でした。§9.1 の注記どおりで、軸平行立方体の
頂点が一致するならその 3 平面も一致して併合されるためです。

#### §9.2 の期待値

ケース 10（$B$ が $A$ の内部に完全に含まれる）は、$A \setminus B$ が
**$C=2$、$\chi=4$**（外殻 + 内殻）。§9.2 の表のとおりです。
$A \cap B = B$ が空でないことも確認しました。

#### §9.3 の除外は 3 構成

| 構成 | 接触 | 壊れる検査 |
|---|---|---|
| 4T $\cup$ | 頂点 | `vertex_manifold`（扇 2 個） |
| 4T′ $A\setminus B$ | 頂点 | `vertex_manifold`（扇 3 個） |
| **11b $\cup$** | **辺** | **`edge_manifold`**（次数 4 の辺） |

**18 ケース × 3 演算 = 54 構成のうち 3 件**（5.6%）です。§9.3.1 の「2〜3 件なら
特殊ケース」の範囲に収まっており、§2.1 の前提を見直す水準ではありません。

---

### 5.6 §12 `side` : `intersect3` の呼び出し比率 ★ 前提が否定されました

**比は 1.26 : 1 でした**（全 18 ケース × 3 演算 × 深度 0〜3 の合計。
`side` 2,713,359 回 / `intersect3` 2,156,253 回）。

`IMPL-phase0.md` §7 はこう書いていました。

> EMBER の想定では構成点は一度作って何度も `side` にかけるので、
> `intersect3` の重さが支配的になるとは限らない。**Phase 1 で実際の比率を測ってから**
> 最適化対象を決めてください。

**測ったら、前提のほうが成り立っていませんでした。**

#### 原因は実装にあります

`split_fragment` の中心の行がこうなっています。

```cpp
s[i] = geom::side(qp, fragment_vertex(t, f, i));
```

`fragment_vertex` は毎回 `intersect3` を呼びます。**構成点を保持せず、述語を評価する
たびに作り直しています。** だから `side` 1 回につき `intersect3` が 1 回必要になり、
比が 1 に張り付きます。「一度作って何度も使う」形になっていません。

#### 時間で重み付けすると

Phase 0 の実測（`BENCH.md`: `side` 11.25 ns、`intersect3` 262 ns）で重み付けすると、

$$ \frac{2{,}156{,}253 \times 262}{2{,}156{,}253 \times 262 + 2{,}713{,}359 \times 11.25} = \mathbf{94.9\%} $$

**述語の時間のほぼすべてを `intersect3` が占めます。**

> **これは推定です。** 呼び出し回数は厳密ですが、単価は別条件のベンチマーク値です。
> 実際の時間は測っていません（Phase 1 に性能要件は無いため）。

#### Phase 2 への含意

最適化の候補が 2 つあり、**順序が逆転します。**

| 候補 | 効き方 |
|---|---|
| **断片の構成点を保持する** | 呼び出し回数そのものを減らす。**比率の前提を作り直す**ほうの手 |
| `intersect3` の混合幅化（§12 が挙げているもの） | 単価を下げる。回数はそのまま |

§12 は混合幅化を挙げていますが、**先にやるべきは構成点の保持**だと考えます。
混合幅化で単価が数分の 1 になっても、回数が `side` と同数のままなら
`intersect3` が支配的な構図は変わりません。**両方やる価値はありますが、
順序は「回数を減らす」が先です。**

#### 計測の仕組み

`KRISITE_COUNT_PREDICATES` を定義したときだけ `thread_local` のカウンタが動きます。
**既定ビルドでは前処理の時点で消えます**（`((void)0)` に展開されることを確認済み）。
`tests/csg/test_call_ratio.cpp` だけがこの定義付きでビルドされます。

代案として述語にカウンタを引き回す案がありましたが、`side` は `split_fragment` や
`crosses` の内側から呼ばれるため呼び出し経路すべてに引数が要り、
**測定のために本番の API を歪める**ことになるので採りませんでした。

---

### 5.7 §10.2 の GMP 不要検査 — 冪等性は 3 変異とも素通りします

§10.2.2（冪等性）と §10.2.3（平面 ID 帰属）を `tests/csg/test_idempotence.cpp` に
実装しました。全 18 ケース × 深度 0〜3 で通ります。

| 深度 | 検査 | 結果 |
|---|---|---|
| 0 | $A\cap A$、$A\cup A$ が `retriangulate(A)` と三角形単位で一致 | 通過 |
| ≥ 1 | $A\cap A$ と $A\cup A$ が互いに一致 | 通過 |
| 全深度 | $A\setminus A$ が空 | 通過 |
| 全深度 | 出力面が $A$ の平面に載っている | 通過 |

#### **ただし、3 変異のどれも検出しません**

| 変異 | 冪等性 | 検出する他のテスト |
|---|---|---|
| 1 第2段の無効化 | **素通り** | cp25 / cp3 |
| 2 開領域割り当て | **素通り** | cp2 / cp25 / cp3 / depth_monotonic |
| 3 分割平面を局所集合に | **素通り** | cp1 / cp2 / cp25 / cp3 / depth_monotonic |

理由は 2 つあり、**どちらも検査の作りから来る構造的なものです。**

1. **比較関数が頂点を値で同一視しています。** それは第2段がやることそのものなので、
   第2段を止めても比較は一致します。**変異 1 に対して構造的に盲目です**
2. **$A\cap A$ と $A\cup A$ を互いに比べています。** 両方に同じ変異がかかるので
   差が出ません（深度 ≥ 1 の検査）

**素通りを `tests/CMakeLists.txt` に固定しました**（§10.5 の規律）。
記録しておかないと「冪等性があるから安心」と誤解されます。

#### 代わりに第2段を直接突く検査を足しました

**出力に値の重複する頂点が無いこと**を `test_cp3.cpp` で見ます。第2段は
「値が厳密に等しいものを併合する」ので、併合後に等しい値が 2 つ残ることはありません。
比較を経由しないので、**第2段を止めれば必ず落ちます**（変異 1 で cp3 の失敗が
222 → 288 件に増えました）。

**冪等性の価値は別のところにあります。** 深度 ≥ 1 の比較は、$A\cap A$ と $A\cup A$ が
**共平面重複で逆向きの選択規則を通る**ことを利用しています（§10.2.2 の但し書き）。
規則の食い違いはここで出ます。変異テストの検出力とは別の軸です。

---

## 6. つまずいた点（再発防止）

### 6.1 八分木は座標範囲全体を分割する ★

**最も重要な発見です。**

最初のコーパスは立方体を 0〜633 に置いていました。b=21 の深度 2 のセル境界は
$\{-2^{20}, -2^{19}, 0, 2^{19}, 2^{20}\}$ なので、立方体はすべて 1 セルの中に収まり、
**深度を上げても断片数が変わりませんでした**（48 → 48）。

このとき深度不変性の検査は**通ります**。しかし何も検証していません。
気づかなければ「空回りしている緑」を CP2 まで持ち込むところでした。

入力を座標範囲に見合った大きさ（−600000〜700000）に直したところ、断片が 48 → 192 になり、
深度が実際に効くようになりました。

**コーパス全体をこの規律で作る必要があります。**実データでは量子化がモデルのバウンディング
ボックスを格子全体に写すので、テストの入力も座標範囲に見合った大きさにするのが自然です。
`tests/corpus.hpp` の `grid_scale_box` がこの意図を持った構築子です。

> 「テストが緑」と「テストが検証している」は別だ、という Phase 0 の教訓
> （`IMPL-phase0` §4.3 の変異テスト）が、別の形で再来しました。

### 6.2 `PlaneTable` の代表の向きは任意

代表は「最初に登録された平面」です。立方体の $-X$ 面から作った平面は法線が $-X$ 向きなので、
`side(t.at(id), p) > 0` が「$x$ が大きい側」とは限りません。

セル面のクリップで保持側を反転させる処理（`r.flipped ? -base : base`）が要りました。
**呼び出し側は代表の向きを仮定してはいけません。**

テストでも一度この仮定を置いて落ちました（`test_split_through_vertex`）。

### 6.3 開領域割り当ては幾何を丸ごと落とす

§4.2 は「共有面 $F$ 上で接するだけの三角形 $Q$ が片側のセルから漏れる」と書いていますが、
実測では**どのセルにも入りません**。

境界 $x = m$ の両隣について、下側は `tri.lo >= chi`、上側は `tri.hi <= clo` で
どちらも弾かれるためです。仕様の記述より深刻な壊れ方をします。
§10.5 の変異 2 はこれを突きます。

### 6.4 テストの期待値を 3 回間違えた

| 誤り | 実際 |
|---|---|
| 積み重ねた立方体の共有平面は 1 枚 | 真上に積むと側面 4 枚も一致するので 5 枚（合計 7 平面） |
| 開領域割り当てでは片側に落ちる | どのセルにも入らない（6.3） |
| `x=0` で切れば正側が残る | 代表の向き次第（6.2） |

いずれも**実装ではなくテストが誤り**でした。期待値を書くときに、
仕様の言葉をそのまま信じずに実際の構成を数えるべきでした。

### 6.5 `ninja` の取りこぼし（Phase 0 から継続）

ヘッダを変更したのに挙動が変わらない事象が再発しました。`CLAUDE.md`「環境について」の
とおり `touch` か `--clean-first` が要ります。CP1 の作業中も 2 回踏みました。

---

### 6.6 「平面上」と「境界上」を取り違えた ★

CP1 の代表点の条件（相手の全平面に対して符号が非零）は、**ケース 2 で最初の断片から
落ちました。** 原因は 2.5 のとおりで、平面上にあることと境界上にあることの混同です。

**教訓は、条件を「十分条件」で書いたまま強さを確かめなかったことです。**
「全平面に対して非零 ⟹ 境界上でない」は正しく、CP1 の一般位置では成り立ちました。
しかし逆は偽で、退化配置では十分条件のほうが先に破れます。
§7.2 に「前提が破れうる箇所」として挙げていた項目そのものです。**予告どおり破れました。**

### 6.7 §9.0 の番人は「各段で狭義増加」にできない ★ 仕様の承認待ち

§9.0 (2) は「断片数が深度 0→1→2→3 で狭義単調増加すること」を求めていますが、
**ケース 5 と 8 では成立しません**（5.2 のとおり 48 → 48 → 48 → 192）。
面がセル境界に乗るケースでは、セル境界平面が入力平面と同一 ID になるため、
深度を上げても新しい切断が入らないからです。**ケース 5 の定義そのものから来ます。**

**生の断片数（重複を落とす前）で判定するのも誤りです。** 規律違反の配置でも、面がたまたま
境界に乗っていれば重複だけで数が増え、空回りを見逃します。実際 CP1 で踏んだ配置
（立方体を 0〜633）は $x=0$ に面が乗るので、生の数は 48 → 60 と**増えてしまいます**。

そこで判定を **「各段で非減少 かつ 深度 0 → 3 で狭義増加」**（正準化後の断片数）に
しました。CP1 の配置は 48 → 48 のままなので検出できます。
`tests/csg/test_depth_monotonic.cpp` は**この検出力自体もテストしています。**

**§9.0 は「増加しないケースがあれば理由を記録して仕様の承認を取ること」としています。
これがその記録です。判定基準の変更について承認をお願いします。**

### 6.8 コーパスの座標を絶対値で書くと b 依存で空回りが再発する

6.1 の罠は `b` が変わると形を変えて戻ってきます。CI は b=21 と b=26 を回しますが、
絶対座標で書いたコーパスは b=26 側で座標範囲の 1/32 になり、深度掃引が空回りします。

そこで位置を $2^{b-1}$ に対する**比**で書くようにしました（`kritest::at(num, den)`、
分母は 2 の冪）。b が変わっても格子点のまま、セル境界との相対関係も保たれます。
b=26 で全テストが通ることを確認済みです。

### 6.9 軸平行コーパスはバグも変異も隠す ★ 最も重要

CP2.5 で分かったことを 1 箇所にまとめます。**軸平行な立方体だけのコーパスは、
3 つのことを同時に隠していました。**

| 隠していたもの | 実測 |
|---|---|
| §5.2 の 4 平面同時交差 | 軸平行では原理的に 3 枚が上限。斜面で 6〜9 枚 |
| `clip_edges` の開始位置のバグ（2.3.1） | 軸平行では断片が常に長方形なので発現しない |
| §10.5 の変異 1（第2段の無効化） | **`test_cp2` は変異 1 を入れても 382 検証すべて通る** |

3 つ目は実際に確かめました。

```
cp2  （軸平行のみ）+ 変異 1 → [  OK  ] 382 検証        ← 素通り
cp25 （斜面）      + 変異 1 → [ FAIL ] 98 / 219 件失敗  ← 検出
```

軸平行では値併合が 0 件（第1段が全頂点を捕まえる）なので、第2段を消しても何も
変わりません。**変異テストのコーパスには斜面の対を必ず入れてください。**

### 6.10 位相検査と体積検査は役割が違う

変異 1 を体積検査にかけても**通ります**（10945 検証すべて OK）。第2段を止めると
継ぎ目で頂点が割れますが、**幾何は変わらないので体積は同じ**です。

| 検査 | 捕まえるもの | 変異 1 |
|---|---|---|
| §10.1 位相 | 縫合の失敗、面の過不足 | **検出** |
| §10.3 体積 | 断片の選択の誤り（別の立体を作る） | 素通り |

**どちらか一方では足りません。** 位相は「正しい形か」を見ず、体積は「割れているか」を
見ません。両方を全ケースで回してください。

---

## 7. Phase 2 への申し送り

### 7.1 実装済みになったもの

**共平面重複の処理**は入りました（2.10）。ケース 2 / 8 で検証済みです。
検出は「符号 0」ではなく「同一領域に両 owner の断片が居ること」で行います。

`Difference` で採用した B の断片の**向きの反転**も入れました。CP1 は union しか
回していなかったので露出していませんでしたが、$A \setminus B$ の境界では B の法線が
逆になるため、頂点順を反転しないと向きが整合しません。

**§10.6 の CI ジョブが揃いました。** 既存の CI にはプラットフォーム・GMP・可搬経路・
ベンチ・format があり、欠けていたのは変異テストと Manifold の 2 つだけでした。
どちらも入り、§10.4 は**開発コンテナでは実施できないまま CI で満たせています**（7.7）。

§10.3 の体積恒等式と解析的期待体積、§10.1 の非多様体辺の次数、§5.4 の併合の局所性も
入りました（5.4、6.10）。

### 7.2 前提が破れうる箇所（CP2.5 の結果で更新）

| 箇所 | 前提 | 状況 |
|---|---|---|
| 2.1 面併合 | 併合後の多角形が凸 | 未破れ（コーパスが凸面のみ） |
| 2.1 面併合 | 境界ループが単一 | 未破れ |
| 2.5 代表点 | 各領域に「全成分が非零の頂点」を持つ断片がある | **破れた**（ケース 2）。2.5 で条件を置き換え |
| 2.5 代表点 | 境界上に無い**頂点**がある | **破れた**（ケース 2）。2.5.1 で対角線の中点に落とした |
| 2.5.1 中点 | 断片の頂点数が 4 以上 | **破れた**（ケース 2T）。2.5.2 で 3 頂点の重心に落とした |
| 2.5.2 重心 | 共線でない 3 頂点がある | 未破れ。全 3 つ組を試すので非退化なら必ず決まる |
| `intersect3` | 3 平面が一点で交わる | 未破れ |
| 斜面を持つ入力 | パイプラインが多様体を出す | **解決**（2.3.1 の `clip_edges` の誤り 1 件）|

**「未検証」と書いた前提は 2 つとも、次のチェックポイントで実際に破れました。**
予告した箇所が予告どおり破れるのは検査体系が効いている証拠です。全前提を事前検証して
から進むほうが確実に高くつくので、この進め方自体は変えません。

**ただし 1 点、安く直せる誤りがありました**（CLAUDE.md に昇格済み）。

代表点の選択は 全非零頂点 → 中点 → 重心 と **2 回破れました**が、
**最も一般的な重心を最初に置いて、速い経路を後から足せば停止は起きませんでした。**
ビット幅を惜しんで狭い十分条件から始めたのが原因です。フォールバック連鎖では
「最も一般的なものを先に用意する」ほうが正しい順序です。

### 7.3 斜面の失敗は解決しました（記録）

CP2 の時点で「斜面を持つ入力が壊れる」と申し送りましたが、**原因は `clip_edges` の
開始位置の誤り 1 件でした**（2.3.1）。直した結果、2T / 4T / 5T が 3 演算 × 深度 0〜3 で
通っています（5.3）。

切り分けの手順は §11 の (a)(b) がそのまま効きました。

| # | 試したこと | 結果 |
|---|---|---|
| a | 四面体単体の冪等性 | **通った** → 単一メッシュ処理は無罪。問題空間が半分に |
| b | 立方体と四面体を分離配置 | **通った** → 分類・選択・向き付けも無罪 |
| — | 非多様体辺の**次数**を数える | **全部が次数 1** → 面が「余分」ではなく「欠け」だと確定 |
| — | $\partial A$ / $\partial B$ を別々に分割して閉じているか | $\partial B$（四面体）だけ壊れる → 分割そのものに絞れた |

**(c) Manifold は切り分けには使っていません。** 開発コンテナでは外部ネットワークが
遮断されており `git clone` も `settings.json` の deny 対象なので、その場では統合できません。
代わりに**非多様体辺の次数**を数えました。次数 1 なら欠け、3 以上なら余分で、
$\chi$ が区別しないものをそのまま区別できます。**Manifold 無しでも同じ問いに答えられました。**

Manifold 自体はその後 CI ジョブとして入りました（7.1、7.8）。ただし
**切り分けの道具としては次数のほうが速い**ので、この順序でよかったと考えています。

### 7.4 変異テスト（§10.5）— 3 件とも入りました

期待値を CMake の表として書き下し、`ctest` の `WILL_FAIL` に検証させています。
**素通りする組合せも固定しました。** 記録しないと「変異テストがあるから安心」と
誤解されます。

| # | 変異 | 検出するコーパス | 検出しないコーパス |
|---|---|---|---|
| 1 | 第2段（値ベースの併合）を無効化 | cp25（斜面）98/219 件 | **cp2（軸平行）382 検証すべて通過** |
| 2 | 割り当てを開領域に変える | cp2 / cp25 / depth_monotonic | — |
| 3 | 分割平面を割り当て集合に変える | cp1 / cp2 / cp25 / depth_monotonic | — |

**変異スイートは検査 OFF で回します。** 検査 ON だと変異 3 が `KRISITE_CHECK`
（`fragment_sign` の不変条件）で abort します。それも検出ですが、**測っているものが
違います。** 変異テストが答えるべきは「テスト体系がこの誤りを検出するか」であって
「表明が止めるか」ではありません。構成時に `FATAL_ERROR` で止めるようにしました。
（加えて CTest は abort を `WILL_FAIL` で拾えません。）

変異 3 は**2 つの独立した機構**が捕まえます。CI は両方を確認します。

### 7.5 未着手の一覧

- 性能の**時間**の計測（回数は 5.6 で測った。Phase 1 に性能要件は無い）
- 性能の計測（Phase 1 に性能要件は無いが、Phase 2 の判断材料になる）
- §4.3.3 の断片数を `BENCH.md` に全ケースぶん記載（5.5 に表はある）

### 7.6 仕様に足すと良さそうなこと

**§9 のコーパス定義に「座標範囲に見合った大きさにすること」の規律**（6.1）。
→ 第6版の §9.0 で入りました。

### 7.7 仕様の確認をお願いしたいこと — **すべて解決しました**

| 依頼 | 解決 |
|---|---|
| §9.3 の 4T 拡張 | 第8版 |
| Manifold を CI で実施 | 第8版（`manifold` ジョブ） |
| §9.3 の適用範囲を「出力に現れる接触」まで広げる | 第10版（§9.3 の表に「演算の結果として接触が生じる」を追加） |
| §9.3.1 の適用条件が辺接触を覆っていない（7.9） | **第11版**（§9.3.0 / §9.3.0.1 / §9.3.1 の改稿） |

未回答の依頼はありません。

### 7.8 Manifold の意味論 — **私の説明が誤っていたので訂正します** ★

#### まず訂正

CP2.5 の報告で「頂点を分裂させれば $\chi$ が 2 増えて $g=0$ になる」と書きました。
**算術が合っていません。** 仕様担当の指摘のとおりです。

頂点 1 個を 2 個に分裂させると $V$ が 1 増えるだけなので、**$\chi$ は 1 しか増えません。**
正しくは **$k$ 分裂で $\chi$ が $k-1$ 増える**であって、$k$ は配置ごとに違います。

**そして $g$ を比較したこと自体が誤りでした。** リンク構造を実測して分かりました。

#### 実測（CP2.5 時点、`tests/csg/test_cp25.cpp` が毎回出力します）

| ケース | 演算 | V | E | F | $\chi$ | C | g | 非多様点 | **扇の数** | **$\chi$ 分裂後** | 扇が同一成分 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 4T | $\cup$ | 16 | 39 | 26 | **3** | 2 | 1 | 1 | **2** | **4** | **0** |
| 4T′ | $A \setminus B$ | 10 | 18 | 12 | **4** | 3 | 1 | 1 | **3** | **6** | **0** |

**(1) 4T の $\chi$ は 3 で、奇数です。**

$g_{\text{total}} = C - \chi/2$ は整数除算なので、$\chi=3$ のとき $g = 2 - 1 = 1$ になります。
**これは種数ではありません。** $\chi$ が奇数の閉曲面は存在せず、$g$ は切り捨ての産物です。
`chi_even` という別のフラグがあるのは、まさにこれを弾くためでした。

つまり **4T の「$g$ が 1 ずれる」は見かけで、実際の差は $\chi$ で 1 です**（3 → 4）。
仕様担当の「$\chi$ の差が 2 なら単純な 2 分裂では説明できません」という前提は、
**4T については $\chi$ の差が 1 なので当てはまりません。**

**$\chi$ が奇数のとき $g$ を比較してはいけません。** 私はそれをやりました。

**(2) 4T′ は $\chi$ の差が 2 で、指摘のとおり 1→3 分裂でした。**

$\chi = 4 \to 6$、扇の数は **3**。仕様担当が挙げた 2 つの可能性のうち
「その点で 3 つの錐が出会っている（1→3 分裂）」が正解です。

**(3) 円環ではありませんでした。**

扇が同一の連結成分に属する頂点は **両ケースとも 0 件**です。すべての扇が別々の
連結成分にあるので、**錐は分離しており、分裂で多様体化できます。**

CP2.5 の報告で「リンクが円環になる」と書きましたが、**あれは測定ではなく推測でした。**
測ったら違いました。円環かどうかは `vertices_fans_in_one_component` が数えます。

#### 結論と留保

$k$ 分裂後の予測 $\chi$（4 と 6）は、**Manifold の値と完全に一致します**
（$C=2, g=0 \Rightarrow \chi=4$ / $C=3, g=0 \Rightarrow \chi=6$）。
機構としては **$k$ 重の頂点分裂**で説明がつきます。

**ただしこれは「Manifold がそう振る舞う」ことの証拠であって、
「それが正しい位相」の証拠ではありません。** 仕様担当の留保 2 のとおりです。

Manifold は exact kernel ではなく許容誤差ベースなので、**厳密に退化した入力では
摂動して退化を消している可能性があります。** 4T / 4T′ はまさに厳密な退化配置です。

同様に、$C$ の一致は「同じ立体である」ことを**含意しません。** CP2.5 の報告で
「立体としては同じもので、その 1 点の表現だけが違うと言い切れます」と書きましたが、
**$C$ の一致だけからは言えません。** 言えるのは、$\chi$ の差が分裂の予測と一致し、
かつ §10.3 の体積が恒等式・解析値の両方で一致している、という 2 つの事実です。
体積が一致することのほうが「同じ立体」の根拠として強いので、そちらを根拠にすべきでした。

#### Phase 2 への申し送り

**選択肢は「分裂させるかどうか」で合っています**（円環が出ていないため）。
ただし CP3 で他のケースを追加したときに **`vertices_fans_in_one_component` が
非零になったら、その時点で選択肢が変わります。** テストがそれを検出して停止します。

### 7.9 §9.3.1 の適用条件と `oriented` — **第11版で解決しました**

CP3 の時点で「§9.3 / §9.3.1 が辺接触を覆っていない」と申し送りました。
**第11版で解決しています。** ただし解決の仕方が私の提案とは違ったので、記録します。

#### 私の提案と、仕様担当の判断

私は「壊れる検査は接触の種類で決まる」として、辺接触のときに `oriented` を**除外**し、
代わりに次数 2 に限った整合（`oriented_on_manifold_edges`）と接触辺での本数の釣り合い
（`excess_edges_balanced`）の 2 本を課す形を提案しました。

**仕様担当は除外ではなく検査そのものの一般化を選びました。**

$$\text{すべての辺について}\quad \#(u,v) = \#(v,u)$$

多様体辺なら $1 = 1$、次数 4 の接触辺なら $2 = 2$ です。
**私が提案した条件 3・4 は、この 1 本に統合されます。**

**こちらのほうが正しい判断です。** 理由は 2 つあります。

1. **除外が 1 つ減ります。** 除外条件が増えるほど、本物のバグが隠れる面積が広がります。
   私は「検査が次数 2 を前提にしている」ことを見つけながら、**検査を直すのではなく
   除外を足すほうへ進みました。** 見つけた側が直すべきでした
2. **接触辺のまわりの向きまで拘束できます。** 除外すると、そこは無検査になります

適用条件も「接触の種類で分ける」形から **「辺次数の性質だけで書く」形**になりました。
「どの辺が接触辺か」を独立に知る手段が無い以上、こちらのほうが実装できる形です。
とくに「**次数 1 が無い**」→「**すべての辺の次数が偶数**」への強化は、
次数 3・5 という「接触では説明できない異常」も同時に弾きます。

#### 検出力は落ちていません（実測）

**「一般化しても検出力は落ちない」は仕様の主張であって、測るまでは仮説です。**
`tests/mesh/test_topology.cpp` に接触辺での実測を足しました。

| 構成 | `max_edge_degree` | `edges_odd_degree` | `oriented` |
|---|---:|---:|---|
| 辺だけを共有する 2 立方体 | 4 | 0 | **true**（旧形では必ず false） |
| 同上、接触辺に接する三角形を 1 枚裏返す | 4 | 0 | **false** |

次数 2 の辺では両者は**同値**です（$\#(u,v)=\#(v,u)$ かつ次数 2 なら $1=1$ しかない）。
次数が奇数なら $\#(u,v) \ne \#(v,u)$ が必ず成り立つので、そこでも落ちます。
**緩むのは偶数次数 $\ge 4$ の辺だけ**で、そこは §9.3.1 の「次数の最大が 4」が縛ります。
`ok()` は `edge_manifold`（全辺が次数 2）を併せて要求するので、**`ok()` の強さは不変**です。

#### 実装の変更

| 対象 | 変更 |
|---|---|
| `TopologyReport::oriented` | `fwd == 1 && bwd == 1` → **`fwd == bwd`** |
| `TopologyReport::edges_odd_degree` | 新設。次数が奇数の辺の数 |
| `oriented_on_manifold_edges` / `excess_edges_balanced` | **削除**（一般形に統合） |
| `nonmanifold_vertices_off_excess` | → **`nonmanifold_vertices_unexplained`**。$k \ge 2$ 個の扇は**それ自体が説明**なので未説明に数えない |
| `tests/corpus_expect.hpp` | `exclusion_conditions_ok` を接触の種類に依らない 1 本に統一 |
| `tests/csg/test_cp3.cpp` | 除外件数を**接触の次元別**に記録（§13） |

除外は **3 構成のまま**です（次元 0 が 2、次元 1 が 1）。

#### 残る 1 件 — 仕様の記述と実測が食い違っています ★

**§9.3 の表が 4T′ を「リンクが円環になる」と書いていますが、円環ではありません。**

§9.3.2 の watch 項目（「4T′ が円環ならこの条件を満たさない」）は、
**CP2.5 の時点で実測済みで、答えは「円環ではない」です。**

| ケース | 演算 | 深度 | $\chi$ | 非多様点 | 扇の数 | $\chi$ 分裂後 | **扇が同一成分** |
|---|---|---|---:|---:|---:|---:|---:|
| 4T | $\cup$ | 0〜3 | 3 | 1 | 2 | 4 | **0** |
| 4T′ | $A \setminus B$ | 0〜3 | 4 | 1 | 3 | 6 | **0** |

`vertices_fans_in_one_component` が全深度で 0 なので、**すべての扇が別々の連結成分に
属します。** 錐は分離しており、$k$ 分裂で多様体化できます。
したがって **§9.3.1 の 4 つ目の条件（リンクが $k \ge 2$ 個の扇）は成立**しており、
除外の枠組みは今のままで成り立ちます。

§9.3 の表の「リンクが円環になる」は**「リンクが 3 個の扇になる」**が正しい記述です。

---

## 8. 検証

| テスト | 検証件数 | 何を見るか |
|---|---:|---|
| `fixed_int` | 2,514,793 | Phase 0 の算術（継続） |
| `predicates` | 847,768 | Phase 0 の述語（継続） |
| `degenerate` | 442,517 | Phase 0 の退化ケース（継続） |
| `plane_order` | 378,235 | `plane_same` と `plane_cmp` の一致、スカラー倍不変性、全順序 |
| `plane_order_gmp` | 6,000,000 | 平面述語の GMP 差分（正解器は mpq で比を直接比較） |
| `topology` | 82 | **検査器自身**の検証（トーラス、空洞、T 字接合、頂点のみ共有） |
| `uniform_grid` | 21,216 | 境界座標、隣接セルの平面共有、閉領域割り当て |
| `faces` | 700 | 頂点が 3 平面の交点として入力頂点に厳密一致すること |
| `fragment` | 1,173 | 分割後も表現が閉じていること |
| `cp1` | 28 | CP1 本体 |
| `bitwidth` | 40 | 実測ビット幅 vs 理論上界 |

構成: **b=21 / b=26 / Debug / 可搬経路（`__int128` なし）**のすべてで緑、警告ゼロ。
**7 コミットすべてが単独でビルド・テストを通る**ことを worktree で確認済みです。

規模: `include/` 17 ファイル 2,635 行、`tests/` 17 ファイル 3,505 行。

### 正解器の独立性

`plane_cmp` は交差乗算で比較しますが、GMP 正解器は **mpq で先頭成分を割って比を直接比較**
します。同じ変形を使う正解器は正解器ではない（`CLAUDE.md`「通しての約束」）ためです。

`plane_same`（6 本の小行列式）と `plane_cmp`（交差乗算）も**別々の式から導いた**ので、
両者が一致することを性質テストの中心に据えています。片方だけ間違えれば必ず食い違います。
