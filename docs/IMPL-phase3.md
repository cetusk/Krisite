# Krisite — Phase 3 実装ノート

> **仕様は [`SPEC-phase3.md`](SPEC-phase3.md)、判断の材料は
> [`DECISION-core-contract.md`](DECISION-core-contract.md)、議論の経緯と訂正は
> [`LOG-phase3-design.md`](LOG-phase3-design.md)。**
>
> 本書に書くのは**なぜそう作ったか**です。コードを読めば分かることと
> `git log` で分かることは書きません。

---

## 1. CP1 — 段 0 と辺平面（2026-08-28）

### 1.1 §3.1.2 の導出は正しい。$b{=}26$ でも 4 リムに収まります ★

**CP1 の最優先項目でした**（`LOG-phase3-design.md` §8 の未決事項 1）。
実測は `tests/arith/test_bitwidth.cpp`（乱択 $10^6$ 規模）。

| 量 | 理論 | $b{=}21$ 実測 / リム | $b{=}26$ 実測 / リム |
|---|---|---|---|
| **辺平面の法線** $N_e$ | $b{+}1$ | **22 / 1** | **27 / 1** |
| **辺平面のオフセット** $d_e$ | $2b{+}2$ | 42 / 1 | 52 / 1 |
| 辺平面 3 枚の交点からの `side` | $5b{+}11$ | 106 / 2 | 131 / 3 |
| 軸平行 2 枚を含む交点 $w$ | $2b{+}6$ | 43 / 1 | 53 / 1 |
| 軸平行 2 枚を含む交点 $xyz$ | $3b{+}8$ | 65 / 2 | 80 / 2 |
| 支持平面の法線（比較） | $2b{+}3$ | 43 / 1 | 53 / 1 |
| **最悪ケース `side`** | $9b{+}20$ | 191 / **4** | **236 / 4** |

**辺平面は支持平面より小さい**（法線 27 対 53 ビット）ので、最悪ケースは支持平面 3 枚の
ままです。**$b{=}26$ で 254 ビット = 4 リム**、退避条件 1（`SPEC-phase3.md` §15）は
発生しませんでした。

> **案 A（$N_s \times (p_2-p_1)$）なら 6 リムでした。** 構成方式の選択が決定的だ、
> という仕様の指摘は数値でも裏づけられます。

**軸選択の可能性の論証には補いが要りました**（§3.1.3 に反映済み）。
$(\Delta\times e_i)\times(\Delta\times e_j) = (\Delta\cdot(e_i\times e_j))\,\Delta$ なので、
**$\Delta$ の第 $k$ 成分が 0 のとき $i$ と $j$ が平行**になり、候補方向が 2 つに減ります。
それでも $N_s$ に平行になり得るのは高々 1 つなので結論は保たれます。

### 1.2 段 0 — 代表点は 2 経路とも実際に使われます

| 経路 | 実測（22 ケース × 3 演算 × 深度 0〜3） |
|---|---|
| 主経路（float ヒント + 軸平行直線） | **2,925** |
| 予備経路（角の 2 辺平面を内側へずらす） | **1,395** |

**予備経路は飾りではありません。** 深度が上がるほど断片が小さくなり、丸めた整数の
重心が多角形から外れます（深度 0 で 0 件、深度 3 で過半）。**両方が走ることを
`interior` の番人で固定しました。** 片方が 0 になったらテストが落ちます。

#### 1.2.1 刻みを細かくする機構を足しました

EMBER §4.4 は「平面係数を精度限界近くまでスケールアップして刻みを細かくする」と
書いていますが、**具体的な上限は示していません。** Krisite は固定幅なので、
`kNormal` / `kOffset` の余裕から**上げ幅を機械的に決めます**（`plane_scaled_for_shift`）。

平面 $(N,d)$ と $(2^sN, 2^sd)$ は同じ平面なので、係数を上げてから $d$ を 1 動かすと
幾何的な移動量が $2^{-s}$ になります。**型は変わりません。**

予備経路は「角ごとに、粗い刻み → 細かい刻み」の順に試します。実測では細かい刻みまで
必要になった配置はありませんでしたが、**細長い断片では効くはずなので残します。**

#### 1.2.2 境界判定が不要になりました

Phase 1 / 2 は候補ごとに `point_on_boundary` を呼んでいました。段 0 の代表点は
**相対内部**なので、$\partial B$ に載りません（$\partial B$ は $B$ の平面の和集合に
含まれ、断片は $B$ の平面配置のセルなので内部はどの平面とも交わらない）。

**判定は消しましたが、表明としては残しました。** 前提が破れたら止まります。

### 1.3 最大ビット幅が下がりました

| | $b{=}21$ | $b{=}26$ |
|---|---|---|
| 段 0 の前（`kTriSide` = $21b{+}46$） | 487 ビット / **8 リム**（512） | 592 / **10 リム**（640） |
| **段 0 の後**（最大は `kCmpH` = $13b{+}27$） | 300 / **5 リム**（320） | 365 / **6 リム**（384） |

`widths.hpp` から**中点系・重心系の 4 定数**（`kMidSide` / `kMidOrient2dH` /
`kTriSide` / `kTriOrient2dH`）と、`HMidPointD` / `HTriPointD` の型と述語が消えました。

**`kCmpH` は残ります**（`LOG-phase3-design.md` §6 の訂正 3）。頂点の併合に要るので、
`to_mesh` へ移るだけです。

### 1.4 変異 14 — 位相でも体積でも捕まらない誤り ★

軸の選択を「最小の $k$」から「最大の $k$」に変える変異を足しました。

**幾何としては正しい平面が返ります。** 辺の直線を含み支持平面と一致しないという条件は
満たすので、**位相も体積も 1 件も落ちません**（`cp3` / `adaptive` / `split_semantics`
すべて素通りを実測）。壊れるのは**決定性**だけです。

**決定性を直接見ている検査が無ければ、誰も捕まえません。** `interior` の正準性の検査が
主検出器です（21 / 147 件失敗）。

> **テスト側の判定は実装と独立に書きました。** 実装の条件式を呼ぶと「同じ変形を使う
> 正解器」になります（`CLAUDE.md`）。棄却理由（$N = 0$ か $N \parallel N_s$）を
> テスト側で再実装しています。

### 1.5 つまずいた点

**`det2(a,b,c,d) = a\cdot d - b\cdot c` の引数割り当てを 1 か所間違えました。**
外積の 3 成分のうち $c_y$ だけ順序を取り違え、**平行判定が常に偽**になっていました。
支持平面と同一の辺平面が返り、最初の煙テスト（軸平行な三角形）で第 3 点の `side` が
0 になって露見しました。**式をコメントに併記して再発を防いでいます。**

**条件 2 の判定を `int64` で書いたのも誤りでした。** 法線は $2b{+}3$（$b{=}26$ で 55
ビット）、辺平面の法線が $b{+}1$ なので、積は 82 ビットで溢れます。**固定幅で書き直しました。**

---

## 2. CP2 — 入口と出口の分離、分類の台の移動（2026-08-28）

### 2.1 契約は閉じました

| 関数 | 実測 |
|---|---|
| `from_mesh` | 全 44 メッシュ。**面併合なし**（辺平面を構成するので隣接情報が要らない） |
| `boolean` | 二項正解器と **264 / 264 一致**（22 ケース × 3 演算 × 深度 0〜3、$(C,\chi)$） |
| `to_mesh` | 往復 44 件で元の位相を再現 |

**§10.3.1（連鎖でビット幅が伸びないこと）を確かめました。**

| 段 | 最大ビット幅（$b{=}21$） |
|---|---|
| 1 段 | **142** |
| 2 段 | **142** |
| 3 段 | **142** |

上界は $7b{+}14 = 161$。**CSG が新しい平面を作らない**（OEBSP §3.1）ことが、
実測でも確かめられました。連鎖しても構成点は入力平面の集合から 3 枚を選んだ交点のままです。

### 2.2 分類の台を移すと、前提が 3 つ壊れました ★

**「相手のスープ」から「生成 0 の整数メッシュ」へ移す**のが CP2 の中心でした
（`SPEC-phase3.md` §14）。移した結果、Phase 1 / 2 が暗黙に置いていた前提が壊れます。
**どれも連鎖でしか出ません。**

| # | 壊れた前提 | 正 |
|---|---|---|
| 1 | 「source の平面 = スープに残っている多角形の支持平面」 | **前段で消えた面の平面が抜けます。** source のメッシュ全体から集めること |
| 2 | 「その source の多角形がセルに無い = 曲面が無い」（early-out） | **面が消えても曲面はそこにあります。** 判定は source の三角形で行うこと |
| 3 | 「重なりはスープの多角形どうしで検出できる」 | **スープに面が残っていなくても、source の曲面とは重なります。** `boundary_orientation` で直接見ること |

**3 が最も分かりにくい壊れ方でした。** 重なりを見落とすと代表点が source の**境界上**に
乗り、レイキャストの契約（`point_on_boundary` でないこと）が破れます。**答えが黙って
変わるので、位相検査でしか気づけません。**

### 2.3 面併合をやめた代償 — 共平面の重なりが揃わない

入口で面併合をやめたので（§3.1.4）、同じ平面に載る面が**別々の三角形分割**を持ちます。
重なりの領域が一致せず、`region_key` で潰せません。

**EMBER §4.3 の C4 と同じ手当てをしました。** 支持平面が同じ断片どうしを、
**相手の辺平面**で切って揃えます。自分の辺平面で切っても no-op なので、まとめて適用できます。

### 2.4 残った限界 — 同じメッシュを 2 度使う連鎖 ★

**内外の 1 ビットでは「同じ曲面を 2 度跨いだ」を表せません。**

| 連鎖 | 結果 |
|---|---|
| **別のメッシュ**との連鎖 $(A \cup B) \setminus D$ vs $(A \setminus D) \cup (B \setminus D)$ | **22 / 22 一致** |
| **同じメッシュ**を 2 度使う $((A \cup B) \setminus B) \cup B$ | **17 / 44 で食い違う** |

後者では $B$ が source 1 と source 2 の両方に現れ、その曲面が二重になります。
巻き数なら $w$ が 2 になるだけですが、**真偽値では表せません**。

> **CP3 の「真偽値 → $\mathbb{Z}^n$」がこれを解きます**（`SPEC-phase3.md` §14 の CP3 の変更 1）。
> CP2 の範囲（§10.3 の連鎖の厳密性 = 丸めが入らないこと、§10.3.1 のビット幅）は
> 満たしています。**丸めの問題ではなく、分類の表現力の問題です。**

### 2.5 つまずいた点

**添字を無効化しながら回しました。** 共平面の整列で、グループごとに `local` から
消しては足していたので、**2 つ目のグループ以降の添字が壊れて**いました。
症状は「一部の重なりだけ揃わない」で、**ケース 3 でしか出ませんでした。**
新しい配列を組み立てる形に直しています。

**`det2(a,b,c,d) = a·d − b·c` を今回も間違えました**（CP1 に続き 2 度目）。
式をコメントに併記する規律を、`plane_from_edge` に続き `boundary_orientation` にも
入れました。

---

## 3. CP3 段 1 — 真偽値から $\mathbb{Z}^n$ へ（2026-08-28）

### 3.1 CP2 の限界が消えました

| 検査 | CP2（真偽値） | CP3 段 1（巻き数） |
|---|---|---|
| 二項正解器との $(C,\chi)$ 一致 | 264 / 264 | **264 / 264** |
| 別メッシュとの連鎖の自己整合 | 22 / 22 | **22 / 22** |
| **同じメッシュを 2 度使う連鎖** | **17 / 44 で食い違う** | **44 / 44 一致** |
| 連鎖でのビット幅（$b{=}21$） | 142 / 142 / 142 | **142 / 142 / 142** |

$(A \cup B) \setminus B \equiv A \setminus B$ が通ります。**曲面を 2 度跨ぐ配置は
$w = 2$ になるだけ**で、真偽値のように潰れません。

### 3.2 表面上の点の巻き数は、3 つに分けて返します

判定点が曲面に載っているとき、巻き数は表裏で違います。`winding_split` は

| 返り値 | 意味 |
|---|---|
| `w_other` | **載っていない面**だけから決まる分（表裏で共通） |
| `c_front` | 基準法線 $+N$ 側で、載っている面が寄与する分 |
| `c_back` | $-N$ 側で寄与する分 |

**載っている面を先に除くのが要点です。** 境界上からレイを飛ばすのは `crosses` の
契約違反で、答えが定まりません。

### 3.3 導出を 2 度間違えました ★

**(1) レイの向きと基準法線の関係を落としていました。**

点 $p$ をシートから $\pm N$ に $\varepsilon$ 離すと、$+X$ のレイがシートを跨ぐのは
**レイが $-N$ 側へ向かうとき**、すなわち $X \cdot N < 0$ のときだけです。

$$w(p_+) = w_{other} + [\,X \cdot N < 0\,]\cdot \mathrm{sign}(N_{S,x}), \qquad
  w(p_-) = w_{other} + [\,X \cdot N > 0\,]\cdot \mathrm{sign}(N_{S,x})$$

最初は「外向き法線が $+N$ なら裏が内側」という**向きだけ**で決めていました。
これは真偽値なら正しく、**巻き数では基準がずれます**（$w_{other}$ がレイの経路に
依存するため）。244 / 264 が食い違いました。

**(2) レイがシートと平行な場合を落としていました。**

$X \cdot N = 0$ だと、どちら側が跨ぐかを決められません。**レイの軸を、基準平面の
法線が非零な軸から選ぶ**ようにして解決しました（`crosses` と記号的摂動を軸について
一般化。摂動の形は巡回順で同じです）。

> **どちらも「真偽値では正しいが巻き数では誤り」でした。** 表現力を上げると、
> それまで隠れていた前提が表に出ます。

### 3.4 辺や頂点に載る点

同じシートを複数の三角形が共有するので、そのまま数えると二重になります。
**内部に載っているものは 1 枚ずつ、境界だけのものは 1 枚**として数えます。
（真に二重なシートが同じ向きで重なっている場合は 1 と数えます。source をまたぐ
二重は別々に数えるので、目的の配置には影響しません。）

---

## 5. CP3 item 2 は Phase 4 の前提ではありません（2026-08-28）★

**判定基準**（`SPEC-phase3.md` §14）は「**葉の分類が、可変な共有状態に触れずに書けるか**」でした。

### 5.1 コードの監査

item 1 の分類（`soup_boolean.hpp` の手順 7）が触るものを列挙します。

| 対象 | アクセス | 並列化 |
|---|---|---|
| `regions` / `frags` / `frag_*` / `planes_of_src` | 読み取り | **不変** |
| `PlaneTable::at()` | 読み取り（`const` メンバ） | **不変** |
| `interior_point(const PlaneTable&, ...)` | 読み取り | **不変** |
| `winding_split(const TriMesh&, ...)` | 読み取り | **不変**（source は共有読み取り専用） |
| `Indicator::eval` | 読み取り | **不変** |
| `PointCache` / `wcache` | 書き込み | **メモ化**。スレッド局所で足りる |
| `BoolStats` | 書き込み | **カウンタ**。集約すればよい |
| `out.polys` | 追記 | **出力**。スレッドごとに貯めて結合 |

**領域どうしの依存はありません。** 各領域の結果は、不変なデータだけから決まります。
可変なのはメモ化・カウンタ・出力バッファで、いずれも並列化の定型です。

### 5.2 機構で確かめました

監査だけでは「見落とし」が残るので、**順序非依存**を検査に落としました。

> `BoolOptions::reverse_regions` で領域を逆順に分類し、**幾何の多重集合が一致すること**

| 検査 | 結果 |
|---|---|
| 22 ケース × 3 演算 × 深度 0〜3 | **264 / 264 一致** |

**これは証明ではありません**（依存があってもたまたま一致し得ます）。ただし
**依存が入ったら落ちる網**にはなります。

### 5.3 したがって item 2 は性能の項目です

| | 大域レイキャスト（item 1） | セグメントトレース（item 2） |
|---|---|---|
| 状態 | source は不変・共有読み取り専用 | 参照点と WNV を親から受け取る |
| 依存 | **各領域が独立** | **再帰木に沿った鎖** |
| コスト | $O(\text{シーン})$ / 領域 | $O(\text{局所})$ |

**依存関係で見ると、レイキャストのほうが並列に素直です。** EMBER §1.1 がトレースを
選ぶ理由も並列性ではなく性能（大域の加速構造を作らないこと）です。

> **item 2 を Phase 5 へ降格できます。** Thingi10K で分類コストが問題になったときに、
> **そこで必要性を確認してから**入れるのが「効いたから使う」の原則に沿います
> （`SPEC-phase2.md` の CP2 で同じ判断をしています）。

### 5.4 item 2 に戻るときの前提（記録）

`phase3/cp3b` に WIP があります。**着手前に確かめること**は 2 つです。

1. **参照点が面の上に載らないことの保証**（`SPEC-phase3.md` §14）。実測では参照点の
   移動 6,567 回のうち **32.5%** が退化し、候補を変えて回復していました。
   **保証ではなく再試行**で凌いでいる状態です
2. **記号的摂動**。レイキャストには §6.1 の摂動がありますが、トレースにはありません

---

## 6. CP4 — 局所 BSP（2026-08-29）

### 6.1 着手前に見つかった誤り — 出力の向き ★

**CP4 の正解器（局所 BSP ↔ 過剰分割）を体積で比べようとしたところ、
過剰分割の側が既に二項正解器と食い違いました。** CP4 で入れた誤りではなく、
**CP2 から残っていた誤り**です（`HEAD` のコードで再現を確認）。

#### 症状

`A ∪ A`（同じメッシュを 2 度）で出力は 12 三角形・$\chi = 2$・$C = 1$ と正しいのに、
符号付き体積が $-\tfrac{1}{3}$ になりました。ケース 1 の $\cup, \cap, \setminus$ でも
すべて $-\tfrac{1}{3}$ 倍です。

`check_topology` を直接当てると `oriented = 0` でした。**面の向きが混在しています。**

#### 原因

断片の**巻き順は「元の三角形の法線」基準**で、`Fragment::flipped` が
「その法線が支持平面の正準な法線と逆か」を記録しています。`from_mesh` が設定し、
`clip_fragment` / `split_fragment` が保存します。

出力の巻き順は**外向き法線**基準にしたい。外向き法線は `in_front` で決まるので、

$$\text{反転が要る} \iff \underbrace{\texttt{in\_front}}_{\text{目標}} \ne \underbrace{\texttt{f.flipped}}_{\text{現在}}$$

です。実装は `in_front` だけを見ていたので、**`f.flipped` が真の面だけ向きが反転**します。
立方体なら面の半分が裏返り、`oriented` が崩れます。

#### なぜ CP2 から CP3 まで残ったのか

**$(C, \chi)$ は面の向きを反転しても変わりません。** スープ経路にあった検査は

| 検査 | 見ていたもの | この誤り |
|---|---|---|
| 往復（§10.1） | `ok()`（向きも見る） | **ブール演算を通らないので発火しない** |
| 二項正解器との一致 | $(C, \chi)$ のみ | **素通り** |
| 連鎖の厳密性 | ビット幅 | 無関係 |
| 順序非依存 | 幾何の多重集合（両側とも同じ誤り） | **素通り** |
| 体積（`volume_gmp`） | 断片の選択 | **二項経路しか見ていなかった** |

**「スープ経路の出力を体積で見る」検査が 1 つも無かった**のが穴です。
往復では `ok()` を見ているのに、ブール演算の出力では見ていませんでした。

#### 足した検出器

1. **`volume_gmp` にスープ経路を追加**（`to_mesh(boolean(from_mesh(A), from_mesh(B)))`
   の体積が二項正解器と一致すること）。**主検出器はこれです**
2. `test_soup.cpp` の正解器比較に **`oriented`** を追加。
   接触の分裂を切っているので `ok()` は使えませんが、`oriented` は
   「各辺で $\#(u,v) = \#(v,u)$」なので次数 4 の辺でも有効です。
   **空の出力（交わらない立体の $\cap$）では向きが定義されないので除外します**
3. **変異 15** として固定（`KRISITE_MUTATION_ORIENT_IGNORE_FLIPPED`）。
   **`cp3` / `depth_monotonic` が素通りすることも登録しました** —
   位相だけの検査では捕まらない、という事実そのものを固定するためです

#### 教訓

- **`to_mesh` は `Fragment::flipped` を読みません。** 向きは巻き順だけが運びます。
  「状態が 2 か所にあり、片方しか使われない」形は、**書くほうが取り違えます**
- **不変量が鈍い方向を先に書き出すこと。** $(C, \chi)$ は向きに鈍く、体積は分裂に鈍い
  （§9.4.2 / 定理）。**両方を持って初めて網になります**
- **正解器を新しく足すときは、まず既存の実装に当ててみること。**
  CP4 の準備として体積の正解器を広げた結果、CP4 とは無関係の誤りが出ました

### 6.2 切断条件 — 「セルに居る三角形のうち、支持平面に触れるもの」

断片 $t$（支持平面 $\pi_t$）を切る平面の集合を、次で定めました。

$$\mathrm{cuts}(\pi_t) = \bigl\{\, \pi_v \;:\; v \in \text{このセルに居る source の三角形},\ \pi_v \ne \pi_t,\ v \text{ が } \pi_t \text{ に触れる} \,\bigr\}$$

「触れる」は **3 頂点の `side` が厳密に同符号でないこと**です（`detail::tri_touches_plane`）。
新しい述語ではなく、整数点に対する `side`（幅 $3b+6$）を 3 回呼ぶだけです。

**健全性**: $v$ が $\pi_t$ の片側に厳密に居るなら $v \cap \pi_t = \emptyset$、よって $v \cap t = \emptyset$
で、$t$ の内部を横切りません。触れる $v$ は $\pi_v \cap \pi_t$ で切るので、
$v \cap t$ を必ず含む直線で切られます。**「代表点 1 点で分類できる」前提が保たれます。**

#### 多角形ではなく source の三角形から作る ★

**`here`（このセルの多角形）から集めてはいけません。** 連鎖では前段で消えた面があるので、
多角形から集めると平面が抜けます。これは `planes_of_src` を source から作っているのと
同じ理由で、Phase 3 で 2 度目の同じ形の罠です。

とくに効くのが**共平面シートの境界**です。$\pi_t$ 上に載る source のシートが $t$ の一部だけを
覆うと、$c_{front}, c_{back}$ が断片上で一定でなくなります。閉多様体ならシートの境界は
隣接三角形との共有辺なので、**その隣接三角形の平面**が切断集合に入り、境界で切られます。
隣接三角形が多角形として残っているとは限らないので、source から集める必要があります。

#### 保守的に「平面まるごと」で切る

交差線分だけを入れると断片が凸でなくなります（EMBER Fig. 7）。凸性は
`fragment_vertex`（頂点 $i$ = 支持 $\cap$ 辺$[i{-}1]$ $\cap$ 辺$[i]$）と扇分割の前提なので、
**必要以上に長く切って凸を保つ**ほうが単純で速い、という EMBER の判断をそのまま採りました。

#### 切断集合は支持平面だけで決まる

$\mathrm{cuts}$ の定義に $t$ の広がりは現れません。**セル内で支持平面をキーにメモ化**しています。
これは速度のためだけでなく、**同じ平面に載る別々の多角形が必ず同じ切り方をする**ことの
保証でもあります（`region_key` で重なりを潰すために要ります）。

### 6.3 符号ベクトルを代表点で取るように変えた ★

過剰分割の下では、断片は**各 source の平面配置の 1 セル**に収まっていたので、
`fragment_sign`（断片の頂点から符号を取る）が使えました。

**局所 BSP は「シートが横切らない」ことしか保証しません。** シートの載っていない平面は
断片をまたぎ得るので、`fragment_sign` は `KRISITE_CHECK`（断片が平面をまたいでいる）で
止まります。そこで**代表点で符号を取る**ように変えました。

- **分類が断片上で一定であること**は、平面ではなく**シート**が横切らないことから従います。
  平面をまたいでも、その上にシートが無ければ巻き数は変わりません
- **キーとしての妥当性**: 閉多様体なら三角形の辺は隣接三角形との共有辺なので、
  **平面配置は三角形分割を細分します。** よって同じ符号ベクトルの点は
  「同じ三角形の内側か外側か」まで一致し、$w_{other}$ も $c_{front}, c_{back}$ も一致します
- **過剰分割の側でも意味は変わりません**（代表点は断片の内部なので、
  断片が片側にあれば代表点も片側）。両方式で同じコードが使えます

### 6.4 実測 — 断片数の比（§14 の CP4 が要求する報告）

コーパス 22 件 × 3 演算 × 深度 0〜3 = 264 構成。

| | 過剰分割 | 局所 BSP | 比 |
|---|---:|---:|---:|
| 生の断片 | 40,317 | **32,376** | **80.3%** |
| 正準化後の断片 | 20,678 | **16,253** | **78.6%** |
| 出力が変わった構成 | — | — | 101 / 264 |

切断候補は 53,025（**切った 41,538 / 省いた 11,487**）。

> **削減が 2 割にとどまるのは、比較対象が「素の過剰分割」ではないからです。**
> 既に `SPEC-phase2.md` §2.3 の絞り込み（箱を横切る平面だけ）が入っています。
> さらに退化コーパスは軸平行が多く、**平面が箱を横切る率がもともと高い**ので、
> 絞り込みの効きが実データより弱く出ます。**Thingi10K で測り直す価値があります。**

$(C, \chi)$ と体積は 264 構成すべてで一致し、三角形の集合は一致しません（それが目的）。

### 6.5 空回りの検査

**「切る」と「省く」の両方が発火したこと**をテスト自身に確かめさせています。
片方が 0 なら判定が一方に倒れていて、機構が働いていません。あわせて

- **出力が 1 件も変わらなければ落とす**（置き換えが no-op なら意味がない）
- **生の断片が減っていなければ落とす**（CP4 の目的そのもの）
- **過剰分割の側で切断候補が 0 であること**（フラグが効いていることの負の対照）

**変異 16**（`KRISITE_MUTATION_BSP_NEVER_CUT`）は切断条件を常に偽にします。
断片の相対内部をシートが横切るので、位相と体積の両方で捕まります。
**二項実装は過剰分割のままなので `cp3` は素通り**します。これも登録しました。

### 6.6 つまずいた点

- **空の出力で `oriented` が既定 `false`。** 交わらない立体の $\cap$ は空メッシュで、
  向きは定義されません。`t.empty` で除外が要ります。**48 件の偽陽性を出しました**
- **GMP が開発コンテナから消えていました。** `.deb` を `dpkg-deb -x` で入れ直しています
  （`CLAUDE.md`「環境について」）。**体積の正解器なしでは CP4 は閉じられません** —
  §6.1 の誤りは体積でしか出ないためです

### 6.7 件数の番人が黙って壊れていました ★

変異を 2 件足したら、CI の**変異テストの件数の番人**が 2 ジョブとも落ちました。
原因は変異ではなく、**件数の数え方**です。

```bash
# 壊れていた式
n=$(ctest --test-dir build -L mutation -N | grep -c "Test #")
```

`ctest -N` は**テスト番号を右詰めで揃えます。**

```
  Test  #99: mut_13_cp3
  Test #100: mut_14_soup
```

総テスト数が 2 桁から 3 桁に上がった瞬間、空白 1 個の `"Test #"` は
**3 桁のものにしか一致しなくなり**、71 件が 3 件に見えました。
**それまで正しく数えられていたのは、たまたま変異テストの番号がすべて同じ桁だったから**です。

```bash
# 直した式
n=$(ctest --test-dir build -N -L '^mutation$' | sed -n 's/^Total Tests: *//p')
```

あわせて 2 点直しました。

- **ラベルを `^...$` で固定。** `-L` は正規表現なので、素の `mutation` は
  `mutation_gmp` にも一致します。「この件数は GMP の有無で動きません」という
  workflow のコメントは、**GMP を切ったジョブでしか成り立っていませんでした**
- 期待値を 71 → 76、GMP 側を 6 → 8 に更新

> **期待値をリテラルのままにしたのは意図的です。** `CLAUDE.md`「期待値は式で持たせる」は
> **比較の件数**（コーパスが増減したら追随してほしいもの）に向けた規律です。
> ここの番人の役目は逆で、**登録が減ったことに気づく**ことなので、
> `krisite_add_mutant` の行数から導出すると削除に黙って追随してしまいます。

**教訓**: 番人そのものが壊れる形を数えていませんでした。
`grep -c` で人間向けの整形出力を数えるのは、**整形が変わった瞬間に静かに壊れます。**
機械可読な要約（`Total Tests:`）があるならそちらを使うこと。
