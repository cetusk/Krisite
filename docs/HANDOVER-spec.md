# 仕様検討担当への引き継ぎ（2026-09-13）

**前任の仕様検討担当から、後任への引き継ぎです。**

**あなたの仕事は `AGENTS.md` に、プロジェクトの規約は `CLAUDE.md` と `.claude/rules/` にあります。**
**この文書は「いま何が起きていて、私が何を間違えたか」を書きます。**

> **2026-09-14 追記（後任仕様担当）:** 本文は前任時点の記録として残す。
> 所有者の訂正により、文書は全体を読む方針とし、内容は仕様・実装・後続訂正と照合する。
> 下記の「全部読む必要はありません」は以後の引継ぎ方針として採用しない。
> また、「直ちに投入できる」「正しさは閉じた」は現時点の承認根拠にしない。
> 参照寿命・追加修復点の衝突検査・実行連鎖の停止に欠陥が確認され、
> 体積検査の旧報告も倍精度比較に限定された。修正設計への条件付き承認と
> 未検証事項は `SPEC-phase5.md` §5.10.14.74 を参照する。
> 6332e4f で起動スクリプトは `tests/tools/run_cp23.sh` に移された。
> 旧バイナリ・対一覧・旧結果の保存は継続する。実装レビューで起動前照合・試験判定に
> 未充足条件を確認したため、修正完了とは扱わない（同 §5.10.14.74 の実装レビュー）。
> レビュー時点では計算資源は所有者判断待ちだったが、同日その後、所有者が
> ローカル検証・CI 検証を許可した。CI 待ちで次の作業を止めないことが条件。
> 設計への承認条件は維持し、CP2/CP3 の実データ投入とバイナリ切り替えは別途判断待ち。
> 全文引継ぎは未完了。

> **同日追記（ee0f6c1 レビュー）:** ca56b59 の負の対照は、保存されたCI診断全文で
> 成立を確認した。最新版CI全体の成功とは区別する。コード修正の承認は維持するが、
> 投入準備の設計 §14.2〜14.3 にコマンド上の欠陥があり、そのまま実行しない。
> 具体的な訂正条件・資産の照合値は `SPEC-phase5.md` §5.10.14.74 の末尾に記録した。

> **同日・所有者決定:** トレーラ方針は確定し、`CLAUDE.md` に共通規約として反映した。
> CP2/CP3 の実行自体も許可されたが、見積もりを所有者が確認した後、別途投入合図まで
> 待機する。合図後にビルド・CI待ちを残さないよう事前準備し、準備完了後は対象を固定して
> 投入用資源を別計算で占有しない。即時投入や、準備完了が承認されたわけではない。

---

> **同日・57201e4レビュー:** 準備完了は保留。manifest生成と起動前照合の上流コマンド失敗、
> 基準の事前固定、対象版CI結果の確認が残る。見積もりは保証幅ではなくシナリオとして扱う。
> CP2失敗時の次段停止は維持する。ghpr/ghbrはOPSEC §3.2の監査手順と対応する名前であり、
> 不要と即断せず保持を推奨する。詳細はSPEC §5.10.14.74の57201e4レビューを参照。

> **同日・f40b5d8レビュー:** 模擬自己検査31/31・13/13を仕様担当も確認したが、
> `dups_of` のコマンド置換内の異常終了を呼び出し元が空出力として受理する欠陥が残る。
> 基準候補の別名作成・提示は事前準備として進めてよい。投入・準備完了承認とは別。
> 層1は観測のみという訂正を受理し、自動停止の追加は今回の必須条件にしない。
> SPEC §5.10.14.74のf40b5d8レビューを参照。

> **同日・835dbb8レビュー:** 基準候補の内容と同一内容での固定を承認した（投入許可ではない）。
> 模擬自己検査36/36・19/19を確認したが、manifest解析のawk異常終了が未伝播で、
> 最初のawkだけexit 7にした模擬連鎖はCP2・CP3を起動してexit 0となった。
> 試験33もキー抽出前の行検査で止まり、目的の分岐を未検証。残件と基準ファイルのSHAは
> SPEC §5.10.14.74の835dbb8レビューを参照。対象CI確認・見積もり確認・投入合図は残る。

> **同日・e6a61aaレビュー:** 基準固定と前回の解析/注入試験修正を承認。模擬41/41・19/19。
> 実装担当が合図前に実物を20秒起動したと報告（到達点の原ログは削除済み）。
> 事故記録・残存証拠の保存・対象環境の停止確認が必要。新設check-onlyは誤記や後置指定を
> 無視して本計算へ進むため、模擬再現に基づき準備完了を保留する。SPEC §5.10.14.74参照。

> **同日・b82235aレビュー:** 引数処理の修正を承認、模擬51/51を確認。仕様担当の追加模擬確認で
> 既存meta・結果・ログと起動記録の保存も確認したが、回帰試験への取り込みは残る。
> 事故記録は、初回停止確認の判定不能と取り直し時点の不在を区別する等の補足が必要。
> 基準固定の承認を維持。対象CI確認・見積もり確認・投入合図は残る（SPEC §5.10.14.74）。

> **同日・所有者決定（監査参照）:** ghpr/*・ghbr/*は監査・復旧用として保持し、通常の
> 不要ブランチ削除の対象から外す。この項目の所有者確認待ちは解消した。将来削除が必要な
> 場合は保存先と監査用途を確認して改めて所有者判断を求める。CP2/CP3の投入合図ではない。

> **同日・2156c37レビュー:** 既存ファイル保存の回帰試験を承認、模擬63/63を確認した。
> 事故記録の訂正を受理するが、保存された0件表示にはps等の失敗と一致なしを区別できない
> 採取上の限界があるため注記する。以後は生出力と終了値を保存する。ghpr/ghbrの保持は
> 所有者決定済みで確認待ちから外す。最終SHAのCI確認と見積もり確認・投入合図は残る。

> **同日・325b62f CI確認完了:** 保存JSONと監視ログを仕様担当が照合し、run 34826697695の
> completed/success（27success・条件付きベンチ1skipped）を確認した。技術面の投入準備を
> 承認し、次は所有者の見積もり確認と別途投入合図を待つ。CP2/CP3各295対。CI成功を
> 投入合図やPhase 5完了と読み替えない。本追記は未コミットで、記録のみのpushは不要。

> **同日・所有者の投入指示:** 所有者が実装担当へCP2/CP3各295対の投入と定期監視を
> 指示したと通知した。合図待ちは終了。実行・監視は実装担当が担い、仕様担当は重複起動
> しない。実際の開始・進捗は未確認。最終照合・異常時の次段停止・ログ保存は維持する。

> **CP2/CP3完走報告のレビュー:** 保存結果を再集計し、各295行ok・FAIL0、修復20対132辺、
> 修復後unresolved0、旧CP2のFAIL8件→okを確認した。予定標本の完走は承認する。
> ただし「GMP257対実施」は駆動の処理と不一致。159+98対はGMPへ回す篩の印であり、
> 別途厳密検査の証拠は未確認。正しさ全体・Phase 5の完了承認は保留する。
> 列定義・時間の母数・入力再現根拠も補足する。SPEC §5.10.14.74の完走結果レビュー参照。

> **所有者決定（完走報告後）:** 結果報告のpush・PR作成は許可済み。報告訂正と仕様担当
> レビュー後に進め、共有文書の取り込みは担当間で確認する。追加GMP厳密検査の資源投入は、
> 見積もりを所有者が確認した後の判断待ち。公開許可を計算許可と読み替えない。

> **3e482c9レビュー:** GMP257対は未実施と実装担当が確定し、訂正を承認した。GMP設計は
> 差し戻し。案Dでも過去の出力を再生成する必要があり、5.13h（索引比較等を含む対時間）を
> そのまま節約できない。対象は§3.0の小標本と篩対象で、対象選択と実装方式を分けて設計する。
> 費用・メモリ測定のための追加計算も所有者判断。公開許可は既にあり、残るのは訂正レビュー。

> **b81263bレビュー:** 案D・対象選択・CMake・列定義の訂正は承認。PR #75の公開報告を受領。
> 時間集計は差し戻し。CPを区別しない後勝ち集計と1件除外で582件・報告値を再現したが、
> CPと対キーを組にすれば590実行すべての2巡を解析できる。対象選択でもCPを落とさない。
> 5軸は項目のみで設計未完。5対案は具体対象・厳密性・停止/メモリ制御を補足してから
> 所有者へ資源許可を求める。追加計算・ビルドは未実施。SPEC §5.10.14.74参照。

> **a877056レビュー:** 集計訂正を承認、駆動内方式は条件付き承認を継続。投入設計は未承認。
> NSIだけでは符号付き体積とw>0領域の体積の一致を保証しない。2対は診断優先候補であり、
> 欠陥確定ではない。負例の期待差・GMP専用再開・全体期限とメモリ上限の意味を補足する。
> 現行再開は旧結果のキーだけで飛ばすため流用不可。diff_errだけ閾値超過の4実行も確認した。
> 所有者への追加計算判断依頼は補足後。ビルド・実データ計算は未実施。SPEC §5.10.14.74参照。

> **52af971レビュー:** 5対の4出力厳密体積整合性診断を条件付き承認。式1は全出力空でも
> 通る必要条件で、独立正解器やPhase 5完了の代替ではない。箱の個別体積とN1/N2を合成検定の
> 最小構成とし、N3/N4は独立期待差を確定してから。全体20分の前に終了猶予10秒を確保する。
> 条件を設計へ反映後、所有者へ実装/ビルド/検定と5対診断の資源判断を分けて提示してよい。
> 261は篩側の候補で小標本10〜20を置き換えない。追加計算・ビルド未実施。

> **5dee73bレビュー:** 4点の設計反映を承認。所有者へ実装/ビルド/合成検定と5対診断の
> 2段階の判断を求める。資源は未許可。診断で索引OFF巡を省く案は通常の§6.3解除ではない。
> 文書の旧引数列はverify_index=1のままなので、診断用実効設定を投入前に固定/照合する。
> 4演算化後も旧3演算の互換ハッシュ等の照合手段を残す。全面再設計は不要。追加計算未実施。

> **adf3b8eレビュー:** 診断引数列のverify_index=0と旧3演算の互換ハッシュ保持の設計を承認。
> 前回の設計指摘2点は解消。所有者の判断1（実装/ビルド/合成検定）と判断2（条件成立後の
> 5対診断）の許可待ち。設計承認と実装検証・資源許可は別。新たな設計差し戻しなし。

> **プロセス誤計数の記録レビュー:** 保存3記録を照合。claude-krisite-v2の
> 2026-09-15T08:38:28+09:00のps可視範囲で駆動0件を確認した。末尾の6件は注釈等を含む
> 自己参照集計でプロセス数ではない。実験BのPIDは報告と異なりargsも途中で切れている。
> A/Bにps終了値はなく、初回3件の原因を実測で確定したとの説明は保留。追加実験は不要。
> 設計承認・所有者の判断1/2待ちは維持する。SPEC §5.10.14.74の保存記録レビュー参照。

> **所有者決定・判断1/2とも許可:** 実装/ビルド/合成検定と、受入条件成立後の指定5対診断を
> 許可済み。所有者判断待ちは終了。実装レビュー・GMPなし構成確認・合成検定・最終照合は維持する。
> 5対診断は全体20分（準備/検定/終了猶予込み）、ワーカー仮想アドレス空間8GiB。
> 条件成立後に同じ範囲の再許可・別途投入合図は不要。全体候補への拡大投入は未許可。
> 実装担当が作業し仕様担当がレビューする。許可は実施済みを意味しない。SPEC §5.10.14.74参照。

> **331d034レビュー:** 保存CTestログで合成4件の通過を確認し、残差検査器とCMake分離は受理。
> 判断1の完了は差し戻し。式1/4演算目の失敗が全体判定へ届かず、再開は先頭4項目のみで
> meta未照合。専用対象/停止制御と4演算の記録も未完成。駆動を通す合成/模擬回帰が必要。
> 5対の投入条件は未成立。両判断の所有者許可は維持し、修正後の再レビューを行う。
> 量子化200/2668の自己申告は実データ前処理の実施であり、「実データ計算なし」を訂正する。

> **1041637レビュー:** 判定接続・4演算記録・対象模型だけの量子化等の修正を確認したが、
> 1対ずつ変わるonly_fpと共通metaが衝突し2対目で停止する。入力/版の指紋と行検査も不足。
> unresolvedを成功判定へ接続する必要がある。CP2/CP3を通した全体20分と、対別RSSも未受入。
> 投入スクリプトと実駆動を組み合わせた複数対の合成回帰が必要。所有者許可は有効だが5対は起動しない。
> 量子化の到達点は400/2668へ自己申告が訂正された。原ログなしの限定を維持する。

> **80e1818レビュー:** 計画全模型の指紋分離・unresolved判定・wait4の修正を受理。
> ただし投入側row_completeが駆動のmeta検証を迂回する。子起動を禁止した模擬確認で、metaなし/
> 不一致/式1残差非零でも再利用成功を再現した。C++の末尾列/有理数等の検査と、準備を含む
> 全体期限・停止時の終了値にも残件。所有者許可は有効だが5対投入は未受入。SPEC §5.10.14.74参照。

> **a082c0b再レビュー:** row_complete削除と駆動への照合一本化を確認。Codex側の合成統合試験も
> 31/31通過し、旧meta迂回は解消した。ただしcheck_onlyのwait4が無期限待機であり、模擬で
> 期限0.3秒に対して0.746秒・終了値0・済み1を再現した。事前/事後照合にも絶対期限の監督が必要。
> 判断1全体の受入はこのP1修正まで差し戻す。所有者許可は維持し、5対はまだ起動しない。
> 実データ未実行。対象版・再現条件・検証範囲はSPEC §5.10.14.74のa082c0bレビュー参照。

> **e90f0f2再レビュー:** 共通監督と照合打ち切りは受理、Codex側模擬50/50通過。
> ただし起動前0.4秒の遅延注入で、全体期限0.3秒なのに0.430秒・終了値0・済み1を再現。
> 絶対期限を共有せずspawn後に相対予算を数え直すP1が残る。「KILLまで」の試験もTERMで
> 終わり、KILL発火を証明していない。判断1全体は差し戻し、所有者許可は維持、5対は未起動。
> 具体的な修正と検定はSPEC §5.10.14.74のe90f0f2再レビュー参照。

> **fa5a799再レビュー:** 絶対期限への接続とKILL検定を受理。Codex側模擬60/60通過。
> 群作成前にTERM/KILLが届かず自然終了まで待つ競合を遅延注入で確認。終了回収時も期限を
> 見ず成功に渡り得る。事後照合(b)の試験は実際には事前照合で止まり未被覆。
> 判断1全体は差し戻し。所有者許可は有効、レビューと最終照合まで5対は起動しない。
> 詳細はSPEC §5.10.14.74のfa5a799再レビュー。今回CI/固定資産の指紋は再確認していない。

> **75304e0再レビュー:** 前回3件の修正は承認、Codex側模擬72/72・統合31/31通過。
> 新設のhard_capで未回収を返した後、mainが事後照合を起動するP1が残る。
> 回収不能を注入した子なしの模擬でcheck→compute→checkを確認。未回収なら新しい子を
> 起動しない形と回帰が必要。判断1全体は差し戻し、所有者許可は維持、5対は起動しない。
> 再現条件と未検証範囲はSPEC §5.10.14.74の75304e0再レビュー参照。

> **bf3e900再レビュー・実装承認:** DONE/CUT/UNREAPEDの分離と呼び出し側の停止を受理。
> 親側で起動要求を同期記録する模擬4ケースで、未回収後の起動0件・非零終了を確認。
> Codex側の合成統合も31/31通過。最終照合へ進める。条件成立後の再許可は不要。
> 子側履歴のn件以下では起動数は確定しないため回帰改善は必要だが、今回は投入阻害としない。
> 試験用環境変数は遅延3種とUNREAPED_ATの全4種を未設定にする。5対実施/Phase 5完了/
> PRマージの承認とは別。詳細と検証範囲はSPEC §5.10.14.74のbf3e900再レビュー参照。

> **d9fc733結果レビュー:** 指定5対の成功/式1一致/式2不一致と合成検定通過を保存ログで確認。
> 実施報告は条件付き承認。Codex側で生結果/metaが見えず、厳密値・列・互換ハッシュは補完待ち。
> 篩の分母はmax(abs(Va),abs(Vb))で、報告の和による正規化とは別。式1一致で出力欠陥を除外せず、
> RSSとRLIMIT_ASを混同しない。DESIGN §31の評価は訂正が必要。次は記録補完であり再実行不要。
> 詳細はSPEC §5.10.14.74。Phase 5完了/候補全体投入/PRマージの承認ではない。

> **d3e46f0訂正レビュー:** 解釈の訂正を承認。抜粋の互換ハッシュと旧CP行の5/5一致を照合。
> 完全な有理数列はなお省略されており、証拠補完は条件付き承認を継続。ハッシュ一致を
> 元出力の全バイト直接比較と同一視しない。終了要約も現状はmain末尾到達時のみ。
> 所有者依頼で日英READMEの古い進捗を更新した。Phase 5継続/追加投入未許可の限定を維持。
> 再実行は不要。既存証拠の完全な保存と記述の補完が次の作業。SPEC §5.10.14.74参照。

> **c3c0396証拠レビュー完了:** SHA256 4/4、保存数値の厳密残差5/5、旧互換ハッシュ5/5を照合。
> 有理数の欠落による条件付き承認を解消。5対診断と保存数値の照合は完了、式2原因は未確定。
> SPEC/HANDOVER-spec/ROADMAPの既存追記の取り込みは事後同意し、取消コミットは不要。
> 手順違反は残し、次回は個別指定・ステージ差分確認・担当の事前合意を行う。
> Phase 5完了/拡大投入/マージの承認ではない。詳細はSPEC §5.10.14.74のc3c0396レビュー。

## 0. 読む順序

```
1. AGENTS.md              あなたの役割と作業方針（10 分）
2. .claude/rules/         思考の手続き。2 文書で 15 分
3. この文書               現在地と、前任の誤りの一覧
4. docs/ROADMAP.md        現在地の唯一の情報源
5. docs/CONTRACTS.md      Phase 0〜4 の確定契約
6. docs/SPEC-phase5.md    現在のフェーズの仕様
```

**`docs/` には 34 文書、約 56,000 行あります。全部読む必要はありません。**
**§3 の地図で、必要なときに引いてください。**

---

## 1. ★★★ いますぐ知るべきこと — 実装担当は【待機中】です

**所有者の合図があれば直ちに投入できる状態を保っています。**

| 項目 | 実体 |
|---|---|
| 回すもの | **CP2 本体 295 対 + CP3 層化 295 対。合計 9.7 時間** |
| バイナリ | `build/thingi_cp1_o3`。**sha256 = 9bf6652e…、HEAD = 6fbe9de** |
| 対の一覧 | `cp2b_only.txt` / `cp3_only.txt`（各 295 対。層 1→5 の順） |
| 実行の連鎖 | `tools/run_cp23.sh`（1 コマンドで CP2 → CP3） |
| 予測 | `docs/BENCH.md` に記録済み |

**待機中に触ってはいけないもの**: 対の一覧、バイナリ、`tools/run_cp23.sh`。
**触ると「直ちに実行できる状態」が崩れます。**

**投入時は、設定表がログの先頭に出た時点でもう一度確かめてから先へ進めます。**

**層 4 が自然な切れ目です**（CP2 で 1.4 時間、CP3 で 1.3 時間。層 5 だけで CP2 287 分）。

> **★ これが Krisite 単独で進められる最後の作業です。**
> **これが終われば、Phase 5 の正しさの完了条件が全部埋まります。**

---

## 2. 現在地

### 2.1 フェーズ

| Phase | 状態 |
|---|---|
| 0〜4 | 完了（〜2026-08-29） |
| **5** | **進行中**。正しさは機構として閉じた。性能は停止中 |
| 6+ | 未着手 |

### 2.2 正しさ — 機構としては閉じました

| CP | 状態 |
|---|---|
| CP1（500 対） | 成功 500 / 失敗 0 |
| CP2 | 層化 295 対で失敗 8 対 → **すべて解決済み。回し直しが待機中** |
| CP3 | **入口の検査は完了**（665 件を弾く）。**対の実行が待機中** |

**辺の水準は radial sort で、頂点の水準は【辺の細分】で解けました。**

**そして頂点の水準は「欠陥ではなく表現の問題」でした** —
扇を 1 個にしているのが正しく、
**「同じ 2 頂点を結ぶ 2 本の辺を索引付き三角形メッシュで区別できない」**だけ。

### 2.3 性能 — 累積 4.21 倍で停止中

| 40 対 | 旧 → 新 |
|---|---|
| 中核 1 演算の幾何平均 | 913.5 → **296.1 ms（3.09 倍）** |
| 実務の分母の合計 | 615 → **146 s（4.21 倍）** |
| p95 | 44.02 → **7.99 s（5.51 倍）** |

**目標は「20,000 面の対の p95 が 1 秒以内」。**
**中央値 1.13〜1.22 s は届いていますが、p95 は 3〜5 倍残ります。**

**切り口を 3 つ閉じました。**

| 切り口 | 閉じた理由 |
|---|---|
| 述語の単価 | 述語は中核 CPU の 14%。Krisite が文献の実装より 4.3 倍速い |
| 確保を減らす | 確保は CPU の 0.7%。効くのは周辺（複製、間接参照） |
| 段を刻む | 実務の分母で単独 10% を超える項が無い |

**残る候補はすべて上限 1.2 倍以下。EMBER 再現実装の p95 待ちです。**

### 2.4 EMBER 再現実装（別プロジェクト）

**EMBER の本家は非公開、Blender の実装は GPL の再現実装。**
**MIT で再現実装し、別リポジトリで公開します。**

**仕様検討は Codex、実装は別の Claude Code。**

> **★ あちらは Krisite を念頭に置きません**（贔屓を防ぐため）。
> **Krisite の情報をあちらに持ち込まないでください。**

---

## 3. 文書の地図

### 3.1 仕様（これが正）

| 文書 | 内容 |
|---|---|
| `SPEC-phase0.md` | 算術基盤、述語、ビット幅の導出 |
| `SPEC-phase1.md` | 出力抽出。**退化ケースの構成の規律** |
| `SPEC-phase2.md` | 適応分割、出力の意味論、T 頂点、接触の分裂、三角形化 |
| `SPEC-phase3.md` | **中核の再設計**（$n$ 項、WNV、局所 BSP、中核と後処理の分離） |
| `SPEC-phase4.md` | 並列化。**決定性は要求であって副産物ではない** |
| **`SPEC-phase5.md`** | **現在のフェーズ。6,429 行。§5.10.14 が性能と正しさの検討の本体** |

### 3.2 索引と現在地

| 文書 | 内容 |
|---|---|
| **`ROADMAP.md`** | **現在地の唯一の情報源** |
| **`CONTRACTS.md`** | **Phase 0〜4 の確定契約を 1 枚に。ここは変えない** |
| `HANDOVER.md` | 実装担当への引き継ぎ。**取り下げ一覧と、誤りの型 6 つ** |

### 3.3 検討ログ（数字を書き換えない）

| 文書 | 内容 |
|---|---|
| `DESIGN-phase5-hotspots.md` | 性能の検討。5,634 行 |
| `DESIGN-phase5-vertex-level.md` | 頂点の水準の検討。**§3.4 に正しい機構** |
| `DECISION-core-contract.md` | 中核の契約をどう決めたか。**5 軸の比較の手本** |
| `LOG-phase3-design.md` | Phase 3 の設計ログ。**§4.1 に出力契約の決着** |
| `LOG-phase5-checkpoints.md` | CP の記録 |

### 3.4 実装記録（書き換えない）

| 文書 | 内容 |
|---|---|
| `IMPL-phase0〜5.md` | 各フェーズの担当の日記 |
| **`IMPL-v2.md`** | **2 人目の実装担当（2026-09-07〜）の記録** |
| `IMPL-phase5_1.md` / `_b4403b.md` | **古い版の残骸**。読まなくてよい |

### 3.5 測定と調査

| 文書 | 内容 |
|---|---|
| **`BENCH.md`** | **数値はここが正。3,283 行** |
| `PERF.md` | 費用モデル（**ローカル保管**） |
| `perf-notes.md` | 実測値の溜め場。仕様ではない |
| `RESEARCH-perf.md` | **文献調査**。EMBER / Cherchi / Nehring-Wirxel の精読 |

### 3.6 その他

| 文書 | 内容 |
|---|---|
| `OPSEC.md` | 公開時の情報露出。**実際の事故の記録** |
| `STYLE.md` | コーディング規約 |
| `README.md` / `README_en.md` | 公開文書 |

---

## 4. ★★★ 私が犯した誤りの一覧

**これが引き継ぎの中心です。**

**同じ型を繰り返さないために、型ごとに分けます。**
**個別の実例は `CLAUDE.md` の該当項目に、実例つきで書いてあります。**

### 4.1 推測を、いつの間にか事実として扱う

**最も多く、最も気づきにくい型です。**

| 実例 | |
|---|---|
| **「EMBER の 300 倍」** | 4 つの倍率を掛けた**推測**を、**2 手後に「現在の推定は 300 倍前後」と事実として扱い**、そのうえで結論を出した |
| 「中核に `cmp_h` が残っている」 | `IMPL-phase4.md` の「`lex_less`」を「`cmp_h` 相当」と読み替えた |

> **★ 他人の主張は疑いますが、自分が 2 手前に書いた推測は疑いません。**
> **`.claude/rules/provenance.md` はこの型のために作りました。**

### 4.2 1 つの標本、1 つの条件から一般則を書く

| 実例 | |
|---|---|
| **§98 の一般則** | 1 本の辺から「複数の辺の組が推移的に繋ぐ」と書かれた主張を、**確かめずに前提にした**（実際は 118 端点中 104 が別の形） |
| 「約 2,200 対」 | **帯なしの数**を、帯ありの文脈で使った |
| CP2 の「4 時間」 | **実測（6.15 時間）が既にあるのに、使わずに書いた** |

### 4.3 文献の主張を、こちらの前提で成り立つか確かめない

| 実例 | |
|---|---|
| **「分類のレイ数が最上位」** | EMBER の「レイキャストは大域の問題」は**大域の加速構造を持たない設計**での話。**Krisite は空間索引を持っている** |
| 「述語の単価が主因」 | 同じ設計の論文の Table 1 で、**Krisite のほうが 4.3 倍速い**と判明 |
| 「中点の幅は 10 リム」 | **既に取り下げられた操作**（点どうしの中点）を前提にしていた |

> **★ そして逆もあります。** early-out は「前提が違うが【満たされている】」形でした。
> **「前提が違う」で止めず、手段と要求を分けてください。**

### 4.4 改善の見積もりで、分母やアムダール則を忘れる

| 実例 | |
|---|---|
| **「桁を変えれば届く」** | 候補を 7 分の 1 にしても、その段が 24.5% なら**全体は 1.27 倍** |
| 「$n$ 倍だけなので足りない」 | **$n$ 倍でもなかった**。走査の回数と述語の評価回数を混同 |
| 「CP3 は CP2 より軽いはず」 | 弾かれるのは 9.9% だけで、**対の数は 2.3 倍** |

### 4.5 母集団を取り違える

| 実例 | |
|---|---|
| **「頂点の水準は契約の範囲外」** | 根拠の 0.34% は**自己交差なしを含む母集団**の数。本来の対象では 2.7% |

### 4.6 目標や条件を、達成できる形に付け替えようとする

| 実例 | |
|---|---|
| **「飽和で区切れば性能改善なしでも達成できる」** | **所有者に指摘された。** 除外した基準を復活させ、目標を付け替えようとしていた |
| 「分布を変える」 | **母集団は EMBER と揃えるために選んだもの。** 変えれば比較が成立しない |

> **★ この型が最も危険です。** 技術的には誤りに見えず、しかも「進んだ」気になります。

### 4.7 共有された文書を全部読まない

**報告の本文だけを読んで、添付を開かずに答えたことが複数回あります。**
**そして 1 度は、本文すら読まずに答えました。**

> **★ 報告の本文に要約しかない細部が、添付にあります。**
> **こちらが求めた作業の結果が、既に添付に入っていることもあります。**

---

## 5. 効いた判断

**誤りばかり書きましたが、効いたものもあります。同じ形を続けてください。**

### 5.1 番人を要求する

> **「機構が発火した回数を数え、0 なら検査を落とす」**

**3 度、誤りを止めました。**

- early-out の比較を `early_out = false` の構成で回していた（バイト一致は通っていた）
- `PointCache` の A/B を `cache_points = false` で回していた（同上）
- O3 の突く構成が、機構を発火させていなかった（出力は一致していた）

**どれも「比較が通った」ことが何も言っていませんでした。**

### 5.2 同値な置き換えを要求する

**「検査を安くするなら近似ではなく同値に。そして従来の経路を残して一致を検査する」**

**これが検証の増分計算（出口 1.8〜2.4 倍）になりました。**

### 5.3 対照を要求する

- 別の日と比べる前に、**旧コードを今日の機械で回し直す**（2 倍近い誤報を防いだ）
- **冪等な連鎖**で「幾何が同じなのに表現が膨らむ」ことを分離した

### 5.4 層化を要求する

**「昇順に流して飽和で区切る」は誤りだと分かり、層化に変えました。**

**そして CP2 本体で、失敗が層 1〜3 に 5 件出ました。**
**昇順なら小さい対で失敗 0 と誤認していた可能性があります。**

### 5.5 実装の前に設計を要求する

**「5 軸で並べる」（費用・厳密性・並列の依存構造・失敗の形・検証の設計）。**

**中核の縫合の置き換えは、実装の【前】に「連鎖で成立しない」と分かって止まりました。**
**7.3 時間の計算を 1 回も回していません。**

---

## 6. 未解決のもの

| # | 内容 | 状態 |
|---|---|---|
| 1 | **CP2 + CP3 の実行** | **所有者の合図待ち。9.7 時間** |
| 2 | **性能目標（CP4）** | **EMBER 再現実装の p95 待ち** |
| 3 | $b$ の既定を 21 から 26 に変えるか | Phase 6 の着手前に決める（`CONTRACTS.md` §1） |
| 4 | 中核と出口の融合（構成点 6.5% + 併合 7.7%） | **設計の変更。記録に留めている** |
| 5 | 出口の逐次の段の並列化 | 上限 1.2 倍程度 |

### 6.1 ★ 2 が片付いたときに議論すること

**EMBER 側の p95 が出たら、目標の妥当性を判断します。**

| EMBER 再現実装の p95 | 意味 |
|---|---|
| Krisite と同じく裾が重い | **「p95 が 1 秒」は EMBER にも厳しい目標** |
| Krisite より軽い | **Krisite の裾に固有の原因がある** |

> **★ そのときも、分布は変えないでください**（§4.6）。

---

## 7. 最初にやること

```
1. AGENTS.md と .claude/rules/ を読む（25 分）
2. この文書と ROADMAP.md を読む
3. CONTRACTS.md を読む（Phase 0〜4 の確定事項）
4. SPEC-phase5.md の §5.10.14 を読む（性能と正しさの検討の本体）
5. 実装担当に「引き継ぎました」と伝え、待機中の状態を確認する
```

**5 が重要です。** **バイナリの sha256（`9bf6652e…`）と対の一覧が変わっていないことを、
あなた自身で確かめてください。**

**そして所有者の合図が来たら、投入の判断はあなたが出します。**

---

## 8. 最後に

**この 2 週間で、私の判断は 19 回覆りました。**
**そして覆ったもののうち、実装に到達する前に止まったものが大半です。**

**それは実装担当が、毎回「予測を先に書き、外れ方の行き先を決め、番人を置いた」からです。**

> **仕様検討担当の仕事は、正しい判断を出すことではありません。**
> **誤った判断が実装に到達する前に、それが分かる形にすることです。**

**そのために、次を続けてください。**

- **実装の前に設計を要求する**（5 軸）
- **番人を要求する**（発火が 0 なら落とす）
- **対照を要求する**（何と比べているかを明示させる）
- **由来を書かせる**（実測 / 導出 / 推測 / 仮説 / 憶測）
- **自分の誤りを記録する**（取り下げは消さない）

**そして自分の推測を、次の判断の前提にしないでください。**
