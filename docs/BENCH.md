# Krisite — ベンチマーク結果

SPEC-phase0.md §9「`side()` のスループットをベンチで計測し、数値を `docs/BENCH.md` に記録」。

> **これは基準線であって目標値ではありません。** Phase 0 では性能目標を設定しません
> （SPEC §9）。SIMD 化・浮動小数点フィルタは、この数値を見てから設計判断します（SPEC §10）。

## 測定環境

| 項目 | 値 |
|---|---|
| 日付 | 2026-08-26（SPEC 2026-08 改訂に追従後） |
| CPU | AMD Ryzen 7 9800X3D (8C/16T) |
| OS | Ubuntu 26.04 (Docker, Linux 6.12.67) |
| コンパイラ | clang 21.1.0（`zig c++` 経由） |
| ビルド | `Release` (`-O3 -DNDEBUG`), `KRISITE_CHECKED_ARITH=OFF` |
| `KRISITE_COORD_BITS` | 21 |
| 測定方法 | 各項目 3 回計測して最小値。入力は L1 に収まる 2048 要素のプールを巡回 |
| ばらつき | **同一条件でも実行ごとに ±15% 程度動く。**1 割前後の差に意味を持たせないこと |

> **注意**: これは開発コンテナの数値です。CI ランナー（GitHub Actions）の数値は大きく
> 揺れるため合否判定には使いません（`.github/workflows/ci.yml` の `bench` ジョブ）。

再現手順:

```bash
cmake -B build-rel -G Ninja -DCMAKE_BUILD_TYPE=Release \
  -DKRISITE_CHECKED_ARITH=OFF -DKRISITE_BUILD_BENCH=ON
cmake --build build-rel
./build-rel/bench/pred_bench
./build-rel/bench/arith_bench
```

## 述語（`bench/pred_bench.cpp`）

`side` は 209 ビット / 4 リム、`cmp_h` は 300 ビット / 5 リムで評価（SPEC §3.3）。

| 演算 | ns/op | Mops/s |
|---|---:|---:|
| **`side(plane, HPoint)`** ★最重要 | **7.80** | **128.3** |
| `side(plane, IPoint)` | 2.35 | 426.0 |
| `orient3d(IPoint × 4)` | 12.87 | 77.7 |
| `orient2d(IPoint × 3, Z)` | 2.08 | 481.6 |
| `cmp_h(HPoint × 2, X)` | 11.25 | 88.9 |
| `lex_less(HPoint × 2)` | 11.42 | 87.6 |
| `plane_from_triangle` | 3.38 | 295.7 |
| `intersect3(Plane × 3)` | 261.76 | 3.8 |

## 固定幅整数（`bench/arith_bench.cpp`）

| 演算 | 1 リム (64bit) | 2 リム (128bit) | 4 リム (256bit) |
|---|---:|---:|---:|
| `add` | 0.23 ns | 0.38 ns | 0.70 ns |
| `mul` | 0.56 ns | 1.90 ns | 11.63 ns |
| `cmp` | 0.43 ns | 0.41 ns | 0.41 ns |
| `sign` | 0.41 ns | 0.40 ns | 0.36 ns |

`cmp` と `sign` がリム数にほぼ依存しないのは、符号ビットと最上位リムで決着する経路が
効いているため（SPEC §5.3）。`mul` はリム数の 2 乗で伸びており、筆算の計算量と一致します。

## 読み取れること

**`side()` は 7.8 ns（128 Mops/s）。** 浮動小数点フィルタを入れるかどうかの判断材料として、
これが基準線になります。EMBER が「フィルタ不要」と主張する根拠と整合する水準です。

**`cmp_h` は `side` より重い（11.3 ns）。** 被符号値が 300 ビット（5 リム）と Phase 0 で
最大幅であること（SPEC §3.3）が素直に出ています。

なお、リム単位の積が生む 7 リムを §3 の上界である 5 リムへ詰める `resize` に、
**実行時コストはありません**（`resize` の有無だけを変えた A/B で 9.66 対 9.67 ns）。
Release では下位リムのコピーに縮み、符号を取るだけの後段では幅の差が出ません。

**`intersect3()` が 262 ns で突出して重い。** これは実装の選択によるもので、
`intersect3` は 3x3 行列式を 4 回計算しますが、列ごとに幅が違う（法線 1 リム、
オフセット 2 リム）ため、**正しさを優先して全列を 2 リムにそろえてから** 7 リム幅で
計算しています（`geom/plane.hpp`）。列ごとの幅を保った混合幅の行列式にすれば
数倍は縮むはずですが、Phase 0 では手を入れません（SPEC §0「性能より正しさを優先」）。

なお EMBER の想定では構成点は一度作って何度も `side` にかけるので、
`intersect3` の重さが支配的になるとは限りません。**Phase 1 で実際の比率を測ってから**
最適化対象を決めてください。

## Phase 1 §4.3.3 / §5.4 の計数（CP3 到達時点）

**性能ではなく設計判断のための数値です**（SPEC-phase1 §13）。
`tests/csg/test_cp3.cpp` を実行すると同じ値が出ます。b = 21。

断片数は正準化後（重複除去後）。最大枚数・値併合・広がりは深度 0〜3 の最大値です。

| # | 構成 | 断片数（深度 0/1/2/3） | 最大枚数 | うち mesh | 値併合 | 広がり |
|---|---|---|---:|---:|---:|---:|
| 1 | 一般位置 | 48 / 108 / 192 / 366 | 3 | 3 | 0 | 1 |
| 2 | 面が完全共平面 | 40 / 40 / 56 / 224 | 3 | 3 | 0 | 1 |
| 3 | 辺が一致（共線） | 34 / 34 / 66 / 264 | 3 | 3 | 0 | 1 |
| 4 | 頂点が一致 | 26 / 26 / 38 / 152 | 3 | 3 | 0 | 1 |
| 5 | 面がセル境界と一致 | 48 / 48 / 48 / 192 | 3 | 3 | 0 | 1 |
| 6 | セル境界から 1 格子ずれ | 48 / 108 / 192 / 432 | 3 | 3 | 0 | 1 |
| **7** | **量子化した回転立方体** | 65 / 117 / 117 / 254 | **6** | **4** | **87** | 1 |
| 8 | 同一の立方体 | 12 / 48 / 48 / 192 | 3 | 3 | 0 | 1 |
| **9** | **四面体 2 個（一般位置）** | 44 / 79 / 137 / 279 | **6** | 3 | **18** | 1 |
| 10 | 一方が他方を完全に含む | 60 / 120 / 120 / 120 | 3 | 3 | 0 | 1 |
| 11a | 面接触のみ | 32 / 32 / 40 / 160 | 3 | 3 | 0 | 1 |
| 11b | 辺接触のみ | 12 / 12 / 34 / 136 | 3 | 3 | 0 | 1 |
| 12 | セル境界をまたぐ大立方体 | 48 / 108 / 246 / 366 | 3 | 3 | 0 | 1 |
| **2T** | **共平面接触を斜面で** | 19 / 57 / 73 / 163 | **6** | 3 | **83** | 1 |
| **4T** | **四面体 2 個が頂点を共有** | 20 / 20 / 62 / 182 | **9** | **6** | **18** | 1 |
| **4T′** | **頂点共有 + 体積も重なる** | 35 / 35 / 95 / 251 | **9** | **6** | **32** | 1 |
| **5T** | **セル角を斜面が通る** | 42 / 42 / 72 / 204 | **6** | **4** | **72** | 1 |
| D | 交わらない 2 立方体 | 32 / 60 / 96 / 308 | 3 | 3 | 0 | 1 |

**読み取れること。**

- **軸平行な 12 ケースは例外なく「3 枚・値併合 0」です。** 1 点を通れる平面は
  1 軸につき高々 1 枚で軸は 3 本だけ、かつセル面が立方体の面と一致しても同一の
  幾何平面なので `PlaneTable` が同じ ID に併合するためです（§9.1）。
  **ケース 4（頂点が一致）も 3 枚止まり**でした
- **斜面を持つ 6 ケースだけが 4 枚以上**（最大 9 枚）。§5.4 の「5 枚以上が出るか」の
  答えは **YES** です。四面体の頂点には面が 3 枚集まるので、2 個が頂点を共有すれば 6 枚
- **§5.3 の第2段は必須です。** 値併合が最大 87 件。**軸平行だけなら 0 件**なので、
  §10.5 の変異 1 は斜面コーパスでしか検出できません
- **併合は局所に閉じます。** 広がりは全ケース・全深度で **1 以下**、つまり 1 セルと
  その面・辺・頂点隣接に収まります。発生率が高くても大域整列は不要で、
  **Phase 3 は「セル並列 + 境界併合」で組めます**（§5.4）
- **深度を上げても断片が増えないケースがあります**（5, 8, 10, 2T など）。面がセル境界に
  乗ると、その境界平面は §4.3.1 の大域分割が既に使っているので新しい切断が入りません。
  §9.0 の番人はこのために「非減少 かつ 深度 0 → 3 で狭義増加」で判定します


### 述語のビット幅（CP2.5 で追加した 4 本）

代表点のフォールバックに使う述語です。**点を構成せず**、被符号値の線形性で評価します。
中点は頂点数 4 以上の断片、重心は三角形の断片に使います（`IMPL-phase1.md` §2.5.1、§2.5.2）。

| 述語 | 理論 (b=21) | 実測 | 理論リム | 実測リム |
|---|---:|---:|---:|---:|
| `side(plane, 中点)` $15b{+}33$ | 348 | 317 | 6 | 5 |
| `orient2d_h(中点)` $14b{+}30$ | 324 | 297 | 6 | 5 |
| `side(plane, 重心)` $21b{+}46$ | 487 | 443 | 8 | 7 |
| `orient2d_h(重心)` $20b{+}43$ | 463 | 423 | 8 | 7 |

上界超過はありません。実測リムが 1 つ小さいのは乱択が真の最大に届かないためで、
**リム数は減らしません**（SPEC-phase0 §3.4）。

### §12 `side` : `intersect3` の呼び出し比率 ★

全 18 ケース × 3 演算 × 深度 0〜3 の合計。

| 述語 | 呼び出し回数 |
|---|---:|
| `side` | 2,713,359 |
| `intersect3` | 2,156,253 |
| **比** | **1.26 : 1** |

**IMPL-phase0 §7 の前提「構成点は一度作って何度も `side` にかける」は成り立って
いません。** `split_fragment` が `side(q, fragment_vertex(...))` の形で構成点を
毎回作り直すため、`side` 1 回につき `intersect3` が 1 回必要になります。

上の実測時間（`side` 11.25 ns / `intersect3` 262 ns）で重み付けすると、
**述語の時間の 94.9% を `intersect3` が占めます**（推定。回数は厳密だが単価は
別条件のベンチマーク値で、実際の時間は測っていない）。

**Phase 2 では「構成点の保持」を `intersect3` の混合幅化より先に検討してください。**
単価を下げても、回数が `side` と同数のままでは支配的な構図が変わりません。
詳細は `IMPL-phase1.md` §5.6。

計測はまだ**時間**を測っていません（Phase 1 に性能要件は無いため）。

## Phase 2 §11 の計数（CP1 到達時点）

**性能ではなく設計判断のための数値です**（SPEC-phase2 §11）。完了条件ではありません。
`tests/csg/split_strategy` を実行すると同じ値が出ます。b = 21。

### 分割平面の絞り込み（§2.3）

全 18 ケース × 3 演算の合計。「総当たり」は絞り込み前の「セル × 分割平面」の数、
「使用」は `plane_crosses_box` が通した数です。

| 深度 | 使用 / 総当たり | 残存率 | 最大/セル |
|---|---|---:|---:|
| 0 | 540 / 540 | **100.0%** | 12 |
| 1 | 2,739 / 4,320 | 63.4% | 10 |
| 2 | 12,927 / 34,560 | 37.4% | 9 |
| 3 | 54,867 / 276,480 | **19.8%** | 7 |

**読み取れること。**

- **深度 3 で 8 割が落ちます。** セルが小さくなるほど、その閉包と交わらない平面が
  増えるためです。残存率は深度とともに単調に減少します
- **深度 0 では 1 枚も落ちません。** 入力座標は座標範囲内にあるので、そこから作った
  平面は座標範囲全体と必ず交わります。**これは番人です。**深度 0 で落ちたら判定が
  壊れています（`IMPL-phase2.md` §2.3）
- **断片数は 1 つも減っていません。** 絞り込みが落とすのは no-op な分割だけなので
  当然で、**§11 の「断片数」が動くのは CP2（適応分割）から**です。
  **CP1 の利得は断片数ではなく「切る回数」です**
- 軸平行なケース（1 / 6 / 8 / 10 / 12 / D）は深度 1 で 50.0% ちょうど。軸平行平面は
  自分に垂直な方向のセル列としか交わりません。斜面を持つケース（7 / 9 / 2T）は
  65〜81% と落ちにくく、**斜面ほど多くのセルを横切る**という素直な結果です

### 述語のビット幅（CP1 で追加した 1 本）

| 述語 | 理論 (b=21) | 実測 | 理論 (b=26) | 実測 | リム |
|---|---:|---:|---:|---:|---:|
| 平面とセルの閉領域の交差 $3b{+}7$ | 70 | 65 | 85 | 80 | 2 |

上界超過はありません。**リム数は減らしません**（SPEC-phase0 §3.4）。

### 適応分割（§3、CP2）

全 18 ケース × 3 演算 × 最大深度 3 の合計。**固定深度（Phase 1 の挙動）が正解器**です。

| 指標 | 固定深度 | 適応分割 |
|---|---:|---:|
| 葉 | 32,256 | **7,434** |
| 断片（正準化後） | 15,624 | **9,714** |
| 葉の深さの差 | 0 | **最大 2 段**（2 段以上が出たのは 7 ケース） |

**§13 の CP2 の判定「節約が実際に出ているか」は満たしています。**
葉が 77% 減り、断片が 38% 減りました。

### T 頂点の解決（§2.4.3、案 D）

| 指標 | 値 |
|---|---:|
| 挿入された T 頂点 | 336 |
| 1 本の辺に載った T 頂点の最大 | **3** |
| **残した**退化三角形（§2.4.4 (2)） | **20** |
| 固定深度での挿入（負の対照） | **0** |

**残した退化三角形は 20 枚です。** 扇の起点を「両隣の元の辺に T 頂点が無い元の角」に
取れば退化は出ませんが、どの角も条件を満たさない多角形が残ります。**捨てません** —
捨てると境界辺が消えて次数 1 の辺が出ます（§2.4.4 (2) の訂正）。
一般解（元の角だけで三角形化してから辺ごとに扇を張る）は、この枚数が実際に効いてから
判断します。

### early-out（§3.2 / §3.3）

適応分割の上で early-out の有無を比べたもの。**無効側が正解器**です。

| 指標 | 無効 | 有効 |
|---|---:|---:|
| 断片 | 9,714 | **8,217** |
| レイキャスト | 1,056 | **660** |
| 領域（相異なる符号ベクトル） | 1,056 | **660** |

| 発火 | 値 |
|---|---:|
| 葉 | 7,434 |
| うち arrangement を省いたセル | **2,787** |
| うち丸ごと飛ばしたセル（どちらも居ない） | **2,727** |
| **セル数ベースの発火率** | **74.2%** |
| 分類を省いた断片 | 3,261（**39.7%**） |
| セルの隅（**整数点**）で済ませた判定 | 2,787 |

**判定が整数座標だけで済むのが効きます**（§3.2）。`side(plane, IPoint)` は 2.35 ns、
`side(plane, HPoint)` は 7.80 ns。省いた 2,787 セルぶんは**構成点を一切作りません。**

**セルの `lo` 隅を使います。** `hi` 隅は $+2^{b-1}$ になり得て `IPoint` に入りませんが、
`lo` 隅は必ず範囲内です。**`lo` 隅が $\partial B$ の上に無いことは割り当てが保証します** —
$\partial B$ が隅を含むなら、その面の AABB は隅を含むので割り当てられ $n_B > 0$ になり、
early-out は発火しません。

### 重複断片が 0 になりました ★ 指標が壊れたわけではありません

**§3.4 でセル割り当てを半開区間 $[\mathrm{lo}, \mathrm{hi})$ に変えたためです。**

| ケース 5 の $\cup$ | 葉 | 断片 | 重複断片 | 生 |
|---|---:|---:|---:|---:|
| 固定深度（閉領域、Phase 1） | 512 | 192 | **144** | 336 |
| 固定深度（半開、現在） | 512 | 192 | **0** | 192 |
| 適応分割（閉領域） | 253 | **204** | 96 | 300 |
| 適応分割（半開、現在） | 106 | **84** | **0** | 84 |

閉領域では**セル境界平面に完全に乗る面が両側のセルに入り**、固定深度では両側の断片が
一致して `region_key` が併合していました。**適応分割では併合しません**（粗い側の 1 枚と
細かい側の 4 枚は「同一領域」ではない）。同じ面積が二重に出力され、$C$ が破裂します。

**固定深度の断片数（192）は変わっていません。** 重複が最初から生じなくなっただけで、
出力は同じです。`region_key` の併合経路も死んでいません — $A$ と $B$ の共平面重複
（`SPEC-phase1.md` §4.3.2）で引き続き使われます。消えたのは
**「同一 owner の、セル境界由来の重複」だけ**で、これは要件ではなく副産物でした。

### 構成点の保持（§4）★ Phase 1 の前提を作り直しました

**Phase 1 の実測が示した最優先項目です。**

| 指標（全 18 ケース × 3 演算 × 深度 0〜3） | 保持なし | **保持あり** |
|---|---:|---:|
| `side` の呼び出し | 4,630,307 | 4,630,307 |
| `intersect3` の呼び出し | 1,539,819 | **28,836（1.9%）** |
| **比** | **3.01 : 1** | **160.57 : 1** |
| Phase 0 の単価で重み付けした `intersect3` の時間占有 | 88.6% | **12.7%** |

| キャッシュ | 値 |
|---|---:|
| 参照 | 1,539,819 |
| ヒット | 1,510,983（**98.1%**） |
| 実体 | 28,836 個 |
| 占有メモリ（1 回の最大） | **49,032 B**（約 48 KB） |

**キャッシュの有無で出力は 1 ビットも変わりません。** 平面3つ組を昇順に並べてから
`intersect3` を呼ぶので、`tests/csg/test_cache.cpp` は**値の完全一致**を要求できます。

**第2段の値併合は 1,704 件で健在です**（§4.3 の分担）。4 平面以上が一点で交わる場合、
異なる3つ組が同じ値を持ちます。**これはキャッシュの誤りではありません。**
キャッシュは3つ組で引き、値の同一性は第2段が扱う、という分担が保たれています。

### 実時間 ★ Phase 1 で測っていないもの（§11）

全 18 ケース × 3 演算 × 深度 3。**合否には使いません。**

| 構成 | 時間 |
|---|---:|
| 固定深度（Phase 1 の挙動） | 220.9 ms |
| 固定深度 + 構成点の保持 | 121.0 ms |
| 適応分割 | 126.4 ms |
| 適応分割 + early-out | 89.6 ms |
| **適応分割 + early-out + 構成点の保持** | **47.6 ms** |

**Phase 1 の構成から 4.6 倍**。ただし **±15% のばらつきがあります**（同一条件でも実行ごとに
動きます）。**1 割前後の差に意味を持たせないこと。** これは基準線であって目標値ではなく、
Phase 4 で性能目標を立てるときに使います。

### 接触の分裂（§5、§5.7 の記録）

全 19 ケース × 3 演算 × 深度 0〜3 = 264 構成。

| 指標 | 値 |
|---|---:|
| 分裂した頂点 | 64 |
| 次数 3 以上の辺 | 36（端点 56） |
| 扇の個数 $k$ の最大 | **3** |
| 分裂前に非多様体だった構成 | 16 |
| 分裂前に $\chi$ が奇数だった構成 | 8 |
| **§9.3 の除外（分裂後）** | **0 件** |
| **§5.1.2.1 の停止に到達した配置** | **0**（radial sort は不要） |

**§5.5 の予測（$\Delta V$ / $\Delta E$ / $\Delta\chi$）は全構成で実測と一致します。**

| ケース | 演算 | $\chi$（前 → 後） | $C$（前 → 後） | $k$ |
|---|---|---|---|---|
| 4T | $\cup$ | 3 → **4** | 2 → 2 | 2 |
| 4T′ | $A \setminus B$ | 4 → **6** | 3 → 3 | **3** |
| 11b | $\cup$ | 3 → **4** | 2 → 2 | 2 |
| **16**（自己接触） | $A \setminus B$ | 4 → **8** | 4 → 4 | 2 |

ケース 16 の深度 0 は §8.2 の期待値に完全一致（次数 4 の辺 4 本、$\Delta E=4$、
$\Delta V=8$、$\Delta\chi=4$）。**深度 1 以降は接触辺がセル平面で分割されて
$\Delta E=8$、$\Delta V=12$ になりますが、$\Delta\chi=4$ は深度不変です。**

### まだ測っていないもの（CP2 以降）

early-out の発火率、構成点キャッシュのヒット率と占有量、`side` : `intersect3` の
新しい比、**そして時間**。時間は Phase 1 でも Phase 2 CP1 でも測っていません。

---

## Phase 3 §11 の計数と時間（CP5 到達時点）

**性能ではなく設計判断のための基準線です**（`SPEC-phase3.md` §11）。完了条件ではありません。
`bench/soup_bench` を実行すると同じ値が出ます（`-DKRISITE_BUILD_BENCH=ON`、Release、b = 21）。
**単発の差に原因を当てないこと。実行ごとに ±15% 程度動きます。**

### 入口・中核・出口の内訳 ★

全 22 ケース × 3 演算、適応分割（深度 3）+ 全機構有効。

| 区分 | 時間 (ms) | 割合 | 主な計数 |
|---|---:|---:|---|
| `from_mesh` | 0.8 | **0.7%** | 多角形 1,464、平面 1,503 |
| **`boolean`（中核）** | **75.9** | **64.6%** | 断片 5,205、分類キー 1,635 |
| `to_mesh` | 40.9 | **34.8%** | 三角形 9,180、値で併合 3,221、T 頂点 623 |
| 合計 | 117.6 | 100.0% | セル 8,004（うち early-out 2,112 = **26.4%**）|

**読み取れること。**

- **入口は無視できます**（0.7%）。凸分割を入れても入口の時間は問題になりません。
  効くとすれば**下流の断片数**を通じてです
- **出口が 3 分の 1 を占めます。** `SPEC-phase3.md` §11 の但し書き
  「EMBER の 1.6 ms はスープを出すまでの時間」がそのまま効いています。
  **中核だけを比べるなら 64.6% の側と比べること。** 全体で比べると
  **1.5 倍不利な比較**になります
- **early-out がセルの 26.4% で発火しています。** 適応分割との併用で、
  4 分の 1 のセルが分類を隅 1 点で済ませています

### 連鎖の段数に対する推移

$k$ 段目まで連鎖したときの値。1 段 = $A \cup B$、2 段 = $(A\cup B)\setminus D$、
3 段 = $((A\cup B)\setminus D)\cup A$。

| 段 | source 数 | 断片 | 分類キー数 | 断片あたり | 最大ビット幅 |
|---|---:|---:|---:|---:|---:|
| 1 | 2 | 2,749 | 545 | 0.198 | **141** |
| 2 | 3 | 2,447 | 988 | 0.404 | **141** |
| 3 | 4 | 3,715 | 797 | 0.215 | **141** |

**読み取れること。**

- **ビット幅が段数で伸びません**（141 のまま。`SPEC-phase3.md` §10.3.1）。
  CSG は新しい平面を作らないので、構成点は何段重ねても
  「入力平面から 3 枚を選んだ交点」のままです。**これは完了条件です**
- **断片数も累積しません。** 2 段目で減っているのは差で面が消えるためで、
  段数に比例して増える形にはなっていません
- **分類コストは source 数と単調ではありません。** 3 段目で下がるのは
  $\cup A$ が領域を粗くするためです。**「段数に比例する」という CP2 時点の想定は
  そのままでは成り立ちません**

> **`BoolStats::regions` と `BoolStats::raycasts` は同じ地点で加算されます。**
> 比は構成上つねに 1.0 なので、**指標として見てはいけません**（一致しようがない量は
> 検査になりません）。分類コストは「断片あたりのレイキャスト」で見ています。

### 局所 BSP と過剰分割（`SPEC-phase3.md` §5.4 の効果）

| | 過剰分割 | 局所 BSP | 比 |
|---|---:|---:|---:|
| 生の断片 | 13,161 | **11,106** | **84.4%** |
| 正準化後 | 6,346 | **5,205** | **82.0%** |

切断候補 19,074（切った 15,717 / **省いた 3,357 = 17.6%**）。

固定深度 0〜3（機構を切った基準構成）で測ると **80.3% / 78.6%** です（`tests/csg/soup`）。
**適応分割のほうが削減幅が小さいのは、セルが既に小さく、過剰分割の側も
`plane_crosses_box` の絞り込みで落ちているためです。**

> **削減が 2 割にとどまるのは、比較対象が「素の過剰分割」ではないからです。**
> 加えて退化コーパスは軸平行が多く、平面が箱を横切る率がもともと高く出ます。
> **Thingi10K で測り直す価値があります。**

### CP5 の相互作用の計数（`tests/csg/cp5n`）

全 22 ケース × 3 演算 × （固定深度 0〜3 + 適応分割）= 330 構成。

| 量 | 値 |
|---|---:|
| 局所 BSP: 切った / 省いた | 57,255 / 14,844 |
| early-out セル | 10,623 |
| 構成点キャッシュの命中 | 2,062,851 |
| 代表点 段 0（軸平行） / 段 1（角のオフセット） | 8,958 / 32,847 |
| 分裂した頂点 | 87 |
| 相互作用: BSP×適応 / BSP×early-out / WNV×分裂 | 63 / 192 / 20 |

**代表点は段 1 が 8 割です。** 速い経路（段 0）が主経路になっていません。
**退化コーパスでは一般的な経路のほうが先に必要になる**という
`CLAUDE.md`「フォールバック連鎖は最も一般的なものを先に用意」の実例です。

### 凸分割（`SPEC-phase3.md` §4.1）

`tests/csg/convex` を実行すると同じ値が出ます。

| | 三角形化 | 凸分割 | 比 |
|---|---:|---:|---:|
| 片（入口、全 44 メッシュ） | 488 | **266** | **54.5%** |
| 平面（入口） | 501 | **325** | **64.9%** |
| 断片（ブール 330 構成） | 21,458 | **16,477** | **76.8%** |

**入口の時間は全体の 0.7% なので、凸分割が効くのは下流を通じてです。**
断片が 76.8% になっているので、中核（全体の 64.6%）に効きます。

> **既定は三角形化のままです。** 凸分割は実行時の選択で、
> **比較の基準側を明示するため**に既定を動かしていません（§4.1.2）。

### まだ測っていないもの

- **凸分割を既定にしたときの時間**（断片は減るが、入口の併合コストが増える）
- **葉の閾値の最適値**（§2.2）。現在は閾値なし（`leaf_threshold = 0`）で測っています
- **八分木が 3 軸を切る代償**（§2.2）
- $b{=}26$ での時間（幅の実測は `tests/arith/bitwidth` が見ています）
- **セグメントトレースの経路指標は Phase 5 へ移りました**（item 2 の降格に伴う）

---

## Phase 4 §9 の計数（CP1 到達時点）

**完了条件ではありません**（`SPEC-phase4.md` §6.3 の予測との整合を除く）。Phase 5 の基準線です。
b = 21、16 コアのマシン。**単発の差に原因を当てないこと。実行ごとに ±15% 動きます。**

### スケーリング（規模コーパス。`SPEC-phase4.md` §8.2）

**既存のコーパスではスケーリングが測れません。** 1 回のブール演算あたりの仕事が
1 ms 程度しかなく、**スレッドのディスパッチが並列区間の 1/3** を占めます。

| $k_c$（中核の速度向上） | 1 | 2 | 4 | 8 | 16 |
|---|---:|---:|---:|---:|---:|
| 球 × 2（8k 三角形、**一様**） | 1.00 | 1.65 | 2.88 | **4.61** | 5.49 |
| 立方体格子 × 球（**偏り**） | 1.00 | 1.43 | 2.16 | **3.05** | 3.10 |

**偏った分布では 3.05 で飽和します。** 葉ごとの仕事量の差が大きく、work-stealing でも
埋まりません。**負荷の偏りが上限を決めています。**

### 規模で profile が一変します ★

| 中核の内訳 | 小さいコーパス（488 三角形） | 規模コーパス（8k 三角形） |
|---|---:|---:|
| 前処理 + 葉の列挙 | 1.4% | 0.5% |
| **arrangement** | **67.8%** | **5.2%** |
| 縫合（**逐次**） | 14.7% | 1.4% |
| **分類（大域レイキャスト）** | **16.1%** | **92.9%** |

**大域レイキャストは 1 領域あたり $O(\text{シーン})$** なので、入力が大きくなると分類が
支配します。**「どこを並列化するか」を小さいコーパスで決めてはいけません** —
逆の結論が出ます。

> **Phase 5 の判断材料**: セグメントトレース（$O(\text{局所})$ 化）が縮めるのは
> この 92.9% です。`SPEC-phase3.md` が「局所化のための機構」と書いた理由が数字になりました。

### 並列化のコスト

| スレッド | 生成・join 132 回 | プールの `run` 132 回 |
|---|---:|---:|
| 2 | 7.8 ms | 4.1 ms |
| 4 | 14.2 ms | 7.1 ms |
| 8 | **26.2 ms** | **9.3 ms** |
| 16 | — | 13.9 ms |

**呼び出しごとの生成は中核の 40% を占めていました。** 永続プールで半分以下になりますが、
小さいコーパスでは依然 19%。**タスクの大きさに対してディスパッチが重い**という
形が残ります。

### 予測との整合（§6.1）

$f_{\text{out}}(k) = 40.9 / (0.8 + 75.9/k + 40.9)$、実測の $k_c$ で評価。

| スレッド | $k_c$ | 実測 | 予測（`BENCH` の基準） | 予測（**同じ実行の基準**） |
|---|---:|---:|---:|---:|
| 2 | 1.26 | 37.1% | 40.1% | **36.9%** |
| 4 | 1.49 | 41.2% | 44.1% | **40.8%** |
| 8 | 1.56 | 42.6% | 45.2% | **41.9%** |

**同じ実行の基準なら 1 ポイント以内で一致します。** 別の実行の定数を使うと、
±15% の変動がそのままモデルの誤差に見えます。**予測の基準をどの実行から取るかを
決めておくこと。**

### メモリ（スレッド局所の器）

| | 1 スレッド | 8 スレッド |
|---|---:|---:|
| 構成点キャッシュの項目 | 288 | **629** |
| 命中 | 506 | 165 |

**項目数がスレッド数で増えるのが、スレッド局所である証拠**です（同じ点が複数の
キャッシュに入る）。**減っていたら共有されています** — `tests/csg/parallel` が検査します。

### 出口のスケーリング（CP2 到達時点）★

`perf_corpus(4)`、`∪`、全機構有効、3 回の最小値（ms）。

`sphere2`（一様）:

| スレッド | 出口 | 構成点 | 併合 | 索引 | 三角形化 | 分裂 | $k_e$ |
|---|---:|---:|---:|---:|---:|---:|---:|
|  1 | 1744.9 | 23.6 | 27.4 | 1531.4 | 42.1 | 117.5 | **1.00** |
|  2 | 1039.3 | 26.9 | 32.9 |  839.7 | 23.6 | 113.2 | **1.68** |
|  4 |  640.4 | 26.2 | 26.6 |  461.8 | 12.9 | 110.0 | **2.72** |
|  8 |  443.5 | 24.1 | 25.4 |  276.0 |  8.5 | 106.2 | **3.93** |
| 16 |  375.4 | 24.0 | 27.5 |  204.9 |  6.8 | 109.2 | **4.65** |

`grid`（偏り）:

| スレッド | 出口 | 構成点 | 併合 | 索引 | 三角形化 | 分裂 | $k_e$ |
|---|---:|---:|---:|---:|---:|---:|---:|
|  1 | 1427.4 | 28.6 | 31.5 | 759.1 | 426.4 | 178.3 | **1.00** |
|  2 |  851.0 | 28.0 | 31.2 | 409.0 | 222.4 | 157.3 | **1.68** |
|  4 |  550.9 | 28.4 | 33.7 | 216.1 | 117.4 | 152.0 | **2.59** |
|  8 |  422.9 | 28.4 | 32.9 | 136.2 |  77.4 | 144.1 | **3.38** |
| 16 |  395.1 | 29.7 | 31.4 | 108.3 |  65.2 | 156.0 | **3.61** |

**索引が 7.5 倍、三角形化が 6.5 倍。** 逐次の 3 段が残って全体は 3.6〜4.7 倍です。

### 分裂の内訳 — 69% は「仕事」ではなく「検査」でした ★

`sphere2`、分裂 117.5 ms（1 スレッド）の内訳:

| 内訳 | 1 スレッド | 16 スレッド | 伸び |
|---|---:|---:|---:|
| 辺 → 三角形の表（逐次） | 11.5 | 11.8 | 1.0 |
| 診断・成分分け（逐次） | 3.7 | 4.3 | 0.9 |
| **扇の計算（並列）** | **18.0** | **2.8** | **6.4** |
| ID の割り当て（逐次） | 0.0 | 0.0 | — |
| **§5.5 の検算（`check_topology` × 2）** | **80.5** | **84.6** | **1.0** |

**検算を除いた出口の伸びは 5.7 倍（sphere2）/ 4.8 倍（grid）**です。

### ディスパッチの待ち — 小さい入力でだけ牙をむきます ★

`ThreadPool::run` が**全ワーカーの完了**を待っていたときの決定性テスト（792 件）:

| | real | user | sys |
|---|---:|---:|---:|
| 全ワーカーを待つ | **31.9 s** | 1.8 s | 0.2 s |
| **走り出したワーカーだけ待つ** | **1.04 s** | 1.55 s | 0.1 s |

**94% が待ちでした。** 規模コーパスでは 1 回の仕事が大きいので現れません
（上の表は修正の前後でほぼ同じ）。**小さい入力を大量に回す構成でだけ出ます。**

### 全域（CP3 到達時点）★ 予測と実測

**$k_c$ と $k_e$ は別々の実行から取っています**（中核だけ並列 / 出口だけ並列）。
同じ実行の内訳で書くと恒等式になり、何も検証しません（`IMPL-phase4.md` §3.2）。

予測 = 入口 + 中核（中核だけ並列の実行）+ 出口（出口だけ並列の実行）。

| コーパス | スレッド | 全体 (ms) | $k_c$ | $k_e$ | 実測 | 予測 | 差 |
|---|---|---:|---:|---:|---:|---:|---:|
| 小さい | 8 | 81.5 | 1.62 | 1.03 | **1.39** | 1.36 | +1.7% |
| sphere2 | 8 | 1557.7 | 4.68 | 4.44 | **4.64** | 4.61 | +0.8% |
| sphere2 | 16 | 1315.8 | 5.44 | 5.60 | **5.50** | 5.46 | +0.7% |
| grid | 8 | 1313.3 | 3.04 | 3.66 | **3.22** | 3.21 | +0.3% |
| grid | 16 | 1228.5 | 3.13 | 4.22 | **3.45** | 3.41 | +1.2% |

**全 13 構成で差は −0.4% 〜 +2.0%。** 中核と出口は互いに干渉していません。

### 小さいコーパスの出口 — 逐次が 61%

| 段 | 1 スレッド | 8 スレッド | |
|---|---:|---:|---|
| 構成点 | 3.2 | 3.3 | 逐次 |
| 併合 | 3.0 | 2.9 | 逐次 |
| **索引** | **0.9** | **2.9** | 並列。**3 倍遅い** |
| **三角形化** | **10.2** | **3.9** | 並列。2.6 倍 |
| 分裂・辺表 | 2.0 | 2.1 | 逐次 |
| 分裂・診断 | 0.5 | 0.5 | 逐次 |
| **分裂・扇** | **2.8** | **3.8** | 並列。**1.4 倍遅い** |
| 分裂・検算 | 13.2 | 12.9 | 逐次（§5.5） |
| **合計** | **36.2** | **33.2** | $k_e = 1.03$ |

**逐次 21.9 ms でアムダール上界は 1.65。** さらに索引と扇は
**1 項目あたりの仕事がディスパッチより小さい**ので遅くなります
（索引は 66 回で 0.9 ms = 1 回 0.014 ms）。

---

## 次に測るべきもの

Phase 1 の完了条件には含まれませんが、Phase 2 の設計判断に必要になります。

- **断片の構成点を保持したときの `intersect3` の削減幅**（§12 の比率から、
  ここが最も効くはず。`IMPL-phase1.md` §5.6）
- 混合幅 `det3` にしたときの `intersect3` の改善幅（単価のほう）
- **実際の時間**。Phase 1 は回数しか測っていません
- `side()` の分岐予測が外れる条件（構成点が平面上に載る割合が高い実データ）
- キャッシュミスを含む現実的なアクセスパターン（プール巡回ではなくランダム参照）
