# Krisite — ベンチマーク結果

SPEC-phase0.md §9「`side()` のスループットをベンチで計測し、数値を `docs/BENCH.md` に記録」。

> **これは基準線であって目標値ではありません。** Phase 0 では性能目標を設定しません
> （SPEC §9）。SIMD 化・浮動小数点フィルタは、この数値を見てから設計判断します（SPEC §10）。

## 測定環境

| 項目 | 値 |
|---|---|
| 日付 | 2026-08-26（SPEC 2026-08 改訂に追従後） |
| CPU | AMD Ryzen 7 9800X3D (8C/16T) |
| OS | Ubuntu 26.04 (Docker, Linux 6.12.67) |
| コンパイラ | clang 21.1.0（`zig c++` 経由） |
| ビルド | `Release` (`-O3 -DNDEBUG`), `KRISITE_CHECKED_ARITH=OFF` |
| `KRISITE_COORD_BITS` | 21 |
| 測定方法 | 各項目 3 回計測して最小値。入力は L1 に収まる 2048 要素のプールを巡回 |
| ばらつき | **同一条件でも実行ごとに ±15% 程度動く。**1 割前後の差に意味を持たせないこと |

> **注意**: これは開発コンテナの数値です。CI ランナー（GitHub Actions）の数値は大きく
> 揺れるため合否判定には使いません（`.github/workflows/ci.yml` の `bench` ジョブ）。

再現手順:

```bash
cmake -B build-rel -G Ninja -DCMAKE_BUILD_TYPE=Release \
  -DKRISITE_CHECKED_ARITH=OFF -DKRISITE_BUILD_BENCH=ON
cmake --build build-rel
./build-rel/bench/pred_bench
./build-rel/bench/arith_bench
```

## 述語（`bench/pred_bench.cpp`）

`side` は 209 ビット / 4 リム、`cmp_h` は 300 ビット / 5 リムで評価（SPEC §3.3）。

| 演算 | ns/op | Mops/s |
|---|---:|---:|
| **`side(plane, HPoint)`** ★最重要 | **7.80** | **128.3** |
| `side(plane, IPoint)` | 2.35 | 426.0 |
| `orient3d(IPoint × 4)` | 12.87 | 77.7 |
| `orient2d(IPoint × 3, Z)` | 2.08 | 481.6 |
| `cmp_h(HPoint × 2, X)` | 11.25 | 88.9 |
| `lex_less(HPoint × 2)` | 11.42 | 87.6 |
| `plane_from_triangle` | 3.38 | 295.7 |
| `intersect3(Plane × 3)` | 261.76 | 3.8 |

## 固定幅整数（`bench/arith_bench.cpp`）

| 演算 | 1 リム (64bit) | 2 リム (128bit) | 4 リム (256bit) |
|---|---:|---:|---:|
| `add` | 0.23 ns | 0.38 ns | 0.70 ns |
| `mul` | 0.56 ns | 1.90 ns | 11.63 ns |
| `cmp` | 0.43 ns | 0.41 ns | 0.41 ns |
| `sign` | 0.41 ns | 0.40 ns | 0.36 ns |

`cmp` と `sign` がリム数にほぼ依存しないのは、符号ビットと最上位リムで決着する経路が
効いているため（SPEC §5.3）。`mul` はリム数の 2 乗で伸びており、筆算の計算量と一致します。

## 読み取れること

**`side()` は 7.8 ns（128 Mops/s）。** 浮動小数点フィルタを入れるかどうかの判断材料として、
これが基準線になります。EMBER が「フィルタ不要」と主張する根拠と整合する水準です。

**`cmp_h` は `side` より重い（11.3 ns）。** 被符号値が 300 ビット（5 リム）と Phase 0 で
最大幅であること（SPEC §3.3）が素直に出ています。

なお、リム単位の積が生む 7 リムを §3 の上界である 5 リムへ詰める `resize` に、
**実行時コストはありません**（`resize` の有無だけを変えた A/B で 9.66 対 9.67 ns）。
Release では下位リムのコピーに縮み、符号を取るだけの後段では幅の差が出ません。

**`intersect3()` が 262 ns で突出して重い。** これは実装の選択によるもので、
`intersect3` は 3x3 行列式を 4 回計算しますが、列ごとに幅が違う（法線 1 リム、
オフセット 2 リム）ため、**正しさを優先して全列を 2 リムにそろえてから** 7 リム幅で
計算しています（`geom/plane.hpp`）。列ごとの幅を保った混合幅の行列式にすれば
数倍は縮むはずですが、Phase 0 では手を入れません（SPEC §0「性能より正しさを優先」）。

なお EMBER の想定では構成点は一度作って何度も `side` にかけるので、
`intersect3` の重さが支配的になるとは限りません。**Phase 1 で実際の比率を測ってから**
最適化対象を決めてください。

## Phase 1 §4.3.3 / §5.4 の計数（CP3 到達時点）

**性能ではなく設計判断のための数値です**（SPEC-phase1 §13）。
`tests/csg/test_cp3.cpp` を実行すると同じ値が出ます。b = 21。

断片数は正準化後（重複除去後）。最大枚数・値併合・広がりは深度 0〜3 の最大値です。

| # | 構成 | 断片数（深度 0/1/2/3） | 最大枚数 | うち mesh | 値併合 | 広がり |
|---|---|---|---:|---:|---:|---:|
| 1 | 一般位置 | 48 / 108 / 192 / 366 | 3 | 3 | 0 | 1 |
| 2 | 面が完全共平面 | 40 / 40 / 56 / 224 | 3 | 3 | 0 | 1 |
| 3 | 辺が一致（共線） | 34 / 34 / 66 / 264 | 3 | 3 | 0 | 1 |
| 4 | 頂点が一致 | 26 / 26 / 38 / 152 | 3 | 3 | 0 | 1 |
| 5 | 面がセル境界と一致 | 48 / 48 / 48 / 192 | 3 | 3 | 0 | 1 |
| 6 | セル境界から 1 格子ずれ | 48 / 108 / 192 / 432 | 3 | 3 | 0 | 1 |
| **7** | **量子化した回転立方体** | 65 / 117 / 117 / 254 | **6** | **4** | **87** | 1 |
| 8 | 同一の立方体 | 12 / 48 / 48 / 192 | 3 | 3 | 0 | 1 |
| **9** | **四面体 2 個（一般位置）** | 44 / 79 / 137 / 279 | **6** | 3 | **18** | 1 |
| 10 | 一方が他方を完全に含む | 60 / 120 / 120 / 120 | 3 | 3 | 0 | 1 |
| 11a | 面接触のみ | 32 / 32 / 40 / 160 | 3 | 3 | 0 | 1 |
| 11b | 辺接触のみ | 12 / 12 / 34 / 136 | 3 | 3 | 0 | 1 |
| 12 | セル境界をまたぐ大立方体 | 48 / 108 / 246 / 366 | 3 | 3 | 0 | 1 |
| **2T** | **共平面接触を斜面で** | 19 / 57 / 73 / 163 | **6** | 3 | **83** | 1 |
| **4T** | **四面体 2 個が頂点を共有** | 20 / 20 / 62 / 182 | **9** | **6** | **18** | 1 |
| **4T′** | **頂点共有 + 体積も重なる** | 35 / 35 / 95 / 251 | **9** | **6** | **32** | 1 |
| **5T** | **セル角を斜面が通る** | 42 / 42 / 72 / 204 | **6** | **4** | **72** | 1 |
| D | 交わらない 2 立方体 | 32 / 60 / 96 / 308 | 3 | 3 | 0 | 1 |

**読み取れること。**

- **軸平行な 12 ケースは例外なく「3 枚・値併合 0」です。** 1 点を通れる平面は
  1 軸につき高々 1 枚で軸は 3 本だけ、かつセル面が立方体の面と一致しても同一の
  幾何平面なので `PlaneTable` が同じ ID に併合するためです（§9.1）。
  **ケース 4（頂点が一致）も 3 枚止まり**でした
- **斜面を持つ 6 ケースだけが 4 枚以上**（最大 9 枚）。§5.4 の「5 枚以上が出るか」の
  答えは **YES** です。四面体の頂点には面が 3 枚集まるので、2 個が頂点を共有すれば 6 枚
- **§5.3 の第2段は必須です。** 値併合が最大 87 件。**軸平行だけなら 0 件**なので、
  §10.5 の変異 1 は斜面コーパスでしか検出できません
- **併合は局所に閉じます。** 広がりは全ケース・全深度で **1 以下**、つまり 1 セルと
  その面・辺・頂点隣接に収まります。発生率が高くても大域整列は不要で、
  **Phase 3 は「セル並列 + 境界併合」で組めます**（§5.4）
- **深度を上げても断片が増えないケースがあります**（5, 8, 10, 2T など）。面がセル境界に
  乗ると、その境界平面は §4.3.1 の大域分割が既に使っているので新しい切断が入りません。
  §9.0 の番人はこのために「非減少 かつ 深度 0 → 3 で狭義増加」で判定します


### 述語のビット幅（CP2.5 で追加した 4 本）

代表点のフォールバックに使う述語です。**点を構成せず**、被符号値の線形性で評価します。
中点は頂点数 4 以上の断片、重心は三角形の断片に使います（`IMPL-phase1.md` §2.5.1、§2.5.2）。

| 述語 | 理論 (b=21) | 実測 | 理論リム | 実測リム |
|---|---:|---:|---:|---:|
| `side(plane, 中点)` $15b{+}33$ | 348 | 317 | 6 | 5 |
| `orient2d_h(中点)` $14b{+}30$ | 324 | 297 | 6 | 5 |
| `side(plane, 重心)` $21b{+}46$ | 487 | 443 | 8 | 7 |
| `orient2d_h(重心)` $20b{+}43$ | 463 | 423 | 8 | 7 |

上界超過はありません。実測リムが 1 つ小さいのは乱択が真の最大に届かないためで、
**リム数は減らしません**（SPEC-phase0 §3.4）。

### §12 `side` : `intersect3` の呼び出し比率 ★

全 18 ケース × 3 演算 × 深度 0〜3 の合計。

| 述語 | 呼び出し回数 |
|---|---:|
| `side` | 2,713,359 |
| `intersect3` | 2,156,253 |
| **比** | **1.26 : 1** |

**IMPL-phase0 §7 の前提「構成点は一度作って何度も `side` にかける」は成り立って
いません。** `split_fragment` が `side(q, fragment_vertex(...))` の形で構成点を
毎回作り直すため、`side` 1 回につき `intersect3` が 1 回必要になります。

上の実測時間（`side` 11.25 ns / `intersect3` 262 ns）で重み付けすると、
**述語の時間の 94.9% を `intersect3` が占めます**（推定。回数は厳密だが単価は
別条件のベンチマーク値で、実際の時間は測っていない）。

**Phase 2 では「構成点の保持」を `intersect3` の混合幅化より先に検討してください。**
単価を下げても、回数が `side` と同数のままでは支配的な構図が変わりません。
詳細は `IMPL-phase1.md` §5.6。

計測はまだ**時間**を測っていません（Phase 1 に性能要件は無いため）。

## Phase 2 §11 の計数（CP1 到達時点）

**性能ではなく設計判断のための数値です**（SPEC-phase2 §11）。完了条件ではありません。
`tests/csg/split_strategy` を実行すると同じ値が出ます。b = 21。

### 分割平面の絞り込み（§2.3）

全 18 ケース × 3 演算の合計。「総当たり」は絞り込み前の「セル × 分割平面」の数、
「使用」は `plane_crosses_box` が通した数です。

| 深度 | 使用 / 総当たり | 残存率 | 最大/セル |
|---|---|---:|---:|
| 0 | 540 / 540 | **100.0%** | 12 |
| 1 | 2,739 / 4,320 | 63.4% | 10 |
| 2 | 12,927 / 34,560 | 37.4% | 9 |
| 3 | 54,867 / 276,480 | **19.8%** | 7 |

**読み取れること。**

- **深度 3 で 8 割が落ちます。** セルが小さくなるほど、その閉包と交わらない平面が
  増えるためです。残存率は深度とともに単調に減少します
- **深度 0 では 1 枚も落ちません。** 入力座標は座標範囲内にあるので、そこから作った
  平面は座標範囲全体と必ず交わります。**これは番人です。**深度 0 で落ちたら判定が
  壊れています（`IMPL-phase2.md` §2.3）
- **断片数は 1 つも減っていません。** 絞り込みが落とすのは no-op な分割だけなので
  当然で、**§11 の「断片数」が動くのは CP2（適応分割）から**です。
  **CP1 の利得は断片数ではなく「切る回数」です**
- 軸平行なケース（1 / 6 / 8 / 10 / 12 / D）は深度 1 で 50.0% ちょうど。軸平行平面は
  自分に垂直な方向のセル列としか交わりません。斜面を持つケース（7 / 9 / 2T）は
  65〜81% と落ちにくく、**斜面ほど多くのセルを横切る**という素直な結果です

### 述語のビット幅（CP1 で追加した 1 本）

| 述語 | 理論 (b=21) | 実測 | 理論 (b=26) | 実測 | リム |
|---|---:|---:|---:|---:|---:|
| 平面とセルの閉領域の交差 $3b{+}7$ | 70 | 65 | 85 | 80 | 2 |

上界超過はありません。**リム数は減らしません**（SPEC-phase0 §3.4）。

### 適応分割（§3、CP2）

全 18 ケース × 3 演算 × 最大深度 3 の合計。**固定深度（Phase 1 の挙動）が正解器**です。

| 指標 | 固定深度 | 適応分割 |
|---|---:|---:|
| 葉 | 32,256 | **7,434** |
| 断片（正準化後） | 15,624 | **9,714** |
| 葉の深さの差 | 0 | **最大 2 段**（2 段以上が出たのは 7 ケース） |

**§13 の CP2 の判定「節約が実際に出ているか」は満たしています。**
葉が 77% 減り、断片が 38% 減りました。

### T 頂点の解決（§2.4.3、案 D）

| 指標 | 値 |
|---|---:|
| 挿入された T 頂点 | 336 |
| 1 本の辺に載った T 頂点の最大 | **3** |
| **残した**退化三角形（§2.4.4 (2)） | **20** |
| 固定深度での挿入（負の対照） | **0** |

**残した退化三角形は 20 枚です。** 扇の起点を「両隣の元の辺に T 頂点が無い元の角」に
取れば退化は出ませんが、どの角も条件を満たさない多角形が残ります。**捨てません** —
捨てると境界辺が消えて次数 1 の辺が出ます（§2.4.4 (2) の訂正）。
一般解（元の角だけで三角形化してから辺ごとに扇を張る）は、この枚数が実際に効いてから
判断します。

### early-out（§3.2 / §3.3）

適応分割の上で early-out の有無を比べたもの。**無効側が正解器**です。

| 指標 | 無効 | 有効 |
|---|---:|---:|
| 断片 | 9,714 | **8,217** |
| レイキャスト | 1,056 | **660** |
| 領域（相異なる符号ベクトル） | 1,056 | **660** |

| 発火 | 値 |
|---|---:|
| 葉 | 7,434 |
| うち arrangement を省いたセル | **2,787** |
| うち丸ごと飛ばしたセル（どちらも居ない） | **2,727** |
| **セル数ベースの発火率** | **74.2%** |
| 分類を省いた断片 | 3,261（**39.7%**） |
| セルの隅（**整数点**）で済ませた判定 | 2,787 |

**判定が整数座標だけで済むのが効きます**（§3.2）。`side(plane, IPoint)` は 2.35 ns、
`side(plane, HPoint)` は 7.80 ns。省いた 2,787 セルぶんは**構成点を一切作りません。**

**セルの `lo` 隅を使います。** `hi` 隅は $+2^{b-1}$ になり得て `IPoint` に入りませんが、
`lo` 隅は必ず範囲内です。**`lo` 隅が $\partial B$ の上に無いことは割り当てが保証します** —
$\partial B$ が隅を含むなら、その面の AABB は隅を含むので割り当てられ $n_B > 0$ になり、
early-out は発火しません。

### 重複断片が 0 になりました ★ 指標が壊れたわけではありません

**§3.4 でセル割り当てを半開区間 $[\mathrm{lo}, \mathrm{hi})$ に変えたためです。**

| ケース 5 の $\cup$ | 葉 | 断片 | 重複断片 | 生 |
|---|---:|---:|---:|---:|
| 固定深度（閉領域、Phase 1） | 512 | 192 | **144** | 336 |
| 固定深度（半開、現在） | 512 | 192 | **0** | 192 |
| 適応分割（閉領域） | 253 | **204** | 96 | 300 |
| 適応分割（半開、現在） | 106 | **84** | **0** | 84 |

閉領域では**セル境界平面に完全に乗る面が両側のセルに入り**、固定深度では両側の断片が
一致して `region_key` が併合していました。**適応分割では併合しません**（粗い側の 1 枚と
細かい側の 4 枚は「同一領域」ではない）。同じ面積が二重に出力され、$C$ が破裂します。

**固定深度の断片数（192）は変わっていません。** 重複が最初から生じなくなっただけで、
出力は同じです。`region_key` の併合経路も死んでいません — $A$ と $B$ の共平面重複
（`SPEC-phase1.md` §4.3.2）で引き続き使われます。消えたのは
**「同一 owner の、セル境界由来の重複」だけ**で、これは要件ではなく副産物でした。

### 構成点の保持（§4）★ Phase 1 の前提を作り直しました

**Phase 1 の実測が示した最優先項目です。**

| 指標（全 18 ケース × 3 演算 × 深度 0〜3） | 保持なし | **保持あり** |
|---|---:|---:|
| `side` の呼び出し | 4,630,307 | 4,630,307 |
| `intersect3` の呼び出し | 1,539,819 | **28,836（1.9%）** |
| **比** | **3.01 : 1** | **160.57 : 1** |
| Phase 0 の単価で重み付けした `intersect3` の時間占有 | 88.6% | **12.7%** |

| キャッシュ | 値 |
|---|---:|
| 参照 | 1,539,819 |
| ヒット | 1,510,983（**98.1%**） |
| 実体 | 28,836 個 |
| 占有メモリ（1 回の最大） | **49,032 B**（約 48 KB） |

**キャッシュの有無で出力は 1 ビットも変わりません。** 平面3つ組を昇順に並べてから
`intersect3` を呼ぶので、`tests/csg/test_cache.cpp` は**値の完全一致**を要求できます。

**第2段の値併合は 1,704 件で健在です**（§4.3 の分担）。4 平面以上が一点で交わる場合、
異なる3つ組が同じ値を持ちます。**これはキャッシュの誤りではありません。**
キャッシュは3つ組で引き、値の同一性は第2段が扱う、という分担が保たれています。

### 実時間 ★ Phase 1 で測っていないもの（§11）

全 18 ケース × 3 演算 × 深度 3。**合否には使いません。**

| 構成 | 時間 |
|---|---:|
| 固定深度（Phase 1 の挙動） | 220.9 ms |
| 固定深度 + 構成点の保持 | 121.0 ms |
| 適応分割 | 126.4 ms |
| 適応分割 + early-out | 89.6 ms |
| **適応分割 + early-out + 構成点の保持** | **47.6 ms** |

**Phase 1 の構成から 4.6 倍**。ただし **±15% のばらつきがあります**（同一条件でも実行ごとに
動きます）。**1 割前後の差に意味を持たせないこと。** これは基準線であって目標値ではなく、
Phase 4 で性能目標を立てるときに使います。

### 接触の分裂（§5、§5.7 の記録）

全 19 ケース × 3 演算 × 深度 0〜3 = 264 構成。

| 指標 | 値 |
|---|---:|
| 分裂した頂点 | 64 |
| 次数 3 以上の辺 | 36（端点 56） |
| 扇の個数 $k$ の最大 | **3** |
| 分裂前に非多様体だった構成 | 16 |
| 分裂前に $\chi$ が奇数だった構成 | 8 |
| **§9.3 の除外（分裂後）** | **0 件** |
| **§5.1.2.1 の停止に到達した配置** | **0**（radial sort は不要） |

**§5.5 の予測（$\Delta V$ / $\Delta E$ / $\Delta\chi$）は全構成で実測と一致します。**

| ケース | 演算 | $\chi$（前 → 後） | $C$（前 → 後） | $k$ |
|---|---|---|---|---|
| 4T | $\cup$ | 3 → **4** | 2 → 2 | 2 |
| 4T′ | $A \setminus B$ | 4 → **6** | 3 → 3 | **3** |
| 11b | $\cup$ | 3 → **4** | 2 → 2 | 2 |
| **16**（自己接触） | $A \setminus B$ | 4 → **8** | 4 → 4 | 2 |

ケース 16 の深度 0 は §8.2 の期待値に完全一致（次数 4 の辺 4 本、$\Delta E=4$、
$\Delta V=8$、$\Delta\chi=4$）。**深度 1 以降は接触辺がセル平面で分割されて
$\Delta E=8$、$\Delta V=12$ になりますが、$\Delta\chi=4$ は深度不変です。**

### まだ測っていないもの（CP2 以降）

early-out の発火率、構成点キャッシュのヒット率と占有量、`side` : `intersect3` の
新しい比、**そして時間**。時間は Phase 1 でも Phase 2 CP1 でも測っていません。

---

## Phase 3 §11 の計数と時間（CP5 到達時点）

**性能ではなく設計判断のための基準線です**（`SPEC-phase3.md` §11）。完了条件ではありません。
`bench/soup_bench` を実行すると同じ値が出ます（`-DKRISITE_BUILD_BENCH=ON`、Release、b = 21）。
**単発の差に原因を当てないこと。実行ごとに ±15% 程度動きます。**

### 入口・中核・出口の内訳 ★

全 22 ケース × 3 演算、適応分割（深度 3）+ 全機構有効。

| 区分 | 時間 (ms) | 割合 | 主な計数 |
|---|---:|---:|---|
| `from_mesh` | 0.8 | **0.7%** | 多角形 1,464、平面 1,503 |
| **`boolean`（中核）** | **75.9** | **64.6%** | 断片 5,205、分類キー 1,635 |
| `to_mesh` | 40.9 | **34.8%** | 三角形 9,180、値で併合 3,221、T 頂点 623 |
| 合計 | 117.6 | 100.0% | セル 8,004（うち early-out 2,112 = **26.4%**）|

**読み取れること。**

- **入口は無視できます**（0.7%）。凸分割を入れても入口の時間は問題になりません。
  効くとすれば**下流の断片数**を通じてです
- **出口が 3 分の 1 を占めます。** `SPEC-phase3.md` §11 の但し書き
  「EMBER の 1.6 ms はスープを出すまでの時間」がそのまま効いています。
  **中核だけを比べるなら 64.6% の側と比べること。** 全体で比べると
  **1.5 倍不利な比較**になります
- **early-out がセルの 26.4% で発火しています。** 適応分割との併用で、
  4 分の 1 のセルが分類を隅 1 点で済ませています

### 連鎖の段数に対する推移

$k$ 段目まで連鎖したときの値。1 段 = $A \cup B$、2 段 = $(A\cup B)\setminus D$、
3 段 = $((A\cup B)\setminus D)\cup A$。

| 段 | source 数 | 断片 | 分類キー数 | 断片あたり | 最大ビット幅 |
|---|---:|---:|---:|---:|---:|
| 1 | 2 | 2,749 | 545 | 0.198 | **141** |
| 2 | 3 | 2,447 | 988 | 0.404 | **141** |
| 3 | 4 | 3,715 | 797 | 0.215 | **141** |

**読み取れること。**

- **ビット幅が段数で伸びません**（141 のまま。`SPEC-phase3.md` §10.3.1）。
  CSG は新しい平面を作らないので、構成点は何段重ねても
  「入力平面から 3 枚を選んだ交点」のままです。**これは完了条件です**
- **断片数も累積しません。** 2 段目で減っているのは差で面が消えるためで、
  段数に比例して増える形にはなっていません
- **分類コストは source 数と単調ではありません。** 3 段目で下がるのは
  $\cup A$ が領域を粗くするためです。**「段数に比例する」という CP2 時点の想定は
  そのままでは成り立ちません**

> **`BoolStats::regions` と `BoolStats::raycasts` は同じ地点で加算されます。**
> 比は構成上つねに 1.0 なので、**指標として見てはいけません**（一致しようがない量は
> 検査になりません）。分類コストは「断片あたりのレイキャスト」で見ています。

### 局所 BSP と過剰分割（`SPEC-phase3.md` §5.4 の効果）

| | 過剰分割 | 局所 BSP | 比 |
|---|---:|---:|---:|
| 生の断片 | 13,161 | **11,106** | **84.4%** |
| 正準化後 | 6,346 | **5,205** | **82.0%** |

切断候補 19,074（切った 15,717 / **省いた 3,357 = 17.6%**）。

固定深度 0〜3（機構を切った基準構成）で測ると **80.3% / 78.6%** です（`tests/csg/soup`）。
**適応分割のほうが削減幅が小さいのは、セルが既に小さく、過剰分割の側も
`plane_crosses_box` の絞り込みで落ちているためです。**

> **削減が 2 割にとどまるのは、比較対象が「素の過剰分割」ではないからです。**
> 加えて退化コーパスは軸平行が多く、平面が箱を横切る率がもともと高く出ます。
> **Thingi10K で測り直す価値があります。**

### CP5 の相互作用の計数（`tests/csg/cp5n`）

全 22 ケース × 3 演算 × （固定深度 0〜3 + 適応分割）= 330 構成。

| 量 | 値 |
|---|---:|
| 局所 BSP: 切った / 省いた | 57,255 / 14,844 |
| early-out セル | 10,623 |
| 構成点キャッシュの命中 | 2,062,851 |
| 代表点 段 0（軸平行） / 段 1（角のオフセット） | 8,958 / 32,847 |
| 分裂した頂点 | 87 |
| 相互作用: BSP×適応 / BSP×early-out / WNV×分裂 | 63 / 192 / 20 |

**代表点は段 1 が 8 割です。** 速い経路（段 0）が主経路になっていません。
**退化コーパスでは一般的な経路のほうが先に必要になる**という
`CLAUDE.md`「フォールバック連鎖は最も一般的なものを先に用意」の実例です。

### 凸分割（`SPEC-phase3.md` §4.1）

`tests/csg/convex` を実行すると同じ値が出ます。

| | 三角形化 | 凸分割 | 比 |
|---|---:|---:|---:|
| 片（入口、全 44 メッシュ） | 488 | **266** | **54.5%** |
| 平面（入口） | 501 | **325** | **64.9%** |
| 断片（ブール 330 構成） | 21,458 | **16,477** | **76.8%** |

**入口の時間は全体の 0.7% なので、凸分割が効くのは下流を通じてです。**
断片が 76.8% になっているので、中核（全体の 64.6%）に効きます。

> **既定は三角形化のままです。** 凸分割は実行時の選択で、
> **比較の基準側を明示するため**に既定を動かしていません（§4.1.2）。

### まだ測っていないもの

- **凸分割を既定にしたときの時間**（断片は減るが、入口の併合コストが増える）
- **葉の閾値の最適値**（§2.2）。現在は閾値なし（`leaf_threshold = 0`）で測っています
- **八分木が 3 軸を切る代償**（§2.2）
- $b{=}26$ での時間（幅の実測は `tests/arith/bitwidth` が見ています）
- **セグメントトレースの経路指標は Phase 5 へ移りました**（item 2 の降格に伴う）

---

## Phase 4 §9 の計数（CP1 到達時点）

**完了条件ではありません**（`SPEC-phase4.md` §6.3 の予測との整合を除く）。Phase 5 の基準線です。
b = 21、16 コアのマシン。**単発の差に原因を当てないこと。実行ごとに ±15% 動きます。**

### スケーリング（規模コーパス。`SPEC-phase4.md` §8.2）

**既存のコーパスではスケーリングが測れません。** 1 回のブール演算あたりの仕事が
1 ms 程度しかなく、**スレッドのディスパッチが並列区間の 1/3** を占めます。

| $k_c$（中核の速度向上） | 1 | 2 | 4 | 8 | 16 |
|---|---:|---:|---:|---:|---:|
| 球 × 2（8k 三角形、**一様**） | 1.00 | 1.65 | 2.88 | **4.61** | 5.49 |
| 立方体格子 × 球（**偏り**） | 1.00 | 1.43 | 2.16 | **3.05** | 3.10 |

**偏った分布では 3.05 で飽和します。** 葉ごとの仕事量の差が大きく、work-stealing でも
埋まりません。**負荷の偏りが上限を決めています。**

### 規模で profile が一変します ★

| 中核の内訳 | 小さいコーパス（488 三角形） | 規模コーパス（8k 三角形） |
|---|---:|---:|
| 前処理 + 葉の列挙 | 1.4% | 0.5% |
| **arrangement** | **67.8%** | **5.2%** |
| 縫合（**逐次**） | 14.7% | 1.4% |
| **分類（大域レイキャスト）** | **16.1%** | **92.9%** |

**大域レイキャストは 1 領域あたり $O(\text{シーン})$** なので、入力が大きくなると分類が
支配します。**「どこを並列化するか」を小さいコーパスで決めてはいけません** —
逆の結論が出ます。

> **Phase 5 の判断材料**: セグメントトレース（$O(\text{局所})$ 化）が縮めるのは
> この 92.9% です。`SPEC-phase3.md` が「局所化のための機構」と書いた理由が数字になりました。

### 並列化のコスト

| スレッド | 生成・join 132 回 | プールの `run` 132 回 |
|---|---:|---:|
| 2 | 7.8 ms | 4.1 ms |
| 4 | 14.2 ms | 7.1 ms |
| 8 | **26.2 ms** | **9.3 ms** |
| 16 | — | 13.9 ms |

**呼び出しごとの生成は中核の 40% を占めていました。** 永続プールで半分以下になりますが、
小さいコーパスでは依然 19%。**タスクの大きさに対してディスパッチが重い**という
形が残ります。

### 予測との整合（§6.1）

$f_{\text{out}}(k) = 40.9 / (0.8 + 75.9/k + 40.9)$、実測の $k_c$ で評価。

| スレッド | $k_c$ | 実測 | 予測（`BENCH` の基準） | 予測（**同じ実行の基準**） |
|---|---:|---:|---:|---:|
| 2 | 1.26 | 37.1% | 40.1% | **36.9%** |
| 4 | 1.49 | 41.2% | 44.1% | **40.8%** |
| 8 | 1.56 | 42.6% | 45.2% | **41.9%** |

**同じ実行の基準なら 1 ポイント以内で一致します。** 別の実行の定数を使うと、
±15% の変動がそのままモデルの誤差に見えます。**予測の基準をどの実行から取るかを
決めておくこと。**

### メモリ（スレッド局所の器）

| | 1 スレッド | 8 スレッド |
|---|---:|---:|
| 構成点キャッシュの項目 | 288 | **629** |
| 命中 | 506 | 165 |

**項目数がスレッド数で増えるのが、スレッド局所である証拠**です（同じ点が複数の
キャッシュに入る）。**減っていたら共有されています** — `tests/csg/parallel` が検査します。

### 出口のスケーリング（CP2 到達時点）★

`perf_corpus(4)`、`∪`、全機構有効、3 回の最小値（ms）。

`sphere2`（一様）:

| スレッド | 出口 | 構成点 | 併合 | 索引 | 三角形化 | 分裂 | $k_e$ |
|---|---:|---:|---:|---:|---:|---:|---:|
|  1 | 1744.9 | 23.6 | 27.4 | 1531.4 | 42.1 | 117.5 | **1.00** |
|  2 | 1039.3 | 26.9 | 32.9 |  839.7 | 23.6 | 113.2 | **1.68** |
|  4 |  640.4 | 26.2 | 26.6 |  461.8 | 12.9 | 110.0 | **2.72** |
|  8 |  443.5 | 24.1 | 25.4 |  276.0 |  8.5 | 106.2 | **3.93** |
| 16 |  375.4 | 24.0 | 27.5 |  204.9 |  6.8 | 109.2 | **4.65** |

`grid`（偏り）:

| スレッド | 出口 | 構成点 | 併合 | 索引 | 三角形化 | 分裂 | $k_e$ |
|---|---:|---:|---:|---:|---:|---:|---:|
|  1 | 1427.4 | 28.6 | 31.5 | 759.1 | 426.4 | 178.3 | **1.00** |
|  2 |  851.0 | 28.0 | 31.2 | 409.0 | 222.4 | 157.3 | **1.68** |
|  4 |  550.9 | 28.4 | 33.7 | 216.1 | 117.4 | 152.0 | **2.59** |
|  8 |  422.9 | 28.4 | 32.9 | 136.2 |  77.4 | 144.1 | **3.38** |
| 16 |  395.1 | 29.7 | 31.4 | 108.3 |  65.2 | 156.0 | **3.61** |

**索引が 7.5 倍、三角形化が 6.5 倍。** 逐次の 3 段が残って全体は 3.6〜4.7 倍です。

### 分裂の内訳 — 69% は「仕事」ではなく「検査」でした ★

`sphere2`、分裂 117.5 ms（1 スレッド）の内訳:

| 内訳 | 1 スレッド | 16 スレッド | 伸び |
|---|---:|---:|---:|
| 辺 → 三角形の表（逐次） | 11.5 | 11.8 | 1.0 |
| 診断・成分分け（逐次） | 3.7 | 4.3 | 0.9 |
| **扇の計算（並列）** | **18.0** | **2.8** | **6.4** |
| ID の割り当て（逐次） | 0.0 | 0.0 | — |
| **§5.5 の検算（`check_topology` × 2）** | **80.5** | **84.6** | **1.0** |

**検算を除いた出口の伸びは 5.7 倍（sphere2）/ 4.8 倍（grid）**です。

### ディスパッチの待ち — 小さい入力でだけ牙をむきます ★

`ThreadPool::run` が**全ワーカーの完了**を待っていたときの決定性テスト（792 件）:

| | real | user | sys |
|---|---:|---:|---:|
| 全ワーカーを待つ | **31.9 s** | 1.8 s | 0.2 s |
| **走り出したワーカーだけ待つ** | **1.04 s** | 1.55 s | 0.1 s |

**94% が待ちでした。** 規模コーパスでは 1 回の仕事が大きいので現れません
（上の表は修正の前後でほぼ同じ）。**小さい入力を大量に回す構成でだけ出ます。**

### 全域（CP3 到達時点）★ 予測と実測

**$k_c$ と $k_e$ は別々の実行から取っています**（中核だけ並列 / 出口だけ並列）。
同じ実行の内訳で書くと恒等式になり、何も検証しません（`SPEC-phase4.md` §6.0）。

予測 = 入口 + 中核（中核だけ並列の実行）+ 出口（出口だけ並列の実行）。

| コーパス | スレッド | 全体 (ms) | $k_c$ | $k_e$ | 実測 | 予測 | 差 |
|---|---|---:|---:|---:|---:|---:|---:|
| 小さい | 8 | 79.1 | 1.59 | 1.11 | **1.41** | 1.39 | +1.6% |
| sphere2 | 8 | 1558.1 | 4.69 | 4.41 | **4.62** | 4.61 | +0.1% |
| sphere2 | 16 | 1302.7 | 5.48 | 5.45 | **5.52** | 5.46 | +1.1% |
| grid | 8 | 1293.7 | 3.08 | 3.65 | **3.27** | 3.24 | +0.9% |
| grid | 16 | 1229.8 | 3.15 | 4.30 | **3.44** | 3.45 | −0.2% |

**全 13 構成で差は −0.7% 〜 +1.6%。** 中核と出口は互いに干渉していません。

> **測定は単独で回すこと。** 変異ビルドを並走させた回では `sphere2` の
> 16 スレッドが 5.52 → 4.97 になりました（10% 差）。

### 小さいコーパスの出口 — 逐次が 61%

| 段 | 1 スレッド | 8 スレッド | |
|---|---:|---:|---|
| 構成点 | 3.2 | 3.2 | 逐次 |
| 併合 | 2.9 | 3.0 | 逐次 |
| 索引 | 0.9 | 0.9 | 下限で逐次 |
| **三角形化** | **10.4** | **4.8** | 並列。**2.2 倍** |
| 分裂・辺表 | 2.0 | 2.1 | 逐次 |
| 分裂・診断 | 0.5 | 0.5 | 逐次 |
| 分裂・扇 | 2.8 | 2.8 | 下限で逐次 |
| 分裂・検算 | 12.9 | 12.8 | 逐次（§5.5） |
| **合計** | **36.1** | **30.7** | $k_e = 1.11$ |

**アムダール上界は 36.1 / 21.5 = 1.68。**

### ディスパッチの下限（§6.3）★ 段の床

**1 項目あたりの仕事が段ごとに違うので、閾値は 1 つでは足りません。**

| 段（小さいコーパス） | 項目数（中央） | 1 回の仕事 | 1 項目 | 損益分岐 | 採った下限 |
|---|---:|---:|---:|---:|---:|
| 索引: 平面 | 8 | 0.014 ms | 1.7 µs | 18 | 64 |
| 三角形化: 多角形 | 72 | 0.155 ms | 2.2 µs | 14 | 64 |
| 中核: 葉 | 113 | 1.16 ms | 9.6 µs | 3 | 64 |
| 分裂・扇: 頂点 | 65 | 0.042 ms | 0.6 µs | 50 | **256** |

ディスパッチのコストは実測 **0.030 ms/回**。

| | 下限なし | 下限あり |
|---|---:|---:|
| 索引（小、8 スレッド） | 2.9 ms | **0.9 ms** |
| 分裂・扇（小、8 スレッド） | 3.8 ms | **2.8 ms** |
| $k_e$（小） | 1.03 | **1.11** |
| 全体（小、8 スレッド） | 1.39 | **1.41** |
| 全体（sphere2、16） | 5.50 | **5.52** |

**規模コーパスは変わりません**（項目数が下限を大きく上回るため）。

### 規模コーパスの段ごとの項目数

| 段の項目 | 小さいコーパス（中央 / 最大） | 規模コーパス（最小 / 最大） |
|---|---|---|
| 中核: 葉 | 113 / 253 | 1240 / 2598 |
| 索引: 平面 | 8 / 15 | 2256 / 5972 |
| 三角形化: 多角形 | 72 / 270 | 25863 / 31688 |
| 扇: 頂点 | 65 / 254 | 23349 / 31722 |

---

## Phase 5 §6 の測定値（CP1.9 到達時点。2026-09-03〜04）

### 測定の条件

**性能の測定は、この条件を明記しないと再現できません**（`PERF.md` §3）。

| 項目 | 値 |
|---|---|
| 日付 | 2026-09-03（錨）/ 2026-09-04（CP2・self-union） |
| CPU | AMD Ryzen 7 9800X3D（**物理 8 コア / 論理 16**） |
| スレッド | 16 |
| `KRISITE_COORD_BITS` | 21 |
| 算術の検査 | **OFF** |
| §5.5 の検算（位相の照合を 2 回） | **常に ON**（無効にする旗がありません） |
| 適応分割 / 深度 | ON / 6 |
| 演算 | $\cup$ $\cap$ $\setminus$ の 3 つ + 三角形への変換 |
| 反復 | 3 回。**最小値** |
| **機械の状態** | **温まった定常状態**（5 分のウォームアップ後） |
| **ドリフト検査** | **先頭の対を最後にもう一度測り、差 0.6%**（判定 3%） |

> **「冷えた 1 回目」は採りません。** 最速値であって代表値ではないためです。
> CP2 と CP3 は何時間も連続で回るので、**予測したい状態は温まった状態**です。

### 錨 6 点（Thingi10K の実データ）

**規模の幅は 23.4 倍**（CP1 の最小と最大）。

| 対 | 入力の三角形数 | **3 演算の合計（秒）** | 出力の多角形数 |
|---|---:|---:|---:|
| `73464x69883` | 2,010 | **0.930** | 50,906 |
| `196190x1129073` | 3,886 | **1.513** | 78,933 |
| `40117x140511` | 7,620 | **12.773** | 415,282 |
| `100815x44740` | 12,200 | **2.715** | 95,509 |
| `67497x1087135` | 27,380 | **22.296** | 559,867 |
| `112546x1396888` | 46,980 | **57.065** | 1,224,468 |

**★ 入力の三角形数に対して時間は単調ではありません。** 7,620 が 12.8 秒、
12,200 が 2.7 秒です。**入力の三角形数は説明変数として機能しません**（`PERF.md` §5.4.9）。

### 段別の内訳（arrange の中）

**葉の粒度で計測**（断片ごとに時計を読むと、断片が数百万個ある実データで計時が支配します）。
**スレッドをまたいで合算するので CPU 時間**であり、見るのは割合です。

| 対 | 多角形の収集 | 存在の判定 | 準備 | **断片の生成** | **共平面の突き合わせ** |
|---|---:|---:|---:|---:|---:|
| 小（2,010） | 18.9% | 2.1% | 9.6% | **65.8%** | 3.6% |
| 基準（7,620） | 0.4% | 0.5% | 2.6% | **91.4%** | 5.2% |
| 大（46,980） | 0.2% | 0.3% | 1.1% | **97.4%** | 1.0% |

**小さい対では葉あたりの線形費用（収集・存在・準備で 30.6%）が効き、
大きい対では消えます**（1.6%）。

### 説明変数の候補と、係数の幅

**同じ 6 点で、段ごとに候補を当てた結果です**（`PERF.md` §5.4.9）。

| 段 | 最良の候補 | 係数の幅 | **leave-one-out の予測誤差（中央値）** |
|---|---|---:|---:|
| arrange | 局所 BSP の切断数 | 1.70 倍 | **±16%** |
| 領域の分類 | 出力の多角形数 | 3.99 倍 | ±28% |
| 縫合 | 出力の多角形数 | 1.43 倍 | **±11%** |

**段によって説明変数が違います。** 領域の分類では、arrange で最良だった候補が ±69% と
悪くなります。**単一の全体式では、この違いが潰れます。**

> **どれも出力量なので、走らせる前の予測には使えません。**
> 入力の三角形数から辿ると、出力の多角形数との比が **6.96 倍**動きます。

### 実データの規模別の実測（CP1 / CP2）

| 量 | CP1（449 対） | CP2（78 対） |
|---|---:|---:|
| 1 対あたりの中央値 | 38.9 s | 48.9 s |
| 1 対あたりの 95 パーセンタイル | 553.7 s | **2,291.9 s** |
| 最大 | 14,578.3 s | 3,226.2 s |
| 合計 | 22.7 時間 | 9.2 時間 |
| **上位 5 対の占有** | **37.9%** | **41.5%** |

**CP1 は自己交差なしを宣言せずに測り、CP2 は検査つきで宣言しています。**

**★ 裾が支配します。** 上位 5 対が全体の 4 割を占めるので、
**平均や中央値では実態を隠します。目標は分位点で立ててください。**

### 自己交差なしの宣言（性能に直結します）

| 母集団 | 模型数 | **宣言できた割合** |
|---|---:|---:|
| CP1（自己交差なしで選んだ 1,000 模型） | 1,000 | **94.1%** |
| CP2（自己交差ありで選んだ 200 模型） | 200 | **12.5%** |

**宣言できると、同一の source しか居ない領域で切断を省けます。**
同一実行の A/B（20 対）で **中央 4.67 倍**、両側とも宣言できた 17 対では **6.41 倍**でした。

**検査の費用は 0.09 秒/模型**です。**演算を 1 回も必要としません。**

### メモリ

| 量 | 値 |
|---|---:|
| 出力の多角形 1 個あたり（構成点のメモ化あり） | **約 5,000 バイト** |
| 同（メモ化なし） | 約 2,200 バイト |

**★ この式は 4 倍程度外します。** 入力の三角形数から多角形数を予測できないためです。
実際、入力 56,678 三角形の対が 30 GB を超えて停止する一方、
97,720 三角形の対は 18 GB で完了しています。

### リークの有無

**同じ演算を 40 回繰り返して、解放されていないバイト数の増加は反復 20 以降ゼロ**でした
（総増分 0.013 MB）。**常駐メモリではなく解放されていないバイト数を見ています**
（確保器が解放済み領域を OS に返さないため）。

---

## 次に測るべきもの

Phase 1 の完了条件には含まれませんが、Phase 2 の設計判断に必要になります。

- **断片の構成点を保持したときの `intersect3` の削減幅**（§12 の比率から、
  ここが最も効くはず。`IMPL-phase1.md` §5.6）
- 混合幅 `det3` にしたときの `intersect3` の改善幅（単価のほう）
- **実際の時間**。Phase 1 は回数しか測っていません
- `side()` の分岐予測が外れる条件（構成点が平面上に載る割合が高い実データ）
- キャッシュミスを含む現実的なアクセスパターン（プール巡回ではなくランダム参照）

## 正準な代表点（`SPEC-phase4.md` §4.4）

| | |
|---|---:|
| 並べ替えても頂点列が同じ | **264 / 264 構成** |
| 代表が入れ替わった回数 | **2410** |
| 値で併合した回数 | 10131 |

**Phase 3（`de1f2f5`）との突き合わせ**（264 構成、生バイトを `h_equal` で比較）:

| | バイト一致 | 幾何は同じ（表現が違う） | 幾何が違う |
|---|---:|---:|---:|
| 正準化の前 | 236 | 28 | **0** |
| 正準化の後 | 26 | 238 | **0** |

**幾何は常に 264 / 264 一致します。**

## §7.3 の検査とコア数

`entries8 > entries1`（キャッシュがスレッド局所であること）を確認できた試行回数:

| コア数 | 何回目 |
|---|---|
| 16 | 1 回目 |
| 4（GitHub Actions の標準ランナー） | 1 回目 |
| 2 | 1 回目 |
| 1 | 2 〜 16 回目 |

**上限は 32 回**（1 回約 2 ms）。

---

## self-union と出口のホットスポット（2026-09-04〜05）

**機械**: Ryzen 7 9800X3D（物理 8 コア）、8 スレッド、機械は空。
**演算**: `boolean(A, A, Union)`（self-union）、適応分割、局所 BSP・early-out・
接触の分裂すべて有効。**指示関数は新定義（$w > 0$）。**

> **絶対時間には ±30% のばらつきがあります**（`78535` を 3 回測って 8.22 / 11.34 / 11.00 秒）。
> **深度 6 → 7 → 8 の比較は、同じ順序・同じ機械で連続して測った**ので、ばらつきの外です。

### 出力の膨張（深度 6）

| 模型 | 入力三角形 | 出力三角形 | **膨張** | ブール演算 | 三角形化 | 合計 |
|---|---:|---:|---:|---:|---:|---:|
| `425318` | 8,352 | 219,786 | **26.3 倍** | 1.6 s | 3.8 s | 5.4 s |
| `78535` | 10,218 | 58,976 | 5.8 倍 | 8.2 s | 0.3 s | 8.5 s |
| `69268` | 30,320 | **7,656,768** | **252.5 倍** | 103 s | **303 s** | **406 s** |

**膨張の内訳**（積が実測と一致）:

| 模型（深度 6） | 八分木の切断 | **局所 BSP** | 扇 | 積 |
|---|---:|---:|---:|---:|
| `425318` | **4.96 倍** | 2.60 倍 | 2.08 倍 | 26.8 |
| `69268` | 4.14 倍 | **29.4 倍** | 2.07 倍 | **252** |

**葉が混むほど BSP の項が効きます。**「主因は八分木」は `425318` でしか成り立ちません。

### 深度を振る（合計時間 = ブール演算 + 三角形化）

| 模型 | 入力三角形 | 深度 6 | 深度 7 | 深度 8 |
|---|---:|---:|---:|---:|
| `425318` | 8,352 | 5.4 s | **3.8 s** | 8.8 s |
| `78535` | 10,218 | 11.6 s | **5.9 s** | 8.4 s |
| `69268` | 30,320 | 406 s | 244 s | **148 s** |

**深度 7 は 3 模型すべてで深度 6 より速い**（1.4 / 2.0 / 1.7 倍）。

**★ 深くすると出力三角形が【減ります】**（`69268`）: 7,656,768 → 4,302,896 → 2,744,310。

### 段ごとの壁時計（`69268`）

| 段 | 深度 6 | 深度 7 | 深度 8 | 説明変数の候補 | 追随するか |
|---|---:|---:|---:|---|---|
| 前処理 | 0.07 | 0.11 | 0.10 | — | — |
| 葉の列挙（**逐次**） | 0.04 | 0.18 | 1.04 | セル数 | **する** |
| 多角形の収集 + 存在判定 | 0.04 | 0.35 | 2.06 | 葉 × 多角形 | **する** |
| 切断集合の準備 | 0.05 | 0.60 | 3.07 | 割り当て件数 | **超線形** |
| **断片の生成（局所 BSP）** | 24.9 | 9.2 | 5.1 | $\sum_\ell P_\ell^2$ | **逆に動く** |
| 共平面の突き合わせ | 26.1 | 9.3 | 3.3 | 断片数 | **速く落ちる** |
| 縫合（**逐次**） | 14.8 | 8.7 | 4.3 | 構成点数 | **合わない** |
| 分類（レイキャスト） | 32.4 | 22.3 | 13.7 | レイ × 三角形 | **する** |
| **三角形化** | **303** | **190** | **113** | 頂点数 | **する** |
| **合計** | **406** | **244** | **148** | — | — |

> **arrange の内訳は CPU 時間で採っています。** 上の表は並列効率で割って
> 壁時計に直した近似です。**CPU 時間と壁時計を混ぜないこと。**

### ★ $\sum_\ell P_\ell^2$ は説明変数ではありません（再否定）

| `69268` | 深度 6 | 深度 7 | 深度 8 |
|---|---:|---:|---:|
| $\sum_\ell P_\ell^2$ | 116,180,204 | 153,209,368 | **639,297,768** |
| 局所 BSP の時間 | 24.9 s | 9.2 s | **5.1 s** |

**5.5 倍に増えて、時間は 5 分の 1。** `IMPL-phase5.md` §41 で一度撤回した候補が、
**深度を振ると再び否定されました。** 追随するのは**断片数**のほうです。

### ★ 並列効率も深度で動きます

| `69268` | 深度 6 | 深度 7 | 深度 8 |
|---|---:|---:|---:|
| arrange の CPU 時間 / 壁時計 | **4.5** | 5.9 | **7.7** |
| 最大の葉の多角形数 | 3,714 | 1,844 | 974 |

**8 スレッドのうち深度 6 では 4.5 本しか働いていません。**
**巨大な葉が 1 つあると他が待ちます。** 深度を上げたときの利得のうち
**およそ 1.7 倍分はこの負荷の平準化**です。

### ★★ 出口（`to_mesh`）の内訳 — **「88%」の記録はここです**

> **`include/krisite/csg/tjunction.hpp` のコメントが「`BENCH.md` で出口の 88%」と
> 引いていましたが、`BENCH.md` にその記録はありませんでした。参照が空振りでした。**
> **以下がその実測です**（2026-09-05 に初めて採取）。

| 模型 / 深度 | 多角形 | 合計 | 構成点 | 値で併合 | **平面索引** | **扇（T解決+三角形化）** | 接触の分裂 |
|---|---:|---:|---:|---:|---:|---:|---:|
| `425318` / 6 | 105,666 | 2.23 s | 0.09 | 0.09 | **1.42 s（64%）** | **0.08 s（3.6%）** | 0.53 |
| `69268` / 8 | 1,340,900 | 134.42 s | 1.96 | 1.53 | **110.50 s（82%）** | **5.23 s（3.9%）** | 14.88 |

**`69268` では全体 148 秒のうち 110.5 秒（75%）を、この 1 段が占めます。**

**平面ごとに、全頂点について厳密な `side` を評価します。**
深度 8 で **頂点 1,372,133 × 平面 約 1.3 万枚 = およそ $1.8 \times 10^{10}$ 回。**
**並列化済み**（平面ごとに独立）。それでもこの時間です。

**扇三角形分割は 4%。** 「2.08 倍」は**個数の比**であって費用ではありません。

### self-union で無効になる機構

| 機構 | 発火 |
|---|---:|
| early-out（相手が居ないセルを飛ばす） | **0 セル** |
| 単一 source の葉の検出 | **0 葉** |
| NSI の宣言 | **使えない** |

$A \cup A$ は 2 入力が完全に重なるので「相手が居ない場所」がありません。
**CP2 の 2 項演算ではこれらが中央値 4.67 倍の高速化を出していました。**
**さらに仕事がちょうど 2 倍**です（分類のレイキャストも 2 倍）。

### セルへの割り当ての過剰

| `69268` / 深度 8 | 件数 |
|---|---:|
| 葉への割り当て | 5,976,209 |
| 生成された断片（正準前） | 2,681,838 |

**割り当ての半分以上がクリップすると空です**（BSP が断片を増やす分を考えると、
空の割合はさらに高い）。**割り当ては三角形の AABB とセルの重なりだけで判定しています。**

### ★★ レイキャストの索引（`ray_index`）は効いています（2026-09-05）

**旗を OFF にして直接測りました**（深度 6、8 スレッド、self-union）。

| 模型 | `ray_index` | 1 レイあたりの三角形 | 分類の時間 |
|---|---|---:|---:|
| `425318`（8,352 三角形） | **ON（既定）** | **210.3** | **0.40 s** |
| | OFF | 8,352.0（= 全数） | 10.65 s |
| `78535`（10,218 三角形） | **ON（既定）** | **733.7** | **3.08 s** |
| | OFF | 10,218.0（= 全数） | 40.76 s |

**枝刈り 40 倍 / 14 倍。分類は 27 倍 / 13 倍 速くなっています。**
**出力多角形数は ON / OFF で同一**（105,666 と 27,193）。**厳密な絞り込みです。**

> **`boolean.hpp` の `ray_tri_tests` のコメントが「枝刈りなし」のままでした**（索引導入前の記述）。
> **実装側がそれを確かめずに引用し、「分類は全三角形を走査する」と報告しました。**
> **機構を足したら、それを説明しているコメントも直すこと。**

**残る問い**: `78535` は `425318` の 3.5 倍の候補を返します（733.7 対 210.3）。
**大きい三角形が階層の粗い段に入っている可能性。未調査。**

### ★★ T 字接合の実測（2026-09-05）— **「固定深度では 0」は誤り**

`425318`（8,352 三角形）の self-union。**「適応」と表示していますが、self-union では
分割の判定 `(両方の source が居る) && (数 > 閾値)` が常に真なので、実質は固定深度です。**

| 深度 | 葉 | **葉あたり支持平面** | 多角形 | 頂点 | 走査した辺 | **挿入した T 頂点** | **率** | **候補/辺** | 候補の総数 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 2 | 8 | 1130.9 | 214,645 | 231,467 | 835,742 | **56,442** | 6.75% | 270.2 | 225,820,587 |
| 3 | 17 | 565.1 | 190,960 | 205,262 | 741,002 | **51,402** | 6.94% | 218.0 | 161,546,579 |
| 4 | 70 | 173.6 | 154,269 | 165,135 | 594,238 | **44,530** | 7.49% | 159.7 | 94,887,991 |
| 5 | 381 | 49.0 | 133,302 | 141,641 | 510,370 | **39,476** | 7.73% | 125.6 | 64,120,757 |
| 6 | 2,225 | 18.0 | 105,666 | 109,913 | 399,826 | **31,292** | 7.83% | 82.5 | 32,968,241 |
| 7 | 14,745 | 9.4 | 102,786 | 104,183 | 388,306 | **25,592** | 6.59% | 64.2 | 24,930,754 |
| 8 | 103,536 | 6.6 | 136,355 | 135,197 | 522,580 | **20,484** | 3.92% | 93.2 | 48,713,822 |

**深度 6、4 通りの対照（適応/固定 × 絞り込み ON/OFF）は、すべて完全に同一でした。**

| 判定 | 内容 |
|---|---|
| **固定深度で `inserted` = 0** | **成り立ちません**（7.83%） |
| **絞り込み（`cull_planes`）が原因** | **違います。**ON / OFF で 1 つも変わりません（絞り込みは厳密で、セルを横切らない平面しか外さない） |
| **セル境界が原因** | **違います。深度を上げると T 頂点は【減ります】**（56,442 → 20,484） |
| **機構** | **未特定** |

**`78535`（深度 6）**: 走査した辺 105,498、挿入 7,864（**7.45%**）、候補/辺 **187.6**、
葉あたり支持平面 **37.6**。**候補/辺が `425318` の 2.3 倍なのは、
葉あたりの支持平面数が 2.1 倍だからで、説明が付きます。**

### 索引の無駄

| 模型 / 深度 | 候補（辺あたり） | 挿入（辺あたり） | **無駄** |
|---|---:|---:|---:|
| `425318` / 6 | 82.5 | 0.078 | **1,050 倍** |
| `78535` / 6 | 187.6 | 0.075 | **2,520 倍** |

### ★★ A-3（空間バケット）の削減率 — 実測（2026-09-05）

**比較する 2 つの量**（どちらも厳密な `side` の呼び出し回数）:

```
現状   |相異なる支持平面| × |全頂点|
A-3    Σ_ℓ |支持平面_ℓ| × |葉の【閉じた箱】に入る頂点_ℓ|
```

**「全頂点 ÷ 葉」ではありません。** 頂点は隣り合う葉に共有されるので、
**箱の中かどうかを `plane_axis_aligned` + `side` で厳密に判定**しています（新しい述語は不要）。

| 模型 | 深度 | 出力のセル | 相異なる支持平面 | 頂点 | **現状** | 箱に入る頂点の延べ | 共有の倍率 | **A-3** | **削減** |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `425318` | 4 | 58 | 8,154 | 165,135 | 1.35e+09 | 175,115 | 1.06 | 7.14e+07 | **19 倍** |
| `425318` | 5 | 252 | 8,154 | 141,641 | 1.15e+09 | 158,142 | 1.12 | 3.54e+07 | **33 倍** |
| `425318` | 6 | 1,147 | 8,154 | 109,913 | 8.96e+08 | 138,710 | 1.26 | 1.19e+07 | **76 倍** |
| `425318` | 7 | 5,054 | 8,154 | 104,183 | 8.50e+08 | 163,398 | 1.57 | 4.70e+06 | **181 倍** |
| `425318` | 8 | 21,495 | 8,154 | 135,197 | 1.10e+09 | 282,153 | 2.09 | 2.82e+06 | **391 倍** |
| `78535` | 4 | 11 | 931 | 51,699 | 4.81e+07 | 53,232 | 1.03 | 1.42e+07 | **3 倍** |
| `78535` | 6 | 37 | 931 | 29,533 | 2.75e+07 | 33,617 | 1.14 | 3.12e+06 | **9 倍** |
| `78535` | 8 | 216 | 931 | 25,377 | 2.36e+07 | 37,345 | 1.47 | 1.47e+06 | **16 倍** |
| **`69268`** | **7** | **3,159** | **30,116** | **2,151,426** | **6.48e+10** | 2,300,652 | 1.07 | **6.74e+08** | **96 倍** |
| **`69268`** | **8** | **14,633** | **30,116** | **1,372,133** | **4.13e+10** | 1,647,264 | 1.20 | **2.36e+08** | **175 倍** |

**削減率は出力が占めるセルの数で決まります。**
`78535` はセルが 11 → 216 （19.6 倍）しか増えず、`425318` は 58 → 21,495（371 倍）。
**`78535` の出力は小さく密集しています**（相異なる支持平面が 931 枚）。

**検査**: `Poly::aabb` が本当にセルの箱か → **全数が箱**（全 12 通りで「セルでない 0」）。

### ★ 格子の跡は深度で急増します（副産物）

| 深度（`425318`） | 4 | 5 | 6 | 7 | **8** |
|---|---:|---:|---:|---:|---:|
| **格子面に載る頂点** | 5.9% | 11.1% | 23.4% | 45.2% | **72.8%** |
| 共有の倍率 | 1.06 | 1.12 | 1.26 | 1.57 | **2.09** |

**深度 8 では出力頂点の 73% がセルの面の上にあります。演算の結果とは無関係な格子の跡です。**
**深度 8 で `425318` の頂点数が増える**（104,183 → 135,197）**のはこれが原因**で、
**深度 7 が最適点だったことと整合します。**

### A-3 を入れたときの見込み（`69268`、深度 8）

| 段 | いま | A-3 の後 |
|---|---:|---:|
| 中核（`boolean`） | 35.23 s | 35.23 s |
| 構成点 / 値で併合 | 3.49 s | 3.49 s |
| **平面索引** | **110.50 s** | **0.63 s**（175 分の 1） |
| 扇（T 解決 + 三角形化） | 5.23 s | 5.23 s |
| 接触の分裂 | 14.88 s | 14.88 s |
| **合計** | **169.7 s** | **59.5 s** |

**全体で 2.85 倍。** そして**深度 6（406 秒）から見れば 6.8 倍**です。

> **A-3 の後は、出口の最大項が「接触の分裂」（14.88 s、新しい出口の 61%）になります。**

### ★★ A-3 実装後の実測（2026-09-05）

**旗 `ToMeshOptions::cell_index`（既定 ON）。self-union、8 スレッド。**

#### 平面索引の時間

| 模型 | 深度 | **OFF（従来）** | **ON（A-3）** | 削減 |
|---|---:|---:|---:|---:|
| `425318` | 6 | 1.406 s | **0.042 s** | **33 倍** |
| `78535` | 6 | 0.042 s | **0.010 s** | 4.2 倍 |
| **`69268`** | **8** | **110.50 s** | **0.779 s** | **142 倍** |

**演算回数の予測は 76 / 9 / 175 倍でした。** 差は **A-3 が新しく払う費用**
（頂点を葉に配る二分探索、$V \times 3 \times D$ 回の `side`）です。**予測に入れていませんでした。**

#### 深度の振り直し（A-3 の後）

| 模型 | 深度 4 | 深度 5 | 深度 6 | 深度 7 | 深度 8 |
|---|---:|---:|---:|---:|---:|
| `425318` 合計 | 5.16 | **2.71** | 3.02 | 3.36 | 7.04 |
| `78535` 合計 | 33.50 | 18.46 | 8.78 | **4.97** | 5.21 |
| **`69268`** 合計 | — | — | 265.55 | 59.41 | **37.65** |

**平面索引が深度によらずほぼ一定になりました**（`425318` で 0.072〜0.096 s）。
**A-3 の前は深度で 10 倍以上動いていた項**です。

**`425318` の深度 5 / 6 / 7 は 2.71 / 3.02 / 3.36 で、差が 24%。ばらつきの中で区別できません。**
**言えるのは「深度 4 と 8 は明確に悪い」「深度 5〜7 が平坦になった」の 2 つです。**
**平坦になったこと自体が、A-3 が「深くするほど得」という項を消した証拠です。**

### ★★ 絶対時間のばらつきが、これまでの想定より大きい

**`69268` の中核（`boolean`）は A-3 の影響を受けません。同じ入力・同じ設定です。**

| 深度 | 別々の実行での中核の時間 | 比 |
|---:|---|---:|
| 6 | 103.16 s / **180.82 s** | **1.75 倍** |
| 7 | 53.91 s / 32.39 s | 1.66 倍 |
| 8 | 35.23 s / 22.58 s | 1.56 倍 |

**±30% と書いてきましたが、この規模では 1.6〜1.75 倍動きます。**

> **したがって「406 → 244 → 148 秒」（A-3 前の深度比較）は、
> 別々の実行をまたいだ比較なので、係数として使えません。**
> **順序（深くするほど速い）は 2 つの独立な実行で一致しているので、そちらは残ります。**

**A-3 の効果を見るときは、同一実行の中で ON / OFF を比べた値**
（平面索引 1.406 → 0.042 など）**を使ってください。**

#### 中核を揃えて比べた A-3 の効果（`69268`、深度 8）

| | A-3 前 | A-3 後 |
|---|---:|---:|
| 三角形化 | 134.42 s | **15.07 s（8.9 倍）** |
| 中核（**同じ値に揃える**） | 35.23 s | 35.23 s |
| **合計** | **169.7 s** | **50.3 s（3.4 倍）** |

### ★★ ばらつきの切り分け（2026-09-05）— **ばらつきは「時間帯」にあります**

**同じ模型・同じ設定で、3 つの水準に分けて測りました。**

#### 1. 実行の中（同一プロセスで繰り返す）

| 対象 | 反復 | 最大/最小 | 構造の数字 |
|---|---:|---:|---|
| `425318` 深度 6・8 スレッド | 5 | **1.086** | **5 回とも完全に同一** |
| `425318` 深度 6・**1 スレッド** | 5 | **1.076** | **同一** |
| `69268` 深度 8・8 スレッド | 5 | **1.064** | **同一** |
| `78535` 深度 6・8 スレッド | 3 | **1.062** | **同一** |

**多角形・断片・葉・頂点は 18 回すべてで 1 ビットも動きません。決定性は健全です。**

**1 スレッドでも 1.076。並列は原因ではありません。**
**CPU 時間と壁時計は同じ倍率で動きます**（比 5.04〜5.25 で安定）。**競合でもありません。**

#### 2. プロセスの間（同じバイナリ、同じ時間帯）

`425318` 深度 6 を、**別プロセスで 6 回**（各 3 反復、最小値）:

```
0.99 / 0.98 / 1.00 / 0.97 / 0.98 / 0.99   → 最大/最小 1.031
```

**プロセスをまたいでも、同じ時間帯なら 1.03 です。**

#### 3. ★★ 時間帯の間（数時間あけて、**同じバイナリ**を回し直す）

| 対象 | **当時の記録** | **いま測り直した値** | 比 |
|---|---:|---:|---:|
| `425318` 深度 6 中核（`why3`） | 1.59 s | **1.06 s** | **1.50** |
| `425318` 深度 6 中核（`redepth`） | 1.66 s | **1.05 s** | **1.58** |
| `78535` 深度 6 中核 | 8.22 / 11.34 / 11.00 s | **6.86〜7.28 s** | **1.20〜1.65** |
| `69268` 深度 8 中核 | 35.23 s | **22.6〜24.1 s** | **1.46〜1.56** |

**バイナリは再コンパイルしていません。当時のものをそのまま回しています。**
**コードは同一で、機械のほうが当時は遅かった。**

### ★ 運用の規則（これに従ってください）

| 規則 | 根拠 |
|---|---|
| **効果は、まず【演算回数】で測る** | 完全に再現する。**ばらつきの影響を受けない** |
| **時間は最後に 1 回、「目に見えて速いか」の確認にだけ使う** | 何回回しても確度は上がらない |


> **★ 時間を測るために反復してはいけません。**
> **ホストの状態が見えない以上、何回回しても確度は上がりません。**
> **上の「3 反復」は原因を切り分ける診断のための 1 回限りの手段で、常用する手順ではありません。**
>
> **そして「ばらつきの範囲内で少し速くなった」は改善ではありません。**
> **どんな状況で計算しても目に見えて分かる差が出て、初めて改善です。**

> **`CLAUDE.md`「絶対時間は汚染されている前提で扱ってください」は正しかったのですが、
> 想定していた幅（±30%）が足りませんでした。実測は 1.5〜1.6 倍です。**
>
> **原因は特定できません。** サンドボックスの `/proc/loadavg` はホストの負荷を見ないので、
> **外の状態を観測する手段がありません。** **推測しません。**

### この文書のこれまでの絶対秒について

**§「self-union と出口のホットスポット」以降の絶対秒は、採った時間帯が混ざっています。**
**係数として使わないでください。**

**比・順序・同一実行の A / B は有効です。**
**深度の順序（深くするほど速い）は 2 つの独立な実行で一致しているので残ります。**

### ★★ 深度 9 の測定（2026-09-05）— **恩恵 2 の検証**

**`69268`（30,320 三角形）、8 スレッド、A-3 あり。同一実行の中で深度 7 / 8 / 9。**

#### 構造（**演算回数。ばらつきに依りません**）

| 深度 | 葉 | 葉あたり多角形 | **割り当て件数** | **$\sum_\ell P_\ell^2$** | **断片(正準前)** | 出力多角形 | **頂点** | レイ×三角形 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 7 | 14,598 | 70.3 | 1,026,072 | 153,209,368 | 4,164,774 | 2,082,359 | 2,151,426 | 2.56e9 |
| 8 | 102,684 | 58.2 | 5,973,336 | 639,297,768 | 2,681,838 | 1,340,900 | 1,372,133 | 1.70e9 |
| **9** | **775,293** | **53.4** | **41,382,996** | **4,070,178,956** | 2,182,996 | 1,091,479 | 1,100,830 | 1.34e9 |

#### 段ごとの壁時計（秒）

| 深度 | 葉の列挙（逐次） | arrange | 縫合（逐次） | 分類 | 三角形化 | **合計** | 並列効率 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 7 | 0.16 | 13.46 | 5.66 | 13.26 | 29.56 | 62.09 | 6.00 |
| **8** | 0.91 | 9.58 | 3.19 | 8.15 | 15.22 | **37.04** | 7.82 |
| 9 | **5.89** | **37.18** | 2.73 | 6.52 | 11.33 | 63.64 | 7.83 |

#### arrange の内訳（CPU 秒）

| 深度 | 収集 | 存在判定 | **準備** | **断片生成** | 共平面 |
|---:|---:|---:|---:|---:|---:|
| 7 | 0.98 | 0.68 | 2.23 | 36.50 | 40.29 |
| 8 | 6.71 | 4.73 | 15.96 | 27.62 | 19.86 |
| **9** | **46.44** | **34.00** | **116.32** | **83.13** | 11.27 |

### ★★ 段を「何に比例するか」で 2 群に分けると、はっきり分かれます

**壁時計に直して（arrange の内訳は並列効率で割る）、深度 8 → 9 の変化:**

| 群 | 段 | 深度 8 | 深度 9 | 比 |
|---|---|---:|---:|---:|
| **割り当て件数に比例** | 葉の列挙 + 収集 + 存在判定 + **準備** + **断片生成** | **7.94 s** | **41.64 s** | **5.2 倍** |
| **出力の大きさに比例** | 共平面 + 縫合 + 分類 + 三角形化 | **29.10 s** | **22.02 s** | **0.76 倍** |
| 合計 | | 37.04 s | 63.64 s | 1.72 倍 |

**割り当て件数が 5,973,336 → 41,382,996（6.9 倍）に増え、第 1 群がそれに追随しています。**

### ★★ Morton を入れても、深度 9 は深度 8 より遅い

**Morton が消せるのは $O(\text{セル} \times \text{多角形})$ の部分**
（葉の列挙・収集・存在判定）**だけです。**

**「準備」と「断片生成」は $O(\text{割り当て件数})$ の実際の仕事で、Morton では減りません。**

| 深度 9 | 秒 |
|---|---:|
| Morton が消せる分（葉の列挙 + 収集 + 存在判定） | 16.16 |
| **残り**（準備 + 断片生成 + 共平面 + 縫合 + 分類 + 三角形化） | **47.49** |
| **深度 8 の合計**（比較対象） | **37.04** |

**Morton が完璧に効いても、深度 9 は 47.5 秒で、深度 8 の 37.0 秒より遅い。**

### ★ 本当の制約は「過剰な割り当て」です

| 深度 | 割り当て件数 | 断片（正準前） | **1 断片あたりの割り当て** |
|---:|---:|---:|---:|
| 7 | 1,026,072 | 4,164,774 | 0.25 |
| 8 | 5,973,336 | 2,681,838 | 2.2 |
| **9** | **41,382,996** | 2,182,996 | **19.0** |

**深度 9 では、割り当ての 95% が何も生みません**（クリップすると空）。

**セルへの割り当ては三角形の AABB とセルの重なりだけで判定しています。**
**斜めの三角形の AABB は、セルが細かくなるほど過剰になります。**

> **深度を上げる障壁は「アドレスの付け方」ではなく「過剰な割り当て」でした。**
> **効くのは §5.9（厳密な三角形-箱交差判定、分離軸定理）です。**

### ★★ §5.9（厳密な三角形-箱交差判定）の効果の見積もり（2026-09-05）

**`69268`（30,320 三角形）。演算回数なので、時間のばらつきに依りません。**

| 深度 | **いまの割り当て（AABB）** | **厳密な判定の後** | **消える割合** | 削減 |
|---:|---:|---:|---:|---:|
| 6 | 250,886 | 107,706 | 57.1% | 2.33 倍 |
| 7 | 1,026,028 | 160,150 | 84.4% | 6.41 倍 |
| **8** | **5,973,256** | **275,064** | **95.4%** | **21.72 倍** |
| **9** | **41,382,534** | **552,172** | **98.7%** | **74.95 倍** |

#### 測定器の検査

**パイプラインが記録した割り当て件数と突き合わせました。**

| 深度 | パイプライン | この道具 | 差 |
|---:|---:|---:|---:|
| 7 | 1,026,072 | 1,026,028 | 44（0.004%） |
| 8 | 5,973,336 | 5,973,256 | 80（0.001%） |
| 9 | 41,382,996 | 41,382,534 | 462（0.001%） |

**同じものを数えています。**

#### ★★ 最も重要な観測 — **厳密な判定では、割り当てが深度で 2 倍ずつしか増えません**

| 深度 | 6 → 7 | 7 → 8 | 8 → 9 |
|---|---:|---:|---:|
| **AABB** | ×4.1 | ×5.8 | **×6.9** |
| **厳密** | ×1.5 | ×1.7 | **×2.0** |

**曲面は解像度 $R$ で $O(R^2)$ 個のセルとしか交わりません。**
**AABB は $O(R^3)$ に近い増え方をします。** これが深度を上げる障壁の正体です。

**厳密な判定の後の深度 9 の割り当て（552,172）は、
いまの深度 8 の割り当て（5,973,256）の 9.2% です。**

### ★★ §5.9 の費用と、絞り込みの段階（2026-09-05）

**算術は `fixed_int`（本実装と同じ）。1 スレッド。`69268`。**

#### 絞り込みの効き

| 深度 | AABB（いま） | **+ 平面 対 箱** | **+ 分離軸 13 軸** | 平面だけで消える | 13 軸で消える | **平面 / 13 軸** |
|---:|---:|---:|---:|---:|---:|---:|
| 6 | 250,886 | 156,032 | 107,706 | 37.8% | 57.1% | 66% |
| 7 | 1,026,028 | 340,942 | 160,150 | 66.8% | 84.4% | 79% |
| **8** | **5,973,256** | **958,750** | **275,064** | **83.9%** | **95.4%** | **88%** |

**「平面 対 箱」は三角形の支持平面とセルの交差判定で、`plane_crosses_box` として
既に実装され、GMP 差分テストもあります**（`kPlaneAabb = 3b+7`）。**新しい述語はゼロです。**

**深度 8 で、平面 1 本だけで 13 軸の 88% の効果が得られます。**

#### 1 回あたりの費用（ns）

| 深度 | **平面 対 箱** | **分離軸（早期打ち切りあり）** | 分離軸（全軸。上界） |
|---:|---:|---:|---:|
| 6 | 5.5 | 26.0 | 29.8 |
| 7 | 4.4 | 16.8 | 29.2 |
| **8** | **3.3** | **14.0** | **29.1** |

**深くするほど安くなります。** 早期に分離する対が増えるためです。
**全軸を評価した上界は 29 ns で一定**（早期打ち切りが無いので当然）。

#### ★ 判定の費用は、削減する仕事に対して無視できます

| 深度 8 | |
|---|---:|
| 判定の総費用（分離軸、1 スレッド） | **83.6 ms** |
| 判定の総費用（平面のみ、1 スレッド） | **19.7 ms** |
| 削減される「割り当て件数に比例する群」 | **7.94 s** |

**判定は削減対象の 1% です。**（**見積もりの最大の不確かさが解けました。**
「1 µs なら利得が消える」を心配しましたが、実測 14 ns です。）

#### 測定器の相互検査

**`__int128` で書いた道具と `fixed_int` で書いた道具が、
交わる件数について完全に一致しました**（107,706 / 160,150 / 275,064）。

**独立に実装した 2 つの算術が同じ答えを出しています。**

### ★★ 10-a（割り当てを支持平面で絞る）の実測（2026-09-05）

**`69268`、8 スレッド、A-3 あり。同一実行の中で旗の ON / OFF。**

#### 構造（**演算回数。2 つの実行で完全に再現**）

| 深度 | 旗 | 断片(正準前) | **$\sum_\ell P_\ell^2$** | 仕事をした葉 | **落とした割り当て** |
|---:|---|---:|---:|---:|---:|
| 8 | **ON** | 2,681,838 | **71,227,676** | 34,126 | **5,014,506** |
| 8 | OFF | 2,681,838 | 639,297,768 | 102,684 | 0 |
| 9 | **ON** | 2,182,996 | **161,366,292** | 175,556 | **38,198,232** |
| 9 | OFF | 2,182,996 | 4,070,178,956 | 775,293 | 0 |

**$\sum_\ell P_\ell^2$ が深度 8 で 9 倍、深度 9 で 25 倍減ります。**
**出力（断片数・ハッシュ）は完全に同一。葉ごとの切断平面も一致。**

#### 時間（**同一実行の中の ON / OFF のみ有効**）

| 深度 | ON | OFF | 比 |
|---:|---:|---:|---:|
| 8 | 76.11 s | 80.65 s | **1.06 倍** |
| 9 | 85.91 s | 139.33 s | **1.62 倍** |

**深度 8 では 1.06 倍で、「目に見えて分かるレベル」に届きません。**

### ★★ 時間の測定が、深度をまたぐと信用できません

**同じ `69268` 深度 9・10-a あり を、2 つの実行で測った値です。**

| 実行 | 中核 | 三角形化 | 合計 |
|---|---:|---:|---:|
| A（深度 8 ON → 8 OFF → 9 ON） | 64.02 s | 21.89 s | **85.91 s** |
| B（深度 8 ON → 9 ON） | 51.28 s | **12.19 s** | **63.47 s** |

**構造の数字は 2 つの実行で完全に一致**（葉 175,556、$\sum P^2$ 161,366,292、
出力多角形 1,091,479）。**計算内容は同一です。**

**それでも三角形化が 1.8 倍違います。**

**そして深度 8 と 9 の大小関係が、実行によって逆転します。**

| 実行 | 深度 8 | 深度 9 |
|---|---:|---:|
| A | 76.11 | **85.91**（深度 9 が遅い） |
| B | 75.61 | **63.47**（深度 9 が速い） |

> **以前「実行の中は 1.06〜1.09」と記録しましたが、それは【同じ仕事を繰り返した】場合です。**
> **違う仕事を順に走らせると、この規模では 1.8 倍動きます。**
>
> **したがって「深度 8 と 9 のどちらが速いか」は、いまの測り方では決められません。**


### ★★ ばらつきの水準を訂正します（2026-09-05）— **4 つ目が見つかりました**

| 水準 | 実測 | 使えるか |
|---|---:|---|
| **同じ仕事を反復**（同一プロセス） | **1.06〜1.09** | — |
| **★ 違う仕事を順に走らせる**（同一プロセス） | **1.8 倍** | **使えない** |
| プロセスの間（同じバイナリ・同じ時間帯） | 1.03 | 使える |
| 時間帯の間 | 1.5〜1.6 倍 | 使えない |

**「実行の中は 1.06〜1.09」は【同じ仕事を繰り返した】場合の値でした。**

**実際の測定は、違う設定（深度・旗）を順に走らせる形です。そこでは 1.8 倍動きます。**

**根拠**: `69268` 深度 9・10-a あり を 2 つの実行で測ったところ、
**構造の数字は完全に一致**（葉 175,556、$\sum P^2$ 161,366,292、出力多角形 1,091,479）
なのに、**三角形化が 12.19 秒と 21.89 秒（1.8 倍）**でした。

**そして深度 8 と 9 の大小関係が実行によって逆転します。**

> **「同じ仕事の反復」で測った 1.06 を、測定の精度として使えません。**
> **最初に走る対象が遅くなる傾向があり、深度を昇順に測ると
> 残留ドリフトが深度と相関します**（`CLAUDE.md` の規律そのもの）。

---

## 案 1（単一 source の葉を割る）の閾値 — 2026-09-06

**設定**: `934258x111599`（Thingi10K、11,532 + 11,554 三角形）、$b=21$、8 スレッド、
`KRISITE_CHECKED_ARITH=OFF`、Release。**1 つの行が 1 回のプロセス実行**なので、
**行の中の比だけが有効**です（実行をまたぐと 2 倍以上ずれます。`IMPL-phase5.md` §81.7）。

合計秒（中核 + 出口）:

| 深度 | $P{=}64$ | **$P{=}128$** | $P{=}256$ | $P{=}512$ | $P{=}1024$ | $P{=}2048$ |
|---|---|---|---|---|---|---|
| 6 | 45.32 | **44.59** | 45.34 | 47.80 | 58.36 | 111.59 |
| 7 | 21.34 | **20.86** | 21.96 | 25.79 | 48.25 | 107.92 |
| 8 | 13.51 | **13.47** | 16.86 | 25.19 | 47.27 | 109.95 |

最大 $P_\ell$（深度が下限を決める。§81.3）:

| 深度 | $P{=}64$ | $P{=}2048$ |
|---|---|---|
| 6 | 2892 | 2892 |
| 7 | 1598 | 2002 |
| 8 | 912 | 2002 |

**体積は 18 設定すべてで厳密に一致**（GMP）。かたまり 2、余分辺 0 も不変。
**既定を $128^2 = 16384$ に設定しました。**

**割らない場合との比較**（同一実行内）:

| 対 | 割らない | $P{=}128$ |
|---|---|---|
| `934258x111599` 深度 6 | $\ge$ 7,488 s（打ち切り） | **44.59** |
| `1228189x509311` 深度 6 | 69.71 | **9.35** |
| `1228189x509311` 深度 6・**NSI 宣言あり** | 0.40 | **0.38**（規則は発火しない） |

---

## CP1 500 対（Thingi10K、実データ）— 2026-09-06

**設定**: 深度 6 / スレッド 8 / $b{=}21$ / **NSI = 検査して通ったものだけ宣言** /
`single_src_sq` = 16384 / Release / `KRISITE_CHECKED_ARITH=OFF`。
**500 対すべて同一設定**（`IMPL-phase5.md` §82 の設定の割れは解消済み）。

**結果**: 成功 499 / **失敗 1**（`135071x75653`）/ 停止 0。合計 **12,829 秒 = 3.56 時間**。

**NSI の宣言**: 1,000 模型ぶんのうち **930（93.0%）**。
宣言できなかった 70 模型（7.0%）は、**量子化後に自己交差する**もの。

| 量 | 中央値 | 95% | 最大 | 合計 |
|---|---:|---:|---:|---:|
| 入力三角形 (A+B) | 10,052 | 88,970 | 196,282 | 11,727,064 |
| **時間（秒）** | **3.0** | 93.4 | **1,418.3** | 12,829 |
| 出力多角形 | 110,112 | 1,439,699 | 7,224,577 | 186,655,000 |
| 領域 | 372,510 | 5,219,859 | 22,468,371 | 612,241,677 |
| レイ×三角形 判定 | 55,456,497 | 1,443,045,024 | 20,756,849,463 | 240,287,043,474 |
| BSP 切断候補 | 1,373,100 | 37,627,221 | 454,818,210 | 4,792,459,659 |

**上位 5 対が全時間の 31.3%。** 中央値と最大が 473 倍離れています。

**両対数の傾き**: $n \to P$ **0.91**、$P \to t$ **1.23**、$n \to t$ **1.19**。

**多角形 1 枚あたりの演算回数**（$n$ の 8 分位。**時間のばらつきに依りません**）:

| 帯 | $n$ | レイ×三角形 / P | BSP 切断候補 / P | 領域 / P |
|---|---:|---:|---:|---:|
| 1 | 2,272 | 398 | 10.2 | 3.35 |
| 4 | 5,828 | 558 | 12.5 | 3.44 |
| 7 | 16,062 | 622 | 11.1 | 3.48 |
| 8 | 24,674 | **786** | 15.3 | 3.47 |

### NSI 宣言の効果（同じ 30 対を両条件で測った対比較）

**同じ対・同じコードで、NSI の宣言だけを変えた**（`IMPL-phase5.md` §82）。

| 量 | 宣言なし | 宣言あり | 倍率 |
|---|---:|---:|---:|
| 出力多角形 | 1,085,434 | 495,336 | **2.2 倍** |
| BSP 切断候補 | 20,534,937 | 8,228,352 | **2.5 倍** |
| レイ×三角形 判定 | 581,160,144 | 243,740,574 | **2.3 倍** |
| **壁時計（秒／対）** | 81.7 | **38.9** | **2.4 倍** |

**演算回数の倍率（2.2〜2.5）と壁時計の倍率（2.4）が一致**しており、
時間の短縮が演算の減少で説明できます。**出力のハッシュは 30 対すべて不一致**
（断片の切れ方が変わるため。体積の恒等式は両条件とも $10^{-13}$ 台）。

---

## 分裂の検証を増分計算にした効果 — 2026-09-07（実装担当 v2）

> **★ 測定環境が変わっています。** この節より前の数字と**絶対値では比べられません。**
>
> | 項目 | 値 |
> |---|---|
> | 日付 | 2026-09-07 |
> | 論理コア | **16**（`nproc`。前任者の記録は「物理コア 8」） |
> | コンパイラ | clang 21.1.0（`zig c++` 経由） |
> | ビルド | `-O2 -g`、検査 OFF、`b = 21` |
> | スレッド | 8 |
> | 駆動 | `tests/thingi10k/exit_stages.cpp`（新規） |
>
> **開発コンテナが作り直されており、機械が同じである保証がありません**
> （`IMPL-v2.md` §0.2 / §0.3）。**比は同一実行の中で対にしています。**

### 測り方

**同じプロセスの中で、`ToMeshOptions::verify_split_naive` を切り替えて交互に回します。**
中核（`boolean`）は 1 度だけ回し、その `PolySoup` を使い回すので、
**比較対象は出口だけ**です。**出力のハッシュが一致することを毎回確かめています。**

### 結果

| 模型 / 深度 | 出口 合計（従来 → 増分） | **検証の比** | **出口の比** | ハッシュ |
|---|---|---:|---:|---|
| `425318` / 6 | 2.054 → **1.015 s** | **11.9 倍** | **2.02 倍** | 一致 |
| `78535` / 6 | 0.484 → **0.199 s** | **36.4 倍** | **2.43 倍** | 一致 |
| `69268` / 8 | 45.42 → **24.70 s** | **14.6 倍** | **1.84 倍** | 一致 |

**実行内のばらつき（1.06〜1.09）を大きく超えています。**
**そして同一実行内の比なので、時間帯の交絡を受けません。**

> **「出口が 2 倍」であって「全体が 2 倍」ではありません。**
> `69268` は中核が 47.3 s なので、**全体では 92.7 → 72.0 s（1.29 倍）**です。

### ★ 出口の段の内訳が、`DESIGN-phase5-hotspots.md` §3.2 から大きく変わっています

**同じ模型・同じ深度で、平面索引が 110.5 秒から 2.09 秒になりました。**
**A-3（セルで区切った索引。`IMPL-phase5.md` §73）が入ったためです。**
**§3.2 は A-3 の実装【前】の測定です。**

| 段（`69268` / 深度 8） | §3.2（A-3 の前） | **いま（A-3 + 増分計算）** |
|---|---:|---:|
| 出口の合計 | 134.42 s | **24.70 s** |
| 構成点 | 1.96 | 2.18 |
| 値で併合 | 1.53 | 2.38 |
| **平面索引** | **110.50（82%）** | **2.09（8.4%）** |
| 扇（T 解決 + 三角形化） | 5.23（3.9%） | **7.64（31%）** |
| 接触の分裂 | 14.88（11%） | **9.69（39%）** |

**多角形数が違います**（1,340,900 対 1,660,373）。変換の乱数種が違うので**配置が違います。**
**割合の比較として読んでください。**

### ★ 検証を外した後の最大の項 — 接触の分裂の【逐次】な下ごしらえ

`69268` / 深度 8、分裂 9.69 s の内訳:

| 段 | 秒 | 並列か |
|---|---:|---|
| **辺表**（無向辺 → 三角形の `std::map`） | **3.41** | **逐次** |
| **診断と成分分け** | **2.67** | **逐次** |
| 検証（増分計算） | 1.57 | 逐次 |
| 扇の計算 | 0.61 | **並列** |
| ID の書き戻し | 0.01 | 逐次 |

**逐次の 3 段で 7.65 s。出口 24.7 s の 31%、全体 72 s の 11%。**
**`SPEC-phase4.md` §3 が並列化の対象に挙げた「接触の分裂」のうち、
実際に並列なのは扇の計算（0.61 s、6%）だけです。**

### ★ 三角形化の内訳 — 96〜99% は T 頂点の解決（2026-09-07）

**`ms_tri`（扇 = T 解決 + 三角形化）を旗つきの計時で分けました**
（`ToMeshOptions::time_stages`。**既定は偽**）。**CPU 時間です。**

| 模型 / 深度 | 段の CPU 時間 | **T 解決** | **三角形化** | 並列効率 |
|---|---:|---:|---:|---:|
| `425318` / 6 | 718.8 ms | **687.5（96%）** | 31.3（4%） | 6.39 / 8 |
| `69268` / 8 | 56,109 ms | **55,461（99%）** | 648（1%） | 7.63 / 8 |

**「三角形化が遅い」ではありません。一般解も扇分割も安く、
重いのは T 頂点の解決です。**

### ★★ T 解決の照合 — 返した候補の 1 万分の 1 しか使っていない

**`CLAUDE.md`「性能を見るときの最初の問い」の 4 件目です。**
**A-3（セルで区切った索引）が入った【後】の数字です。**

| 模型 / 深度 | 走査した辺 | 索引が返した候補 | 挿入した T 頂点 | 辺あたり候補 | 辺あたり挿入 | **無駄** |
|---|---:|---:|---:|---:|---:|---:|
| `425318` / 6 | 410,155 | 25,733,589 | 32,365 | 62.7 | 0.079 | **794 倍** |
| `69268` / 8 | 6,515,491 | **2,522,158,684** | 213,117 | 387.1 | 0.033 | **11,834 倍** |

**構造的な理由**: `insert_t_vertices_with` は**多角形の辺ごとに、
その多角形が属する (葉, 支持平面) 群の候補を全部走査**します。
**同じ候補集合を、多角形の辺の数だけ走査し直しています。**

> **A-3 が効いていない、という話ではありません。** A-3 は群の大きさを
> 3〜391 倍に絞りました。**絞った後の集合を辺ごとに何度も走査している**という
> 別の問題です。**手法はまだ選んでいません**（`IMPL-v2.md` §3.3）。

### T 解決の走査順（辺ごと → 多角形あたり 1 回）— 2026-09-07

**候補集合は (葉, 支持平面) 群で共有されるので多角形の中で同じ**なのに、
**$n$ 角形なら $n$ 回走査し直していました**（`SPEC-phase5.md` §5.11）。
**判定は 1 つも変えず、走査の順序だけを入れ替えた**結果です。
**同一実行の中で旗（`ToMeshOptions::scan_per_edge`）を切り替えて比較しています。**

| 対象 | 候補集合の走査 | **`side` の評価** | 扇の CPU 時間 | 出口の壁時計 | 出力 |
|---|---:|---:|---:|---:|---|
| `425318` / 6 | 410,155 → **108,171**（3.79 倍） | 24,913,279 → 24,853,376（**1.002 倍**） | 709.8 → 720.5 ms | 1.041 → 1.035 s | **ハッシュ一致** |
| `69268` / 8 | 6,515,491 → **1,660,373**（3.92 倍） | 2,509,127,702 → 2,508,695,549（**1.000 倍**） | 58,826 → 54,038 ms | 24.428 → 23.826 s | **ハッシュ一致** |
| コーパス 420 構成 | 118,507 → **33,610**（3.53 倍） | 614,139 → 610,115（**1.007 倍**） | — | — | 三角形の列が一致 |

**走査回数は 3.5〜3.9 倍減りましたが、述語の評価回数は動きません。**
**`side` の評価対象は「候補 × 辺」の対で、走査順を変えても対の集合は同じ**だからです。
「載ったら打ち切る」は効きますが、**載るのは 1 万分の 1 以下**です。

**時間の向きは 2 模型で一致していません**（−1.5% と +8.9%）。
**どちらもばらつきの範囲内です。**

### ★ `side` の内訳 — 91〜96% は「載っていない」ことを言うためだけ

| | `425318` / 6 | | `69268` / 8 | |
|---|---:|---:|---:|---:|
| `side` の評価 | 24,853,376 | 100% | 2,508,695,549 | 100% |
| そのうち **0 を返した** | 2,279,931 | **9.2%** | 99,155,931 | **4.0%** |
| そのうち区間の内部（挿入） | 32,365 | **0.13%** | 213,117 | **0.0085%** |

### ★★ T 解決の候補を X 軸で整列し、区間を二分探索で絞る（案 (b2)）— 2026-09-07

**`DESIGN-phase5-hotspots.md` §13 で選んだ案です。**
**同一実行の中で旗（`ToMeshOptions::sort_candidates`）を切り替えた比較です。**

| | `425318` / 6 | `69268` / 8 |
|---|---:|---:|
| **`side` の評価** | 24,853,376 → **3,411,683（7.28 倍）** | 2,508,695,549 → **96,481,014（26.0 倍）** |
| `strictly_between` の評価 | 2,279,931 → 399,962 | 99,155,931 → 7,606,946 |
| **索引の段**（整列の費用がここ） | 46.5 → 50.2 ms（**+3.7**） | 1,361.9 → 1,386.7 ms（**+24.8**） |
| **扇の段**（壁時計） | 64.9 → **25.5 ms** | 4,459.8 → **550.1 ms** |
| **2 段の合計** | 111.4 → **75.7 ms（1.47 倍）** | 5,821.7 → **1,936.8 ms（3.01 倍）** |
| **出口全体** | 0.445 → **0.410 s（1.09 倍）** | 12.524 → **8.817 s（1.42 倍）** |
| 予測（`DESIGN` §13.3） | 1.56 倍 | 3.46 倍 |
| **挿入した T 頂点** | 32,365 → **32,365** | 213,117 → **213,117** |
| 出力のハッシュ | **一致** | **一致** |

**予測と実測が 6〜13% の差で一致しました。**

> **★ 扇の段だけを見ると 3.27 倍 / 10.1 倍ですが、整列の費用は索引の段に乗ります。**
> **足して 1.47 倍 / 3.01 倍が正しい数字です。**
> **機構が触る段だけを見ると過大評価になります。**

**二分探索に払う `cmp_h`**: 多角形あたり 16.2 回（`425318`）/ 20.9 回（`69268`）。

**コーパス（420 構成）**: 飛ばした候補 51,530、`side` 610,115 → 430,875（1.42 倍）。
**コーパスは小さいので削減率が低い**（群の候補集合が 61 / 382 に対し、コーパスは数個）。


---

## EMBER と比較可能な数字（`SPEC-phase5.md` §4.3.2）— 2026-09-08

> **★ 測定の条件を先に書きます。揃っていない部分を明記します。**

| 項目 | Krisite（この測定） | EMBER（§5.1） |
|---|---|---|
| 機械 | **不明**（コンテナ。`nproc` 16） | Intel i9-9900K（8 コア） |
| コンパイラ | clang 21.1.0（`zig c++`） | clang-12 |
| **最適化** | **`-O2 -g`** | **`-O3`** |
| 検査（`KRISITE_CHECKED_ARITH`） | **OFF** | — |
| スレッド | 8 | 並列（コア数不明） |
| $b$ | 21 | 26 |
| 深度 | **7**（§5.10） | 不明 |
| **§5.5 の検算** | **OFF**（§4.3.2） | — |
| NSI / NNC の宣言 | **していない** | **している**（§4.5.1） |
| 集計 | **幾何平均**（中核）/ **p95**（全体） | 幾何平均 |
| 対の合計面数 | 中央値 16,921 | **約 20,000** |

**揃っていないもの**: 機械、最適化の水準、$b$、NSI の宣言、深度。
**揃えたもの**: 集計（幾何平均）、範囲（中核だけ）、面数の帯。

### 層化

**`cp1.txt` の 500 対から、対の合計面数の帯で 40 対を決定的に選びました**
（`data/thingi10k/ember_cmp.README.md`）。

### 結果（40 対、失敗 0）

**`-O2 -g` と `-O3 -DNDEBUG` の両方で回しました。**

| 帯（対の合計面数） | 対 | **中核 1 演算の幾何平均**（`-O3`） | 中核 p95 | **全体 1 対の p95** | 全体の中央値 |
|---|---:|---:|---:|---:|---:|
| 2,000〜5,000 | 10 | **427.9 ms** | 1,609 ms | **8.30 s** | 2.09 s |
| 15,000〜25,000 | 20 | **3,456.5 ms** | 8,696 ms | **45.74 s** | 15.99 s |
| 50,000〜100,000 | 10 | **9,060.0 ms** | 35,526 ms | **178.70 s** | 60.94 s |
| **全体** | 40 | **2,608.7 ms** | 18,249 ms | **118.96 s** | 15.81 s |

**面数の中央値 19,016 は、EMBER の「1 対あたりおよそ 20,000 面」の 0.95 倍です。**

#### 最適化の水準（`-O2 -g` → `-O3 -DNDEBUG`）

| 量 | 中央値の比 | 最小 | 最大 | `-O3` が速い対 |
|---|---:|---:|---:|---:|
| 全体 | **1.021** | 0.964 | 1.206 | **33 / 40** |
| 中核 | **1.019** | 0.961 | 1.234 | **33 / 40** |

**効果は中央値で 2%。ばらつきの範囲内ですが、方向は一貫しています。**

#### 段の内訳（`-O3`。40 対の合計）

| 区分 | 割合 |
|---|---:|
| 入口 `from_mesh` | **0.2%** |
| **中核 `boolean`** | **72.9%** |
| **出口 `to_mesh`** | **26.9%** |

| 出口の 5 段 | 出口の割合 | 全体の割合 |
|---|---:|---:|
| **接触の分裂** | **41.0%** | 11.0% |
| 値で併合 | 18.7% | 5.0% |
| 構成点 | 18.4% | 5.0% |
| 平面索引（整列込み） | 15.2% | 4.1% |
| 扇（T 解決 + 三角形化） | **6.8%** | 1.8% |

| 中核の 3 段 | 中核の割合 |
|---|---:|
| **arrange** | **33.7%** |
| 縫合 | 29.2% |
| 分類 | 23.9% |
| （前処理・葉の列挙など） | 13.2% |

> **★ 引き継ぎ時の「出口が 76%」（`69268` の self-union、深度 8）とは、
> 測っている対象が違います。**
> **あちらは self-union 1 件、こちらは 2 項演算 40 対です。**

#### EMBER との比

| | 値 | 由来 |
|---|---:|---|
| Krisite の中核 1 演算（幾何平均、`-O3`） | **2,608.7 ms** | **実測** |
| EMBER の報告値 | 1.6 ms | 論文 |
| **比** | **約 1,630 倍** | **導出**（割り算） |

**揃っていない条件**: NSI / NNC の宣言（Krisite はしていない）、$b$（21 対 26）、
機械、深度。**揃えたもの**: 最適化（`-O3`）、集計（幾何平均）、範囲（中核だけ）、
面数（中央値で 0.95 倍）、検算 OFF。

### NSI 宣言ありで測り直した（2026-09-08）

**EMBER は NSI / NNC を宣言します**（§4.5.1）。**残る条件差のうち最大のものでした。**

**`nsi_mode = 1`（検査して通ったものだけ宣言）を使いました。**
**`3`（検査せずに宣言）は EMBER に近いのですが、量子化後に自己交差する模型があるので、
偽の宣言で出力が壊れます。** 実測で 80 模型ぶんのうち **77 が宣言でき、3 が却下**（96.2%）。

| | 宣言なし | **宣言あり** | 比 |
|---|---:|---:|---:|
| **中核 1 演算の幾何平均** | 2,608.7 ms | **1,266.8 ms** | **2.06 倍** |
| 全体 1 対の p95 | 118.96 s | **39.69 s** | 3.00 倍 |
| 全体の中央値 | 15.81 s | **6.22 s** | 2.54 倍 |
| 総時間（40 対） | 1,270 s | **836 s** | 1.52 倍 |
| 入口 / 中核 / 出口 | 0.2 / 72.9 / 26.9% | **2.4 / 77.6 / 20.0%** | — |

| 帯（対の合計面数） | **中核 1 演算の幾何平均** | 全体 1 対の p95 |
|---|---:|---:|
| 2,000〜5,000 | **420.7 ms** | 12.87 s |
| 15,000〜25,000 | **1,243.1 ms** | 27.63 s |
| 50,000〜100,000 | **3,961.9 ms** | 313.37 s |
| **全体** | **1,266.8 ms** | 39.69 s |

**EMBER 比**: 1,266.8 / 1.6 = **約 792 倍**。

#### ★ ただし 11 / 40 の対で遅くなります

| 量 | 中央値 | 最小 | 最大 | 宣言ありが速い対 |
|---|---:|---:|---:|---:|
| 全体 | **1.79 倍** | **0.57** | 16.88 | **29 / 40** |
| 中核 | **1.66 倍** | 0.53 | 20.82 | 29 / 40 |

**最悪の対**: `472008x1451898`（85,000 面）が **178.70 → 313.37 s（0.57 倍）**。
**片方だけが NSI を宣言できています（0/1）。**

**入口の費用**: 1 対あたり **+376 ms**（NSI の検査）。
**小さい対が軒並み遅いのはこれで半分は説明できますが、残りと最悪の対は未説明です。**


---

## 構成点キャッシュの実装（2026-09-09）

**`SPEC-phase5.md` §5.10.12。`std::map` → 開番地法（線形探索）の自前実装。**

> **★ 同一プロセスの中で旗を切り替えた A/B です。**
> **時間帯の交絡（下記「ばらつきの 3 つの水準」）を受けません。**

**設定**: `ember_o3_only.txt` の 40 対、深度 6（適応）、8 スレッド、$b=21$、
3 演算（$\cup$ / $\cap$ / $\setminus$）、**中核のみ**（出口は含みません）。

| 統計 | CPU 時間の比（`std::map` ÷ 開番地法） |
|---|---:|
| 対の数 | 40 |
| **最小** | **1.12 倍** |
| 第 1 四分位 | 1.23 倍 |
| **中央値** | **1.27 倍** |
| 第 3 四分位 | 1.32 倍 |
| **最大** | **1.59 倍** |
| 幾何平均 | 1.28 倍 |
| **出力の食い違い** | **0 対** |

**探索の単価**（同じ機械で実測、88,000 節点 = 全体 ÷ スレッド数、96 バイトの値）:

| 引き方 | `std::map` |
|---|---:|
| 局所性なし | **116.0 ns** |
| 局所性あり（64 個の作業集合） | **34.5 ns** |

**実測の削減は 54.7 ns/探索**（`425318＼73464` の同一実行 A/B から導出）。**上の範囲の中にあります。**

### 中核の CPU 時間の種類別の内訳（`425318＼73464`、深度 6、8 スレッド）

**分母は計測を切ったビルドの CPU 時間 3.437 秒**（計測を入れると +22%）。

| 種類 | 割合 |
|---|---:|
| 述語（`side` / `intersect3` / `cmp_h`） | **13.0%** |
| 記憶の確保（1,955 万回、単価 1.2 ns） | 0.7% |
| **構成点キャッシュの探索**（置き換え前） | **10.6〜35.7%** |
| 未計上 | 64.6% |

**`side` の被符号値の実際の幅**（上界は $9b+20$）:

| $b$ | 上界 | 観測した最大 | > 192 ビット |
|---:|---:|---:|---:|
| 21 | 209（4 リム） | **162〜166** | **0.0%** |
| 26 | 254（4 リム） | **209〜212** | **0.0〜0.1%** |

**★ ただしリム数は減らせません**（`CLAUDE.md`「乱択の実測値でリム数を減らさない」）。

---

## 40 対の絶対値の測り直し — 候補を尽くした累積（2026-09-10）

**`SPEC-phase5.md` §5.10.13 の「4. 40 対の絶対値の測り直し」。**
**時間で確かめた改善（構成点キャッシュ 1.27 倍、`cutbits` 1.32 倍、早期 return 1.3 倍、
縫合の並列化 1.6〜1.8 倍）は掛け算できないので、累積はここで初めて出ます。**

**設定**: `ember_nsi.txt` と同じ 40 対（`ember_cmp.README.md`）、深度 7、8 スレッド、$b=21$、
`-O3 -DNDEBUG`、`nsi_mode = 1`、検算 OFF。**前回（2026-09-08）と同じ手順。**

### ★ 先に、時間帯の交絡を測りました

**前回の数字（2026-09-08、836 s）は別の日のものです。** `CLAUDE.md` の「時間帯の間 1.5〜1.6 倍」に
当たるので、**前回と同じコード（`973c47a`）を今日の機械で回し直しました。**

| 同じ旧コード | 2026-09-08 | **2026-09-10** | 比 |
|---|---:|---:|---:|
| 中核 1 演算の幾何平均 | 1,266.8 ms | **702.0 ms** | **1.80 倍** |
| 総時間（40 対） | 836 s | **494 s** | **1.69 倍** |
| 対ごとの比 | | 最小 1.19 / 中央値 1.85 / 最大 2.56 | |

> **コードは同一で、機械のほうが 1.7〜1.8 倍速くなっています。**
> **前回の数字と直接比べていたら、累積を 2.56 倍（総時間）/ 3.71 倍（中核）と報告するところでした。**
> **「時間帯の間 1.5〜1.6 倍」の 2 例目です（今回は 1.7〜1.8 倍）。**

### 結果 — 同じ日・同じ機械で、旧（`973c47a`）対 新（`1bae000`）

| 帯（対の合計面数） | 対 | 中核 1 演算の幾何平均: 旧 → 新 | 比 | 全体 1 対の p95: 旧 → 新 | 比 | 総時間: 旧 → 新 | 比 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 2,000〜5,000 | 10 | 207.5 → 109.4 ms | **1.90** | 6.60 → 4.88 s | 1.35 | 17 → 13 s | 1.37 |
| 15,000〜25,000 | 20 | 708.7 → 329.6 ms | **2.15** | 15.15 → 10.68 s | 1.42 | 190 → 136 s | 1.40 |
| 50,000〜100,000 | 10 | 2,330.3 → 1,139.1 ms | **2.05** | 186.32 → 104.36 s | 1.79 | 287 → 178 s | 1.62 |
| **全体** | 40 | **702.0 → 341.1 ms** | **2.06** | 25.61 → 19.65 s | **1.30** | **494 → 326 s** | **1.51** |

| 量 | 旧 | 新 | 比 |
|---|---:|---:|---:|
| 全体 1 対の中央値 | 4.04 s | 2.90 s | 1.39 |
| 対ごとの比（全体） | | 最小 1.30 / 中央値 1.39 / 最大 1.79 | **40 / 40 が速い** |
| 対ごとの比（中核） | | 最小 1.64 / 中央値 1.96 / 最大 3.37 | 40 / 40 |
| 失敗 / 停止 | 0 / 0 | 0 / 0 | |

**EMBER 比**（幾何平均、機械が違う）: 341.1 / 1.6 = **約 213 倍**（前回の記録 792 倍は、
別の日の機械の状態が 1.8 倍遅かった分を含んでいます）。

#### 段の内訳（40 対の合計、壁時計）

| | 入口 | **中核** | うち arrange / 分類 / **縫合** | 出口 | 未計上（駆動側） | 合計 |
|---|---:|---:|---|---:|---:|---:|
| 旧 | 11 s | **304 s** | 127 / 76 / **66** | 87 s | 93 s | 494 s |
| **新** | 11 s | **139 s** | 52 / 65 / **7** | 86 s | 91 s | 326 s |
| 比 | 1.0 | **2.19** | 2.4 / 1.2 / **9.4** | 1.0 | 1.0 | 1.51 |

**観測**: 中核が 2.19 倍、縫合が 9.4 倍、arrange が 2.4 倍。**出口と駆動側（量子化・NSI 検査・
ハッシュ・位相検査。合計の 28%）は変わっていません。**
**評価**: 中核は 40 対の 62% → 43% になり、**出口（26%）と駆動側（28%）が並ぶ段になりました。**
**全体の p95（19.65 s）を 1 秒に近づけるには、中核だけでは足りません**（推測。出口が 86 s = 26% を占め、
中核をゼロにしても 40 対で 187 s 残る、という導出から）。

**ハッシュは 40 / 40 で違います**（縫合の並列化で出力の順序が変わる。`DESIGN` §31.1）。
**正しさはこの実行では見ていません**（検算 OFF）。正しさは ctest / 変異 / 順序を除いた鍵で別に見ています。

### 駆動側の 91 s を刻んだ（2026-09-10。仕様側の指示 §5.10.14「次」の 1）

**駆動（`thingi_cp1`）に位相検査・体積検算・ハッシュの時計を足し、同じ 40 対を回し直しました**
（新コード、設定は上と同じ）。**和は合計と 0.8% で合います。**

| 区分 | 40 対の合計 | 割合 | 実務で残るか |
|---|---:|---:|---|
| 入口 `from_mesh`（NSI 検査込み） | 10.8 s | 3.2% | 残る |
| **中核 `boolean`** | **142.7 s** | **42.9%** | 残る |
| **出口 `to_mesh`** | **90.4 s** | **27.1%** | 残る |
| **位相検査 `check_topology`**（演算ごとに 2 回） | **84.7 s** | **25.4%** | **切る**（CP4 §4.2） |
| 体積の検算 `volume6_fp` × 5 | 1.5 s | 0.5% | 切る |
| 出力のハッシュ × 3 | 0.2 s | 0.1% | 切る |
| 残り（印字など） | 2.5 s | 0.8% | — |
| 合計 | 332.8 s | 100.0% | |

**観測**: 未計上 91 s のほぼ全部が位相検査でした（1 演算に 2 回呼んでいる。統計用と合否用）。
**評価**: 駆動側は「実務で切る」検査で、目標との距離を測る分母から外します。

#### ★ 実務の分母（入口 + 中核 + 出口 = 243.9 s）

| | 入口 | **中核** | **出口** |
|---|---:|---:|---:|
| 割合 | 4.4% | **58.5%** | **37.1%** |

| 帯 | 全体 1 対の p95（駆動込み） | **p95（実務の分母）** | 中央値（同） |
|---|---:|---:|---:|
| 2,000〜5,000 | 4.38 s | **3.08 s** | 0.82 s |
| 15,000〜25,000 | 9.85 s | **7.05 s** | 1.69 s |
| 50,000〜100,000 | 110.30 s | **75.57 s** | 5.87 s |
| 全体 | 19.56 s | **12.76 s** | 2.14 s |

**目標「p95 が 1 秒以内」との距離**: 帯 15,000〜25,000 で **7.05 s（約 7 倍）**。駆動込みの 9.85 s より近い。

#### 出口の内訳（実務の分母に対して）

| 出口の 5 段 | 40 対の合計 | 出口の中 | **実務の分母の中** |
|---|---:|---:|---:|
| **接触の分裂** | 31.7 s | 35.1% | **13.0%** |
| 平面索引（整列込み） | 22.3 s | 24.7% | 9.2% |
| 値で併合 | 16.0 s | 17.7% | 6.6% |
| 構成点 | 14.3 s | 15.8% | 5.9% |
| 扇（T 解決 + 三角形化） | 5.9 s | 6.6% | 2.4% |

**2026-09-08 の内訳（接触の分裂 41% / 値で併合 19% / 構成点 18% / 平面索引 15% / 扇 7%）と
順位が変わっています**（平面索引が 15% → 25%）。同じ日の測定ではないので理由は分かりません。

#### ★ 同じコードでも、同じ時間帯の中で 1.22 倍ばらつきました

**新コードの 40 対を、11:24〜11:22 の 1 時間の中で 4 回回しました**（うち 2 回は駆動の編集が
失敗して同じバイナリだった）: **326.5 / 397.7 / 339.6 / 332.8 s**。最大/最小 = **1.22**。
**「プロセスの間 1.03」（上の表）より大きい。** `load average` は毎回 1 未満で始めています。
**同じ日の対照（旧コード 494 s 対 新 326 s = 1.51 倍）は、このばらつきの外にあります。**

### 分類と arrange の中を刻んだ（2026-09-10。§5.10.14.7「まず刻むだけ」）

**同じ 40 対、新コード（分類の内訳の時計を足した版）。時計は領域ごとに 4 回で、
`BoolOptions::measure_classify`（既定 偽）の下。駆動 2 つは真にして回しています。**

#### 実務の分母（231.2 s）に対する、段ごとの割合（壁時計）

| 段 | 40 対の合計 | 割合 |
|---|---:|---:|
| 出口 | 84.5 s | 36.5% |
| **分類: レイキャスト** | **56.5 s** | **24.5%** |
| **arrange: 断片の生成**（クリップ + 局所 BSP + 確定） | **35.9 s** | **15.5%** |
| 前処理 + 葉の列挙 | 14.9 s | 6.5% |
| 入口 | 10.8 s | 4.7% |
| 縫合（大域） | 7.2 s | 3.1% |
| 分類: 代表点 | 5.7 s | 2.4% |
| arrange: 準備（early-out、平面の絞り込み、切断集合） | 5.5 s | 2.4% |
| arrange: 収集 + 存在判定 + 共平面 | 4.7 s | 2.0% |
| arrange: 縫合（葉ごと） | 4.3 s | 1.9% |
| 分類: 準備 + 判定 + 出力 | 1.2 s | 0.5% |
| **和** | **231.2 s** | **100.0%** |

**分類と arrange の内訳は CPU（全スレッドの和）の比で壁時計を按分しています**
（分類は並列効率 0.99、arrange は葉ごとの壁時計の和なので同じ扱い）。
**この実行の実務の分母は 231.2 s で、前の実行（243.9 s）と 5% 違います**（プロセスの間のばらつき）。

#### 分類の中（CPU の和 488.3 s、並列部の壁時計 61.5 s、逐次部 1.8 s、並列効率 0.99）

| 区分 | CPU | 割合 |
|---|---:|---:|
| 準備（代表断片の選択、強制値） | 6.1 s | 1.2% |
| 代表点（`interior_point`） | 43.6 s | 8.9% |
| **レイキャスト（`winding_split` × source）** | **435.7 s** | **89.2%** |
| 判定 + 出力 | 3.0 s | 0.6% |

**2 模型・深度 6 の `chain_frag` では レイ 53〜65%、代表点 29〜42%** で、
**40 対・深度 7 ではレイに寄ります**（模型が大きく、レイあたりの候補が 554 個）。

#### レイの候補の漏斗（「返した候補のうち何割を使っているか」）

| 構成 | レイ | 索引の候補 / レイ | **前判定を通過 / レイ** | 寄与 / レイ |
|---|---:|---:|---:|---:|
| 40 対（深度 7） | 40,272,069 | **554.5** | （未計測。駆動の列にない） | — |
| `425318` 単発（深度 6） | 141,224 | 138.5 | **13.64** | 2.09 |
| `425318` 連鎖 1 | 439,415 | 217.9 | 19.99 | 2.13 |
| `73464` 単発 | 26,682 | 55.6 | 6.28 | 1.57 |

**観測**: 索引が返す候補の 90% は前判定（投影 AABB の `cmp_axis_int`）で落ち、
厳密判定に回るのは 10%、寄与するのは 1.5%。**40 対では 1 本のレイに 10.8 µs、
候補 1 個あたり 20 ns**（レイの CPU 435.7 s ÷ 候補 22.3 G 個。**前判定と厳密判定を分けて測っていないので、
候補 1 個あたりの平均**）。
**`ray_tri_aabb` / `ray_tri_fwd` / `ray_cheap_tests` は `opt.record_ray_filter`（既定 偽）の下**なので 0 です
（計測の費用を本番に持ち込まない）。

**評価**: 分類の中の「大きな 1 つ」はレイキャストの候補の走査です（分類の 89%、実務の分母の 24.5%）。
**§5.10.10.2.2 と同じ問い（候補の 90% は「載っていない」と言うためだけに払われている）が、分類にも立ちます。**
ただし前判定は既に安い（候補 1 個 20 ns）ので、減らすなら**候補の数**（索引の粒度）のほうです。

#### arrange の中（葉ごとの壁時計の和 341.7 s）

| 区分 | 和 | 割合 |
|---|---:|---:|
| 収集 | 14.8 s | 4.3% |
| 存在判定 | 8.7 s | 2.5% |
| 準備 | 37.1 s | 10.9% |
| **断片の生成**（セル平面のクリップ + 局所 BSP の切断 + 確定） | **243.3 s** | **71.2%** |
| 共平面の揃え | 8.4 s | 2.5% |
| 縫合（葉ごと） | 29.4 s | 8.6% |

**断片の生成の中はこの実行では刻んでいません**（1 段だけ）。既知の内訳（§5.10.11〜12 の実測）は、
`split_fragment` の 91〜95% が早期 return、述語は中核 CPU の 13%、`edge` の複製と確保が主でした。
**刻むならクリップ / 局所 BSP の切断 / 確定 の 3 つで、多角形ごとに時計 3 回です**（未実施）。

#### 未計上

**中核 135.9 s のうち arrange 50.4 + 分類 63.4 + 縫合 7.2 = 121.0 s で、残り 14.9 s（11%）は
前処理と葉の列挙**（駆動の列に無いので差で出した）。

### 索引の粒度を測った（2026-09-10。§5.10.14.11「まず測るだけ」）

**`BoolOptions::record_ray_levels`（既定 偽）の下で、候補がレイ索引のどの段から来たか（`ray_cand_level`）と、
段ごとの項目数（`ray_items_level`。source × 3 軸の和）を数えました。段 0 が最も細かい。**
同じ 40 対、深度 7、8 スレッド、-O3。**候補の総数は駆動の `ray_tri_tests` の和と 0.07% で合います。**

#### 40 対の合計 — 粗い段が候補を支配しています

| 段 | 項目数 | 割合 | 候補 | **割合** | **候補 / 項目** |
|---|---:|---:|---:|---:|---:|
| 0（最細） | 8,457,624 | 29.7% | 596,924,876 | 2.7% | 71 |
| 1 | 8,894,760 | 31.2% | 239,609,544 | 1.1% | 27 |
| 2 | 6,529,620 | 22.9% | 942,718,493 | 4.2% | 144 |
| 3 | 3,049,092 | 10.7% | 2,051,480,542 | 9.2% | 673 |
| 4 | 990,450 | 3.5% | 2,296,607,384 | 10.3% | 2,319 |
| 5 | 368,262 | 1.3% | 4,311,570,081 | 19.3% | 11,708 |
| **6** | **179,724** | **0.6%** | **10,668,350,404** | **47.8%** | **59,360** |
| 7 | 10,830 | 0.0% | 1,207,412,746 | 5.4% | 111,488 |

**観測**: 段 3 以上は項目の 16% で、候補の **92%** を出しています。段 6 は項目の 0.6% で候補の 47.8%。
**候補 / 項目は段が 1 つ粗くなるごとに約 4〜5 倍**（段 2 以降: 144 → 673 → 2,319 → 11,708 → 59,360）。

**機構（導出）**: 段 $\ell$ のセルは段 0 の $4^\ell$ 倍の面積なので、そのセルの項目を見るレイの数も約 $4^\ell$ 倍。
**項目 1 個の費用が段ごとに 4 倍になる**ので、粗い段の少数の項目が候補を支配します。
2 模型で「候補の割合 ∝ 項目数 × $4^\ell$」を検算すると、**形（最多の段）は合い、最も粗い段は過大**でした
（`425318`: 予測 1.2 / 0.9 / 8.7 / 22.7 / **43.0** / 23.6%、実測 8.4 / 6.2 / 25.2 / 22.9 / **33.4** / 3.9%）。
レイは投影の箱に一様には落ちないので、外縁の粗いセルは予測ほど見られません。**予測は上界です。**

#### 対ごと — 「78535 型」でなくても粗い段が支配します

| 項目の最多段 | 対 | 候補 / レイの中央値 | 最大 | 粗い段（≥ 3）から来た候補の割合（中央値） |
|---|---:|---:|---:|---:|
| 0 | 14 | 247 | 772 | 85% |
| 1 | 15 | 196 | 596 | 78% |
| 2 | 8 | 234 | 500 | 81% |
| **3** | 3 | 313 | **1,436** | 90% |

**対ごとの「段 3 以上から来た候補の割合」: 最小 28%、中央値 84%、最大 99%。**
**§11.3 の「大きい三角形が粗い段にある模型（`78535` 型）」は候補が最多になりますが、
そうでない対でも候補の 8 割は粗い段から来ています。** 機構が一般だからです（項目の費用 ∝ $4^\ell$）。

#### 候補 / レイの分布（同じ 40 対。深度は関係しません）

| 統計 | 候補 / レイ |
|---|---:|
| 最小 | 50.8 |
| 中央値 | 230.9 |
| p90 | 499.9 |
| 最大 | **1,436.2**（`110044x1146261`） |
| 加重平均（総候補 ÷ 総レイ） | 554.5 |

**平均 554.5 は 1 対に引っ張られています**: `110044x1146261` だけで全候補の **50%**、次の `472008x1451898` が 18%。
**`110044` は項目の最多段が 6（X 軸: 8 / 1,696 / 7,106 / 5,082 / 2,224 / 5,858 / **7,748**）で、`78535` 型です。**

**★ 深度について**: レイ索引は source ごとの 2 次元格子で、**八分木の深度に依りません。**
`425318` 単発を深度 6 と 7 で回すと候補 / レイは 138.5 と 142.4 でした（同じ模型・同じ設定で比較）。
「候補 138.5 → 554.5」は深度ではなく**対の違い**（分布の上側）です。

#### 費用の見積もり（推測。候補 1 個 20 ns の平均から）

| 何を | 候補 / レイ | レイ 1 本 |
|---|---:|---:|
| 今 | 554.5（加重平均） | 10.8 µs |
| 段 3 以上の項目を別の形にして候補が消えたとすると（上界） | 44（8%） | 約 0.9 µs |

**分類のレイは実務の分母の 24.5% なので、上界で全体 1.29 倍**（$1 / (1 - 0.245 \times 0.92)$）。
**D-3 が「候補が消える」形にならなければこの上界には届きません**（推測）。

### 粗い段に入る理由と、段 0 に割り当てた場合の費用（2026-09-10。§5.10.14.13「検討の前に確かめる」）

**`RayIndex::granularity()` を足し、三角形ごとに「段」「その段のセル 1 つに収まる大きさか」
「段 0 のセルで数えた覆いの数 $c_u c_v$」を数えました**（`record_ray_levels` の下。source × 3 軸の和。同じ 40 対）。

| 段 | 三角形 | **うち段のセル 1 つに収まる大きさ** | 今の項目 | **段 0 のセルで数えた項目** | 倍 | 候補の割合 |
|---|---:|---:|---:|---:|---:|---:|
| 0 | 3,400,449 | 24% | 8,457,624 | 8,457,624 | 1.0 | 2.7% |
| 1 | 2,826,210 | 0% | 8,894,760 | 20,017,275 | 2.3 | 1.1% |
| 2 | 2,204,568 | 34% | 6,529,620 | 33,846,495 | 5.2 | 4.2% |
| 3 | 1,065,024 | 48% | 3,049,092 | 44,605,671 | 14.6 | 9.2% |
| 4 | 359,853 | 58% | 990,450 | 44,810,103 | 45.2 | 10.3% |
| 5 | 136,668 | 50% | 368,262 | 63,823,971 | 173.3 | 19.3% |
| 6 | 70,092 | 72% | 179,724 | 105,222,297 | 585.5 | 47.8% |
| 7 | 3,834 | 61% | 10,830 | 23,656,191 | 2,184 | 5.4% |

- **「段のセル 1 つに収まる大きさ」**: 幅が $2^\ell$ 個以下の段 0 セル。段 $\ell$ にいるのは境界をまたいで
  段 $\ell-1$ で 3 セルになったからで、**揃えれば 1 段細かい段に入ります**（段 1 が 0% なのは、
  段 1 の三角形は段 0 で 3〜4 セルなので定義上収まらないため）
- **段 3 以上の三角形 1,635,471 枚（全体の 16.2%）のうち 52% が「収まる大きさ」、48% は本当に幅が 1 セルを超える**

**観測 1（仕様側の問い）**: 「本当に大きい」と「境界をまたぐだけ」が**ほぼ半々**です。
**評価**: 境界をまたぐ分を 1 段細かくしても項目 1 個の費用は 4 分の 1 にしかならず、
粗い段の候補 92% のうち半分が 4 分の 1 になる → 候補は約 1.5 倍減るだけ（推測。段の費用 $4^\ell$ の機構から）。
**「桁を変える」には足りません。**

**観測 2（段 0 に全部割り当てる案の費用）**: 項目は **12.1 倍**（28.5M → 344M。三角形 1 枚あたり平均 34.2 セル）。
対ごとの倍率は中央値 9.9、最大 72.5（`110044x1146261`）。1 項目 4 バイトなので、**最大の模型でも 1 軸 16 MB**。

**観測 3（同案の効果の見積もり）**: 一様なレイの仮定で、候補 / レイ ≈ 三角形 1 枚あたりの段 0 セル数（$\sum c_u c_v / n$）。

| 対（今の候補 / レイの順） | 今（実測） | 段 0 に割り当てた場合（見積もり） | 比 | 項目の倍率 |
|---|---:|---:|---:|---:|
| `73464x69883` | 51 | 12.2 | 4.2 | 3.9 |
| `109576x384574` | 171 | 30.4 | 5.6 | 10.3 |
| `636814x200078` | 231 | 18.6 | 12.4 | 6.1 |
| `103146x472126` | 315 | 29.5 | 10.7 | 11.0 |
| `97912x73738` | 500 | 47.4 | 10.6 | 16.1 |
| `472008x1451898` | 772 | 38.1 | 20.3 | 16.0 |
| `110044x1146261` | 1,436 | 185.6 | 7.7 | 72.5 |
| **中央値** | **230** | **28.7** | **7.0** | 9.9 |

**評価**: 段 0 に割り当てれば候補は中央値で 7 分の 1（推測。上界側の見積もり。§33 の同じ仮定は実測より 2 倍ほど多く出ました）。
**これは「桁を変える」道に届く数字です。** 床は投影 AABB が点を含む数（`425318` で 13.6 / レイ）で、
見積もり 17〜18 と整合します。**`110044` は本当に大きい三角形が 3,000 枚あるので床が 186 / レイと高く、
AABB では絞れません**（三角形そのものを走査する索引が要る。別の問題）。

### p95 の裾を調べた（2026-09-10。§5.10.14.16。既存の 40 対の結果から。計算ゼロ）

**分母は実務の分母（入口 + 中核 + 出口）。同じ 40 対（`ember_gr_results.txt`）。**

#### 裾の対は「面数が同じで断片が 5〜16 倍」の対です

| 帯 | 対 | 面 | 葉への割り当て / 面 | 局所 BSP の切断 | 断片 | **断片 / 面** | 断片 / 割り当て | 実務の分母 |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| 15k–25k | 中央値 `1628449x1681123` | 23,664 | 9.6 | 750,117 | 148,057 | **6.3** | 0.65 | 1.71 s |
| | `75117x92152` | 18,210 | **96.1** | 5,992,776 | 488,551 | **26.8** | 0.28 | 6.70 s |
| | p95 `84737x1146165` | 17,560 | **59.0** | 5,320,449 | 588,176 | **33.5** | 0.57 | 7.93 s |
| | 最大 `110044x1146261` | 21,716 | **73.0** | 10,934,493 | 2,238,596 | **103.1** | 1.41 | 53.88 s |
| 50k–100k | 中央値 `104442x252119` | 99,640 | 3.7 | 1,309,869 | 427,239 | **4.3** | 1.15 | 6.71 s |
| | `53754x1396886` | 80,168 | 17.4 | 5,537,517 | 783,632 | 9.8 | 0.56 | 12.12 s |
| | p95 `472008x1451898` | 85,000 | 6.2 | **24,763,926** | 5,090,790 | **59.9** | **9.59** | 75.30 s |

**時間との log 相関（40 対）**: 断片 **0.92**（帯の中では 0.98〜1.00）、割り当て 0.75、三角形の大きさ（Σ段 0 セル）0.76、
面 0.59（**帯 15k–25k の中では 0.08**）。

**観測**: 帯の中で時間を決めているのは面数ではなく**断片数**です。裾の対は面数が中央値と同じで、断片が 5〜16 倍。
**段の構成は中央値の対とほぼ同じ**（中核 51〜76% / 出口 20〜46%。`110044` だけレイが 49%）。
**評価**: 裾は「特定の段の爆発」ではなく**「全体が断片数に比例して遅い」形**です。
そして断片が多い理由は 2 つに分かれます。

| 形 | 対 | 何が多いか | 機構（推測） |
|---|---|---|---|
| **割り当てが多い** | `75117` / `84737` / `110044` / `53754` | 割り当て / 面が 17〜96（中央値 4〜10） | 三角形が多くの葉にまたがる（大きい三角形）→ 葉ごとにセル平面でクリップされて断片になる |
| **切断が多い** | `472008` | 断片 / 割り当てが 9.6（他は 0.3〜1.5）、切断 24.8M | 葉の中の局所 BSP が細かく切る（交差の密度、共平面） |

**どちらも「入力の性質」で、Thingi10K の中の実務らしくない模型かどうかは、模型を見ないと分かりません**（未確認）。
**裾を縮めるなら、段ではなく断片数を減らす機構（大きい三角形の扱い、切断の抑制）が対象になります**（推測）。

**目標との関係**: 帯 15k–25k の中央値 1.71 s は目標の 1.7 倍、p95 7.93 s は 7.9 倍。
**p95 は「断片 / 面が 30 以上の対」で決まっています。**

### A を実装した — 段 0 の覆うセル全部に割り当て、K は記憶の上限から（2026-09-10。§5.10.14.15）

**`RayIndex::build(m, along, fine_cap, fine_budget)`。** 段 0 で覆うセル数 $c_u c_v \le K$ の三角形は覆うセル全部に、
超える三角形は従来の粗い段に入れます。問い合わせは変えていません。
**K の決め方は 2 つ**: 固定値（`ray_index_fine_cells`）と、**記憶の上限「三角形 1 枚あたり `fine_budget` 項目」を
満たす最大の K を模型・軸ごとに二分探索で導く形**（`ray_index_fine_budget`。入力だけの関数なので決定的）。
**既定は上限 16。** 0 で従来（正解器）。

#### ★ 「K = 256 なら上限に収まる」は誤りでした

**前の報告で「K = 256 は 40 対の最大でも 13.5 倍・6 MB に収まる」と書きましたが、それは【今の項目数に対する倍率】で、
仕様側が置いた上限「項目 / 三角形」ではありませんでした。** K = 256 では `296803x117525` で
三角形 1 枚・1 軸あたり **38.4 項目**になり、上限 16 を超えます。**上限から導く形（既定）は最大 15.9 で収まります。**

#### 40 対（同じ手順。候補は計数、時間は 1 回ずつ別プロセス）

| 版 | 候補 / レイ 中央値 | 候補の比（中央値 / 最小 / 最大 / 総数の比） | 項目 / 三角形 / 軸（中央値 / **最大**） | 記憶の最大（source × 3 軸） | 出力 |
|---|---:|---|---:|---:|---|
| 従来（K = 0） | 230 | — | 2.8 / 3.2 | 3.6 MB | — |
| K = 256（固定） | **120** | 2.02 / 1.15 / 4.93 / 1.31 | 15.2 / **38.4** | 19.7 MB（`109576x384574`） | 40 / 40 バイト一致 |
| **上限 16（既定）** | **142** | **1.73** / 1.16 / 3.01 / 1.33 | 14.3 / **15.9** | 14.3 MB（`53754x1396886`） | **40 / 40 バイト一致** |

| 帯 | 候補 / レイ 中央値: 従来 → 上限 16 | 実務の分母の中央値（1 回） | 同 p95（1 回） |
|---|---:|---:|---:|
| 2,000〜5,000 | 162 → 101 | 0.95 → 0.67 s | 4.84 → 3.38 s |
| 15,000〜25,000 | 283 → 166 | 3.25 → 1.69 s | 14.29 → 7.84 s |
| 50,000〜100,000 | 220 → 126 | 6.10 → 5.87 s | 82.83 → 68.90 s |

**観測（計数）**: 候補は中央値で 1.73 倍減。**前判定を通過 / レイと寄与 / レイは 1 つも動かず、出力は 40 / 40 でバイト一致。**
総数の比が 1.33 なのは `110044x1146261`（全候補の 50%、本当に大きい三角形）が 1.10 倍しか減らないため。
**観測（時間、1 回ずつ）**: 分類（壁）85.0 → 56.6 s、実務の分母 290 → 235 s。
**ただし従来側の 290 s は、同じコードの前の実行（231 s）と 1.25 倍違います**（プロセスの間のばらつき）。
**時間の比は信用しないでください。効きは候補の計数で見ます。**

**評価**: 見積もり（7 分の 1）に対して実測 1.7〜2 分の 1。差は 2 つ。**上限 16 が小さい模型で K を 32〜100 に押さえる**
（帯 2k–5k で K = 256 なら 52 / レイまで下がる）ことと、**本当に大きい三角形は粗い段に残る**ことです。
**上限を「三角形あたり」でなく「絶対量（例: 1 軸 16 MB）」で置けば小さい模型で K が上がります**（推測。判断は仕様側）。

#### chain_frag（単発、深度 6。同一実行の A/B）

| 模型 | 版 | K（source × 軸） | 項目 | 候補 / レイ | 通過 / レイ | 寄与 / レイ | 出力 |
|---|---|---|---:|---:|---:|---:|---|
| `425318` | 従来 | — | 70,797 | 138.5 | 13.64 | 2.09 | |
| | 上限 16 | 32〜259 | 413,559 | **56.5** | 13.64 | 2.09 | バイト一致 |
| `73464` | 従来 | — | 18,530 | 55.6 | 6.28 | 1.57 | |
| | 上限 16 | 70〜182 | 62,162 | **22.3** | 6.28 | 1.57 | バイト一致 |
| `110044` | 従来 | — | 291,328 | 2,003.5 | 182.89 | 1.72 | |
| | K = 256 | 256 | 2,528,962 | 1,817.4 | 182.89 | 1.72 | バイト一致（床 183 = 本当に大きい三角形） |

### 上限を絶対量にした（2026-09-10。§5.10.14.17 の判断）

**`ray_index_fine_bytes`（source × 軸ごとのバイト数。既定 16 MB）。** この上限を満たす最大の K を模型・軸ごとに導きます。
同じ 40 対、候補は計数。**出力は 40 / 40 でバイト一致。**

| 版 | 候補 / レイ 中央値 | 候補の比（中央値 / 最小 / 最大 / **総数**） | 記憶の最大（1 source・1 軸） |
|---|---:|---|---:|
| 従来 | 230 | — | 0.6 MB |
| K = 256 固定 | 120 | 2.02 / 1.15 / 4.93 / 1.31 | 3.3 MB |
| 上限 16 項目 / 三角形 | 142 | 1.73 / 1.16 / 3.01 / 1.33 | 2.4 MB |
| **絶対量 16 MB / 軸（既定）** | **66** | **3.25** / 1.29 / 6.33 / **4.06** | **8.8 MB**（`53754x1396886`） |

| 帯 | 候補 / レイ 中央値: 従来 → 絶対量 |
|---|---:|
| 2,000〜5,000 | 162 → **49** |
| 15,000〜25,000 | 283 → **80** |
| 50,000〜100,000 | 220 → **71** |

**観測**: 絶対量にすると候補は中央値で 3.25 分の 1、総数で 4.06 分の 1（`110044` を含めて）。
1 source・1 軸の記憶の最大は 8.8 MB で、上限 16 MB の中（40 対の 6 索引の合計の最大は 52.6 MB）。
**評価**: 仕様側の「上限は制約の単位（絶対量）で置く」が実測で裏付けられました。相対上限（項目 / 三角形）は
小さい模型で K を押さえていました（帯 2k–5k で 101 → 49）。**§34 の見積もり（7 分の 1）に対して 3〜4 分の 1**。
残りは本当に大きい三角形（K を超える）です。

**`425318` 単発（`chain_frag`、同一実行）**: K = 32〜609、候補 138.5 → 48.7（2.84 倍）、通過 13.64 / 寄与 2.09 は不変、バイト一致。

### 断片が多い型の分布と、裾の対で深度を振った（2026-09-10。§5.10.14.19「次の測定 2 つ」）

#### 型の分布（40 対。既存の結果から。計算ゼロ）

型は「割り当て / 面が中央値（12.5）の 2 倍以上 → 割り当て型」「断片 / 割り当てが中央値（0.51）の 2 倍以上 → 切断型」で決めました。

| 型 | 対 | 裾に居る対 |
|---|---:|---|
| 普通 | 22 | — |
| **割り当て型**（葉にまたがる） | **13** | 帯 2k–5k の裾 2 対、帯 15k–25k の裾（`84737`） |
| 切断型（局所 BSP） | 4 | 帯 50k–100k の裾 2 対（`472008` / `109576`） |
| 両方 | 1 | `110044`（帯 15k–25k の最大） |

**観測**: 小さい帯と中の帯の裾は割り当て型、大きい帯の裾は切断型。**時間との log 相関は、割り当て / 面 0.12、
断片 / 割り当て 0.61、断片 / 面 0.50**（40 対全体）。

#### 深度を振った（裾 5 対 + 中央値 3 対、深度 5〜8。計数。時間は 1 回ずつ）

| 対 | 型 | 断片 / 面: 深度 5 / 6 / 7 / 8 | 局所 BSP の切断（M） | 割り当て / 面 | 実務の分母（1 回） |
|---|---|---|---|---|---|
| `296803x117525` | 割り当て型 | 102.8 / 74.5 / **66.9** / 75.8 | 1.8 / 1.8 / 2.3 / 4.2 | 19 / 52 / 193 / **844** | 4.48 / 3.11 / **2.69** / 4.25 s |
| `84737x1146165` | 割り当て型 | 53.9 / 40.5 / 33.5 / **32.7** | 4.6 / 4.3 / 5.3 / 8.5 | 8 / 17 / 59 / 275 | 11.45 / 7.71 / **6.27** / 7.72 s |
| `110044x1146261` | 両方 | 321.7 / 189.0 / 103.1 / **76.5** | 18.0 / 13.4 / 10.9 / 13.9 | 13 / 26 / 73 / 288 | 214.95 / 76.35 / 36.53 / **26.65** s |
| `472008x1451898` | 切断型 | 113.4 / 83.4 / 59.9 / **40.7** | 33.5 / 29.2 / 24.8 / 24.9 | 3 / 4 / 6 / 11 | 442.24 / 121.25 / 63.31 / **36.64** s |
| `109576x384574` | 切断型 | 42.7 / 21.4 / 15.0 / **13.1** | 12.5 / 6.5 / 5.5 / 6.6 | 4 / 6 / 10 / 23 | 53.15 / 14.50 / **11.69** / 16.75 s |
| `1628449x1681123` | 中央値 | 20.2 / 9.3 / 6.3 / **6.0** | 2.5 / 1.1 / 0.8 / 0.9 | 5 / 6 / 10 / 21 | 12.96 / 2.26 / **1.61** / 2.06 s |
| `95476x107007` | 中央値 | 27.6 / 19.7 / **18.5** / 23.9 | 0.6 / 0.6 / 0.8 / 1.5 | 9 / 19 / 54 / 186 | 0.97 / **0.71** / 0.76 / 1.48 s |
| `104442x252119` | 中央値 | 15.1 / 7.3 / 4.3 / **3.2** | 7.4 / 2.7 / 1.3 / 0.8 | 3 / 4 / 4 / 4 | 23.77 / 8.18 / 5.68 / **5.51** s |

出力のハッシュは深度ごとに違います（分割が違うので当然。正しさは別に検査済み）。

**観測 1**: **深度を上げると断片 / 面は減ります**（深度 5 → 7 で全 8 対）。割り当て / 面は増えるのに、です。
**観測 2**: 深度 8 では割り当て型が反転します（`296803` 66.9 → 75.8、`95476` 18.5 → 23.9。割り当て / 面が 800 超）。
切断型は 8 でも減り続けます（`472008` 59.9 → 40.7、時間 63 → 37 s）。
**観測 3**: **裾と中央値の差は深度を変えても残ります**（深度 8 の帯 15k–25k: `84737` 32.7 対 中央値 6.0、時間 7.72 対 2.06 s = 3.7 倍。深度 7 では 3.9 倍）。

**評価（導出 + 推測）**:
- **断片を決めているのは葉の中の局所 BSP の切断で、葉ごとのクリップではありません。** 深度を上げると葉あたりの平面が減って切断が減り、
  その効果が割り当ての増加を上回ります（深度 5 → 7）。**「割り当て型」という名前は代理でした**（割り当てが多い対は、
  面が大きく葉の中で多くの平面に切られる対でもある）。
- **深度は「分割の設定」として 1.5〜4 倍効きますが、型ごとに最適な深度が違います**（割り当て型 7、切断型 8 以上）。
  適応分割の閾値（`SPEC-phase2.md` §3.1.1 の 128）で型ごとに振る余地があります（推測）。
- **裾そのものは入力の性質です。** どの深度でも裾 / 中央値は 3.7〜3.9 倍で、深度では埋まりません。

### 局所 BSP の切断が実際に分割した割合と、O2 の前提（2026-09-10。§5.10.14.22。計数のみ）

**測る前の予測（推測。外れ方ごとの行き先を先に書く）**:

| 量 | 予測 | 根拠 | 行き先 |
|---|---|---|---|
| 切断の試行（断片 × 平面）のうち実際に分けた割合 | **5〜10%** | `split_fragment` の早期 return が 91〜95%（§5.10.11〜12 の実測）。試行の大半は片側で終わる | **10% 以下なら「返した候補のうち何割使うか」の 6 例目**で O3 に根拠。ただし早期 return は既に安い（移動で 0 確保）ので、**減るのは試行の費用ではなく「分けなくてよい平面で分けている」分**。**割合が大きければ断片は交差の数で決まり、O3 は効かない** |
| 切断平面に一度も分けられなかった断片の割合 | **50〜70%** | 早期 return 率と、割り当て / 面が中央値 12.5（多くの断片はセル平面のクリップだけ） | 大きければ O2 でレイがその分減る（裾には効かない）。小さければ O2 は効かず、裾は切断そのものへ |

**実測（40 対、深度 7。3 演算の和）**:

| 量 | 40 対の合計 | 対ごと（最小 / 中央値 / 最大） |
|---|---:|---|
| 切断の試行（断片 × 切断平面） | 1,300,754,428 | |
| **実際に分けた** | **19,837,092（1.5%）** | 0.0% / **3.7%** / 7.0% |
| 領域（= レイ） | 40,272,069 | |
| 切断平面に一度も分けられなかった断片 | 2,666,038（領域の 6.6%） | 1% / 37% / 100%（出力多角形比） |
| うち元の三角形の数 | 1,619,879 | |
| **O2 のレイ削減の上界**（未分割 − 三角形） | **1,046,159（領域の 2.6%）** | 0% / 5% / 34% |

| 対 | 帯 | 実際に分けた割合 | 未分割の断片（出力比） | レイ削減の上界（領域比） |
|---|---|---:|---:|---:|
| `1628449x1681123`（中央値） | 15k–25k | 5.4% | 40% | 15% |
| `84737x1146165`（裾） | 15k–25k | 2.1% | 10% | 6% |
| `110044x1146261`（最大） | 15k–25k | 1.1% | 6% | 4% |
| `472008x1451898`（裾） | 50k–100k | 0.8% | 1% | 0% |

**観測 1（6 例目）**: 切断の試行の 98.5% は分けていません（裾の対ほど低く 0.8〜2.1%）。予測（5〜10%）より無駄は大きい。
**観測 2**: O2 の前提は外れました。未分割の断片は領域の 6.6% で、レイの削減の上界は 2.6%（予測 50〜70% は誤り。
分母を「出力多角形」で取ると 16.7% ですが、レイは領域ごとなので領域比が正しい）。
**評価**: **O2 は効きません**（行き先どおり「裾は切断そのものへ」）。
**試行の 98.5% が無駄でも、その無駄は早期 return（確保なし）なので費用は小さい**（§5.10.12 の実測）。
**問いは「実際に分けた 1.5% が本当の交差か」に移ります。** 切断ループには「切る三角形の箱に触れない平面を飛ばす」
実験（`KRISITE_EXPERIMENT_BSP_SKIP_DISJOINT`。Phase 3 で保留）が既にあり、これが O3 の形です。次の測定はこの旗で。

### O3 の形の実験 — 切る三角形の箱に触れない平面を飛ばす（2026-09-11。`KRISITE_EXPERIMENT_BSP_SKIP_DISJOINT`。計数）

**Phase 3 で保留された実験**（`soup_boolean.hpp` の切断ループ。断片が切る三角形の箱の外にあるなら、その平面では切らない）
**を 40 対で回しました。** 判定は `fragment_outside_box`（断片の頂点と三角形の箱の厳密な比較）。

| 量 | 従来 | 実験 | 比 |
|---|---:|---:|---:|
| 切断の試行 | 1,300,754,428 | 499,983,010 | **2.60 分の 1** |
| **実際に分けた** | 19,837,092 | 10,084,957 | **1.97 分の 1** |
| 領域（= レイ） | 40,272,069 | 24,626,221 | 1.64 分の 1 |
| 出力多角形 | 15,921,882 | 8,982,505 | 1.77 分の 1 |
| 出力のハッシュが違う対 | — | **33 / 40** | 分割が変わるので当然。意味論の比較は下記 |

| 対 | 帯 | 実際に分けた: 従来 → 実験 | 領域 | 実務の分母（1 回ずつ） |
|---|---|---:|---:|---:|
| `1628449x1681123`（中央値） | 15k–25k | 2.23 分の 1 | 1.75 分の 1 | 1.57 → 1.50 s |
| `296803x117525`（裾） | 2k–5k | 1.41 分の 1 | 1.30 分の 1 | **2.83 → 6.35 s（悪化）** |
| `84737x1146165`（裾） | 15k–25k | 1.35 分の 1 | 1.30 分の 1 | 9.80 → 6.53 s |
| `110044x1146261` | 15k–25k | 1.72 分の 1 | 1.67 分の 1 | **37.53 → 45.48 s（悪化）** |
| **`472008x1451898`（裾）** | 50k–100k | **3.53 分の 1** | **3.26 分の 1** | 64.57 → 20.63 s |
| `109576x384574`（裾） | 50k–100k | 2.44 分の 1 | 1.94 分の 1 | 11.94 → 8.59 s |

**観測 1**: 実際に分けた 19.8 M のうち**半分（9.8 M）は、切る三角形の箱に触れない断片を切ったもの**でした。
切断型の裾（`472008`）では 3.5 分の 1。**「断片は本当の交差の数で決まる」は半分だけ正しい**（残り半分は箱にも触れない平面による切断）。
**観測 2**: 時間は 1 回ずつで信用できませんが、**2 対で悪化**しています。実験の判定は厳密な述語（断片の頂点 × 三角形の箱）で、
割り当て型（断片が小さく多い）では判定の費用が切断の削減を上回ると見ます（推測）。**採るなら安い判定（整数の箱どうし）が要ります。**
**観測 3（Phase 3 の保留理由）**: 「共平面に載る別々の多角形が違う切り方をして `region_key` が潰せなくなる」懸念は未証明のまま。
出力の一致は意味論の水準（体積・三角形数・位相）で下記に。

#### 意味論の比較（従来 対 実験。同じ 40 対、体積と三角形数の列を足して回し直し）

| 検査 | 結果 |
|---|---|
| 3 演算 × 40 対 = 120 の出力の 6 倍体積（浮動小数の篩） | **相対差の最大 3.1e-13**。1e-9 を超える差 0 件 |
| 位相（辺 / 頂点多様体・向き・退化なし・χ 偶数） | 従来 40 / 40、実験 40 / 40 |
| 三角形数が違う対 | 33 / 40（分割が変わるので当然） |
| 体積の恒等式の誤差（`vol_err` の最大） | 従来と実験で同じ値 |

**観測**: 出力は分割が違うだけで、体積と位相は一致します（篩の水準）。**Phase 3 の懸念（`region_key` が潰せなくなる）は
この 40 対では発火していません。ただし「発火していない」であって証明ではありません。**

**時間（1 回ずつ。同じ時間帯に連続で実行）**: 実務の分母の合計 216.3 → 124.4 s（**1.74 倍**）、対ごとの比 中央値 1.27、
最大 3.36（`472008x1451898`）、遅くなった対 1（`104442x252119`、0.91）。**前の実行で悪化した 2 対（`296803` / `110044`）は
今回は悪化していません**（1 回ずつなのでばらつきの範囲。判定の費用の問題は残る）。

| 帯 | 実務の分母の p95: 従来 → 実験 | 中央値 |
|---|---:|---:|
| 2,000〜5,000 | 3.38 → 2.31 s | 0.69 → 0.55 s |
| 15,000〜25,000 | 6.64 → 5.08 s | 1.59 → 1.21 s |
| 50,000〜100,000 | **65.35 → 19.44 s** | 5.21 → 4.58 s |

**評価**: **裾に効く機構は、この形（切る三角形が触れない断片を切らない）です。** 切断型の裾（帯 50k–100k）で p95 が 3.4 分の 1、
割り当て型の裾では 1.3 分の 1。**採るには (1) `region_key` の懸念の証明か、それを突くコーパスケース、(2) 安い判定
（整数の箱どうし。今は断片の頂点の厳密な比較）の 2 つが要ります。** 実験の旗のままでは既定に入れられません。

### O3 の条件 2 — 安い前判定（2026-09-11。§5.10.14.24。実行時の選択肢 `bsp_skip_disjoint`。**既定 0**）

**形**: 断片は「多角形の箱 ∩ セルの箱」（整数）の中にあるので、切る三角形の箱がそれと交わらなければ飛ばす（値 1）。
交わるときだけ、断片の頂点と三角形の箱の厳密な比較（Phase 3 の実験）に回す（値 2）。**判定は超集合を切るだけなので、
分類の健全性は変わりません。** 同じ 40 対、計数。時間は 1 回ずつ（同じ時間帯に連続）。

| 版 | 切断の試行 | 実際に分けた | 領域 | 出力多角形 | 飛ばした（箱 / 厳密） | 従来との 6 倍体積の相対差の最大 | 実務の分母（1 回） |
|---|---:|---:|---:|---:|---|---:|---:|
| 従来（0） | 1,300,754,428 | 19,837,092 | 40,272,069 | 15,921,882 | — | — | 216.3 s |
| 実験（旗。頂点の厳密比較のみ） | 499,983,010 | 10,084,957 | 24,626,221 | 8,982,505 | — | 3.1e-13 | 124.4 s |
| **O3 = 1（整数の箱だけ）** | 637,821,545 | 11,715,691 | 27,486,372 | 10,029,638 | 23,828,211 / 0 | 2.1e-13 | 134.3 s |
| **O3 = 2（箱 + 厳密）** | 499,983,010 | 10,084,957 | 24,626,221 | 8,982,505 | 23,828,211 / 8,377,052 | 3.1e-13 | 133.4 s |

**観測 1**: O3 = 2 は実験（旗）と**出力のハッシュが 40 / 40 で一致**し、計数も同じ（同じ判定を、安い箱を先に置いて到達している）。
**観測 2**: 整数の箱だけ（値 1）で、飛ばす（断片 × 平面）の **74%**（23.8 M / 32.2 M）を捕まえます。厳密な比較が要るのは残り 26%。
実際に分けた数は 従来 19.8 M → 値 1 で 11.7 M → 値 2 で 10.1 M。
**観測 3**: 時間（1 回ずつ）は値 1 と 2 でほぼ同じ（134 / 133 s）。厳密な比較の費用は、飛ばす量で相殺されている（推測）。

| 帯 | p95: 従来 / 実験 / O3 = 1 / O3 = 2 |
|---|---|
| 2,000〜5,000 | 3.38 / 2.31 / 2.42 / 2.20 s |
| 15,000〜25,000 | 6.64 / 5.08 / 5.38 / 5.09 s |
| 50,000〜100,000 | 65.35 / 19.44 / 24.35 / 23.38 s |

**評価**: 条件 2 は満たしました（安い判定で実験と同じ結果。ctest 47 / 47）。**既定は条件 1（`region_key` の証明かコーパスケース）
が済むまで 0 のまま。** 値 1 と 2 の選択は、厳密な比較の費用が問題になったときの退避として 1 を残す形。


### 累積の測り直し（2 回目。2026-09-11。同じ日・同じ機械で 旧 → 新・既定 → 新 + O3 を連続）

**所有者の依頼 4。** 前回（09-10）と同じく、**先に旧コード（`973c47a`）を今日の機械で回しました**: 実務の分母の合計は
09-10 の 402 s に対して今日 **615 s（1.53 倍遅い）**。同じコードです。**前回の数字と直接比べてはいけません。**
設定は同じ 40 対、深度 7、8 スレッド、-O3、NSI 宣言あり。**新・既定** = レイ索引の細かい割り当て（絶対量 16 MB）あり、O3 なし。
**新 + O3 = 2** は条件 1 待ちで既定ではありません（参考）。時間は 1 回ずつ。

| 帯 | 対 | 中核 1 演算の幾何平均: 旧 / **新** / 新 + O3 | 比（**新** / 新 + O3） | 実務の分母の p95: 旧 / **新** / 新 + O3 | 比 | 実務の分母の合計 | 比 |
|---|---:|---|---|---|---|---|---|
| 2,000〜5,000 | 10 | 236.2 / **116.0** / 111.3 ms | **2.04** / 2.12 | 5.37 / **3.71** / 4.06 s | **1.45** / 1.32 | 14 / **10** / 10 s | **1.46** / 1.42 |
| 15,000〜25,000 | 20 | 916.5 / **386.0** / 296.5 ms | **2.37** / 3.09 | 16.95 / **9.84** / 6.63 s | **1.72** / 2.56 | 230 / **119** / 77 s | **1.94** / 2.97 |
| 50,000〜100,000 | 10 | 3,510.3 / **1,717.2** / 785.6 ms | **2.04** / 4.47 | 238.48 / **99.53** / 19.03 s | **2.40** / 12.53 | 370 / **182** / 59 s | **2.03** / 6.33 |
| 全体 | 40 | 913.5 / **415.1** / 296.1 ms | **2.20** / 3.09 | 44.02 / **18.68** / 7.99 s | **2.36** / 5.51 | 615 / **311** / 146 s | **1.98** / 4.21 |

| 量 | 旧 | **新・既定** | 新 + O3 |
|---|---:|---:|---:|
| 実務の分母の中央値 | 4.64 s | **2.62 s** | 1.88 s |
| 対ごとの比（旧 ÷）: 最小 / 中央値 / 最大 | — | 1.07 / **1.44** / 3.09（40 / 40 が速い） | 1.02 / 2.35 / 12.53（40 / 40） |
| 失敗 / 停止 | 0 / 0 | 0 / 0 | 0 / 0 |

#### 段の内訳（40 対の合計、壁時計）

| 版 | 入口 | 中核 | 出口 | 実務の分母 | 割合（入口 / 中核 / 出口） |
|---|---:|---:|---:|---:|---|
| 旧 `973c47a` | 15 s | 462 s | 137 s | **615 s** | 2 / 75 / 22% |
| **新・既定**（A あり、O3 なし） | 15 s | 168 s | 128 s | **311 s** | 5 / 54 / 41% |
| 新 + O3 = 2（既定ではない） | 12 s | 86 s | 49 s | **146 s** | 8 / 59 / 33% |

**観測**: 前回の累積（09-10: 中核 2.06 倍、合計 1.51 倍）から、**新・既定で中核 2.20 倍、合計 1.98 倍、p95 2.36 倍**に伸びました
（レイ索引の A のぶん）。**O3 を入れると中核 3.09 倍、合計 4.21 倍、p95 5.51 倍**（帯 50k–100k の p95 は 238 → 19 s）。
`110044x1146261` は 実務の分母 124 → 58（新）→ 31 s（新 + O3）で、中核 1 演算 35.3 → 11.7 → 6.7 s。
**評価**: O3 なしでは出口が 41% で最大の段。O3 を入れると出口の絶対量も減り（137 → 49 s。断片が減るため）、中核 59% に戻ります。
**目標との距離（帯 15k–25k の p95、実務の分母）**: 旧 16.95 s → 新 9.84 s → 新 + O3 6.63 s（今日の機械。09-10 の機械なら約 1.5 分の 1）。


### O3 あり（既定 2）の段の内訳（2026-09-11。§5.10.14.27「O3 ありを前提に刻む」。`ember_cum_o3` の結果から。計算ゼロ）

**実務の分母 146.0 s = 入口 11.6 + 中核 85.6 + 出口 48.7**（中核 = arrange 37.6 + 分類 26.5 + 縫合 6.5 + 前処理 / 葉の列挙 15.0）。
分類と arrange の内訳は CPU の比で壁時計を按分（分類の並列効率 0.97）。

| 段 | 40 対の合計 | 割合 |
|---|---:|---:|
| 出口 | 48.7 s | 33.4% |
| arrange: 断片の生成 | 23.3 s | 16.0% |
| 分類: レイ | 21.8 s | 14.9% |
| 前処理 + 葉の列挙 | 15.0 s | 10.3% |
| 入口 | 11.6 s | 8.0% |
| arrange: 準備 | 6.6 s | 4.5% |
| 縫合（大域） | 6.5 s | 4.5% |
| arrange: 収集 + 存在判定 + 共平面 | 4.7 s | 3.2% |
| 分類: 代表点 | 4.0 s | 2.7% |
| arrange: 縫合（葉ごと） | 3.0 s | 2.1% |
| 分類: 準備 + 判定 | 0.8 s | 0.5% |
| **和** | **146.0 s** | 100.0% |

分類の中（CPU）: 準備 1.9 / 代表点 15.0 / **レイ 82.0** / 判定 1.0%。
arrange の中: 収集 6.4 / 存在判定 3.9 / 準備 17.5 / **断片の生成 62.1** / 共平面 2.1 / 縫合（葉） 8.0%。
レイ 24,626,221 本、候補 131.9 / レイ（O3 で断片が減り、レイあたりの候補も 554 → 132）。

**観測**: O3 ありでは出口が最大（33%）、次が断片の生成と分類のレイ。**O3 なしの構成（出口 36.5 / レイ 24.5 / 断片の生成 15.5%）と
順位は同じで、レイの割合が下がりました**（候補が減ったため）。

### 断片の生成の中を刻んだ（2026-09-11。§5.10.14.30。O3 = 2 の既定で。対照に O3 = 0）

**`BoolOptions::measure_frag`（既定 偽）の下で、多角形ごとに 4 区分の CPU を足しました**（全スレッドの和）。
同じ 40 対、深度 7。壁時計は CPU の比で按分。

| 版 | 実務の分母 | 断片の生成（壁） | 同 CPU の和 | クリップ | 準備（切断集合） | **切断ループ** | 確定 | 切断の試行 | 実際に分けた | 試行 1 回の平均 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| O3 = 0（対照） | 208.0 s | 36.7 s（17.7%） | 243.2 s | 5.8% | 5.4% | **88.5%** | 0.3% | 1,300,754,428 | 19,837,092 | 166 ns |
| **O3 = 2（既定）** | 122.6 s | 18.1 s（14.8%） | 129.9 s | 10.8% | 9.9% | **78.9%** | 0.4% | 499,983,010 | 10,084,957 | 205 ns |

**O3 = 2 の切断ループの中身**（計数）: 試行 500 M = 箱で飛ばした 23.8 M（4.8%）+ 厳密で飛ばした 8.4 M（1.7%）+
**`split_fragment_into` に回った 468 M（93.6%）。そのうち実際に分けたのは 10.1 M（2.2%）で、残り 97.8% は早期 return。**

**観測**: 断片の生成の「大きな 1 つ」は切断ループで、その中身は**分けない試行**です（早期 return が 4.58 億回）。
試行 1 回の平均 205 ns は、早期 return でも各頂点の構成点を取得して `side` を評価する費用（構成点キャッシュの探索 35〜116 ns × 頂点数）と
整合します（推測。§5.10.11.4 の単価から）。
**評価**: 「返した候補のうち何割使うか」の **7 例目**の形です。切断集合の平面のうち、その多角形を実際に分けるのは 2.2%。
**多角形の箱（整数）が平面の片側に完全にあれば、頂点を評価せずにその多角形のすべての断片で飛ばせます**（超集合の判定なので出力は不変）。
次の測定は「箱が片側に完全にある試行の割合」（計数）。

> **駆動の既定の罠**: 最初の実行は駆動の第 12 引数の既定が 0 で、ライブラリの既定（2）を上書きしていました。
> 設定表の先頭に「O3 = 0」と出ていたので気づき、引数が無ければライブラリの既定を使う形に直しました。O3 = 0 の結果は対照として残しています。

### 7 例目 — 多角形の箱が平面の片側なら、頂点を評価せずに飛ばす（2026-09-11。§5.10.14.30）

**形**: 切断ループで平面 $q$ を見るとき、多角形の箱 ∩ セルの箱（整数）の 2 つの極値の隅（$q$ の法線の向きで選ぶ）の `side` が
同じ非零の符号なら、その多角形のすべての断片は片側にあり、`split_fragment_into` は全断片で早期 return する。
**同じ判定を整数点 2 つで済ませ、頂点（構成点キャッシュの探索）を触らない。** 出力は不変（判定は超集合）。
`BoolOptions::bsp_skip_boxside`（既定 真。偽で従来 = 正解器）。同じ 40 対、O3 = 2、計数。時間は 1 回ずつ（連続）。

| 版 | `split` に回った試行 | 箱で飛ばした（断片 × 平面） | 実際に分けた | 切断ループ CPU | 断片の生成 CPU | 断片の生成（壁） | 実務の分母（1 回） |
|---|---:|---:|---:|---:|---:|---:|---:|
| O3 = 2（前） | 499,983,010 | 0 | 10,084,957 | 102.5 s | 129.9 s | 18.1 s | 122.6 s |
| **箱が片側なら飛ばす** | **275,954,741** | 331,410,206 | 10,084,957 | **59.6 s** | **86.6 s** | 12.4 s | 115.8 s |

**観測**: 実際に分けた数は 1 つも変わらず、出力は 40 / 40 でバイト一致。`split` に回る試行が 1.81 分の 1、切断ループの CPU が 1.72 分の 1、
断片の生成の CPU が 1.50 分の 1。実務の分母は 1.06 倍（対ごとの中央値 1.04、35 / 40 が速い。1 回ずつなのでばらつきの範囲）。
**評価**: 断片の生成（実務の分母の 14.8%）の中の「大きな 1 つ」（切断ループの早期 return）に、7 例目の前判定が効きました。
**残る 2.76 億回の試行は、箱が平面をまたぐ多角形の断片**です。次の刻みは断片ごとの箱（切られるほど小さくなる）か、
頂点の評価そのもの（構成点の取得）の単価です。

### 前処理と葉の列挙を刻んだ（2026-09-11。§5.10.14.33。計数と時計だけ。O3 = 2 + 7 例目の既定で）

**前処理に 6 つ、葉の列挙の「数える」呼び出しに 1 つの時計を置きました**（逐次なので壁時計 = CPU。既定の経路に時計 7 回）。
同じ 40 対。実務の分母 117.6 s（入口 10.8 / 中核 64.0 / 出口 42.8）。

| 段 | 40 対の合計 | 実務の分母の中 | 中身 |
|---|---:|---:|---|
| **前処理** | 8.8 s | 7.5% | 下表 |
| 葉の列挙 | 3.1 s | 2.6% | **「数える」呼び出しが 98.9%**（3.0 s） |

| 前処理の区分 | 秒 | 前処理の中 | 実務の分母の中 |
|---|---:|---:|---:|
| **多角形の複製と平面表の統合** | **7.4 s** | **84.5%** | **6.3%** |
| 三角形の平面の intern | 0.6 s | 7.1% | 0.5% |
| レイ索引の構築（source × 3 軸。A の細かい割り当て込み） | 0.6 s | 7.4% | 0.6% |
| レイ用の平面の複製 / 切断平面の集合 / 三角形の箱 | 0.0 s | 0.2% | 0.0% |
| 和 | 8.7 s | 99.1% | |

**葉の列挙の「数える」呼び出し**: 132,744 回（訪れたセル）× **全多角形の走査**（1 回あたり 26,919 個）= 35.7 億回の箱の判定。
1 判定 0.9 ns（分岐だけ）。**セルごとに全多角形を走査する $O(\text{セル} \times \text{多角形})$ の形**です。

**観測**: 前処理の「大きな 1 つ」は多角形の複製と平面表の統合（`Poly` の複製 + 辺平面の付け替え。7.4 s = 実務の分母の 6.3%）。
葉の列挙は 2.6% で、その全部が「数える」走査。**評価**: どちらも実務の分母の 7% 以下で、「大きな 1 つ」（10% 超）はありません。
複製は入力の多角形数に比例し、O3 の影響を受けません（推測: `Poly` 104 バイト × 辺の `std::vector` の複製）。
数える走査は $O(\text{セル} \times \text{多角形})$ ですが、1 判定 0.9 ns で 3.0 s に収まっています。

### 出口の 5 段（2026-09-11。§5.10.14.33 の次。O3 = 2 + 7 例目の既定。`ember_pre` の結果から。計算ゼロ）

実務の分母 117.6 s、出口 42.8 s（36.4%）。5 段の和 42.7 s（計上漏れなし）。

| 出口の段 | 40 対の合計 | 出口の中 | 実務の分母の中 |
|---|---:|---:|---:|
| **接触の分裂** | **13.4 s** | **31.4%** | **11.4%** |
| 平面索引（整列込み） | 10.2 s | 23.9% | 8.7% |
| 値で併合 | 9.1 s | 21.2% | 7.7% |
| 構成点 | 7.6 s | 17.8% | 6.5% |
| 扇（T 解決 + 三角形化） | 2.4 s | 5.5% | 2.0% |

**観測**: O3 の前（分裂 35.1 / 索引 24.7 / 併合 17.7 / 構成点 15.8 / 扇 6.6%）と順位は同じ。単独で 10% を超えるのは接触の分裂だけ（11.4%）。

#### 接触の分裂の中（既存の 5 つの時計を駆動の列に足した。同じ 40 対）

接触の分裂 13.6 s（実務の分母の 11.6%）。5 区分の和 11.7 s（**未計上 1.9 s = 14%。時計の外の複製など**）。

| 区分 | 秒 | 分裂の中 | 実務の分母の中 |
|---|---:|---:|---:|
| **辺 → 三角形 / 頂点 → 三角形の表**（逐次） | **5.1 s** | 37.5% | 4.3% |
| 診断と成分分け（逐次。次数 3 以上の辺のまわりの連結成分） | 3.1 s | 22.8% | 2.6% |
| 分裂後の多様体性の検査（`unresolved` に要る。契約の一部） | 2.1 s | 15.3% | 1.8% |
| 扇の計算（**並列**） | 1.5 s | 10.9% | 1.3% |
| ID の割り当てと書き戻し | 0.0 s | 0.0% | 0.0% |

**観測**: 出口の最大の段（接触の分裂）の中も、最大の区分は 4.3%（辺と頂点の表。逐次）で、並列の扇は 1.3% だけ。
**評価**: 出口の 5 段（6.5 / 7.7 / 8.7 / 2.0 / 11.4%）の中に「大きな 1 つ」（10% 超の単独の項）はありません。
接触の分裂を刻んでも、単独で 5% を超える項がありません。**出口は「小さい逐次の段が並ぶ」形**です（推測: 逐次の表の構築が並ぶので、
並列化より「表の作り方（`std::map` か整列か）」の切り口）。

### 目標の議論の起点 — O3 + 7 例目の既定での中央値（2026-09-11。§5.10.14.35。既存の 3 実行から。計算ゼロ）

同じ 40 対、深度 7、8 スレッド、-O3、NSI 宣言あり。**今日の機械**（09-10 より 1.5 倍遅い状態。`BENCH.md`「累積の測り直し（2 回目）」）。
実務の分母 = 入口 + 中核 + 出口。3 実行はいずれも O3 = 2 + 7 例目（同じコード。プロセスの間のばらつきの目安）。

| 帯（対の合計面数） | 中央値（実務の分母）: 3 実行 | p95: 3 実行 | 中央値（駆動込み） |
|---|---|---|---|
| 2,000〜5,000 | 0.55 / 0.54 / 0.54 s | 2.23 / 2.14 / 2.13 s | 0.75〜0.77 s |
| **15,000〜25,000** | **1.13 / 1.22 / 1.22 s** | **4.83 / 4.70 / 4.72 s** | 1.57〜1.59 s |
| 50,000〜100,000 | 3.42 / 4.05 / 4.07 s | 16.24 / 17.94 / 17.92 s | 4.92〜4.99 s |

**観測**: 帯 15k–25k の中央値は 1.13〜1.22 s（O3 前は 1.69 s）、p95 は 4.7〜4.8 s（O3 前は 7.05 s）。
今日の機械は 09-10 の状態より 1.53 倍遅いので、**09-10 の状態なら中央値 約 0.8 s、p95 約 3.1 s に当たります**（推測。同じ旧コードの比から）。
**評価**: 今日の機械で中央値は目標（1 s）の 1.1〜1.2 倍、09-10 の機械なら中央値は目標の中。**p95 は今日で 4.7 倍、09-10 なら約 3.1 倍。**
「20,000 面の対の p95 が 1 秒」との距離は、中央値ではほぼ届き、裾で 3〜5 倍残る、という形です。

### CP1 + CP2 の回し直し（2026-09-11。§5.10.14.37。**回す前の予測**）

**設定**: `cp1.txt`（1000 模型 = 500 対）と `cp2.txt`（200 模型 = 100 対。自己交差を含む対）を**同じ実行**（同じバイナリ・同じ連鎖）で。
深度 6（適応）、8 スレッド、$b = 21$、-O3、**検算 ON**（`verify_index = 1`、`verify_split_delta = 1`）、NSI は 1（検査して通ったものだけ宣言）、
O3 = 2、7 例目 = 真、`alloc_reuse = 7`、レイ索引の絶対量の上限 16 MB / 軸、縫合は葉ごと。
**ハッシュはこの版で採り直します**（旧版とは出力の順序が違う。§5.10.14.5）。旧結果は `*_results.pre_o3.txt` に退避。

**予測（推測。仕様側の指定どおり、外れ方の行き先つき）**:

| 量 | 予測 | 外れ方の行き先 |
|---|---|---|
| CP1 の失敗 | **0**（radial sort で解けたはず） | 1 以上なら新しい配置か回帰。対を特定して機構まで降りる |
| CP2 の失敗 | **0 か 1**（頂点の水準の 1 件だけ: `242231x38631` の ∪） | 2 以上なら頂点の水準が複数ある |
| `unresolved_post` の合計 | 頂点の水準の件数と一致（0 か 1） | 不一致なら別の原因 |
| 時間 | 合計 約 2 時間（前回 7.3 時間。改善後の比 3〜4 倍から） | 4 時間超なら裾の対で何かが起きている |

**そして数えること**: 頂点の水準の残件（`unresolved_post` / `unsplit_edges`）が実データ全体で何件か。

**結果（2026-09-12。同じ連鎖で CP1 → CP2。設定表は駆動が先頭に出力。旧結果は `*_results.pre_o3.txt`）**

| | 対 | 成功 | **失敗** | `unresolved` / `unresolved_post` / `unsplit_edges` | `degenerate_kept` | radial（試行 / 解決） | `split_vertices` | `excluded_ops` | 空の出力 | 時間 |
|---|---:|---:|---:|---|---:|---|---:|---:|---:|---:|
| **CP1** | 500 | 500 | **0** | 0 / 0 / 0 | 0 | 16 / 16 | 42 | 0 | 116 | 8,416 s（中央値 2.80 s、最大 424 s） |
| **CP2** | 84（200 模型のうち 168 が量子化を通過） | 82 | **2** | **3 / 3 / 0** | 0 | 701 / 701 | 3,036 | 3 | 24 | 6,388 s（中央値 7.54 s、最大 793 s） |

| 予測 | 実測 | 行き先 |
|---|---|---|
| CP1 の失敗 0 | **0**（当たり。旧結果の唯一の FAIL `135071x75653` が ok に） | — |
| CP2 の失敗 0 か 1 | **2**（`242231x38631` 1 本、`302390x40893` 2 本。どちらも「辺多様体でない」、旧結果でも FAIL） | **頂点の水準が複数ある**（2 対 3 本） |
| `unresolved_post` の和 = 頂点の水準の件数 | **3 = 3**（当たり。`unsplit_edges` は 0） | — |
| 時間 約 2 時間 | **4.1 時間**（CP1 2.3 h + CP2 1.8 h） | 「4 時間超なら裾で何か」の境界。**予測は検算 ON の費用（多様体性の検査、分裂の予測との突き合わせ）を勘定に入れていませんでした**（推測。40 対の検算 OFF の比から見積もったため） |

**観測 1**: 実データ全体（584 対）で、失敗は頂点の水準の 2 対だけ。**CP2 の旧結果（09-07）の 12 FAIL のうち 10 が ok になりました**
（radial sort と一般解の効果）。残る 2 対は radial sort が全部（153 / 153、72 / 72）解けた上で、それぞれ 1 本・2 本の辺が残ります。
**観測 2**: **頂点の水準の残件は実データ全体で 2 対 3 本**（`unresolved_post` の和と一致。別の原因の `unsplit_edges` は 0）。
除外の機構（`excluded_ops` 3）が 3 演算で発火しています。
**観測 3**: ハッシュはこの版で採り直しました（旧版とは順序が違う）。全対の状態（ok / FAIL）は上の 11 対以外で旧結果と同じ。
**評価**: 性能改善の 6 件（radial sort、一般解、O3、7 例目、aabb、early-out）は実データ全体で正しさを壊していません。
残件 3 本を「解くか、契約の範囲外として記録するか」は仕様側の判断です。

### CP2 の本体の見積もりを立て直した（2026-09-12。§1.5.1「飽和で区切る」の形で。計算ゼロ）

**材料**: 84 対の実測（6,388 s、検算 ON、O3 = 2 + 7 例目、深度 6、8 スレッド、今日の機械）と、
`data/thingi10k/metadata/geometry_data.csv`（Thingi10K の 9,997 模型の面数と自己交差の数）。

#### ★ 対の数の訂正 — 母集団は 1,616 対（「約 2,200 対」は帯の【無い】数）

| 母集団 | 模型 | 対 |
|---|---:|---:|
| 自己交差あり（帯なし） | 4,524 | **2,262**（`SPEC-phase5.md` §1.5 の「約 2,200」はこれ） |
| **同、帯（1,000〜100,000 面）の中** | **3,232** | **1,616**（同 §4.1 の「帯を切ると約 3,232」はこれ） |
| 帯の外 | 1,000 面未満 596、100,000 面超 696（最大 567 万面） | — |

**§1.5 の表と §4.1 の記述が、別の母集団を指しています。** 帯は「自己交差の効果と規模の効果を混ぜない」ために課したので、
**本体は 1,616 対**と見ます（判断は仕様側）。量子化の通過率（実測 168 / 200 = 84%）を掛けると **約 1,360 対**。

#### 見込み（推測。段 1。2 通りの方法で同じ値）

84 対に $T \approx k\,n^{b}$ を当てはめ（$b = 1.42$、$k$ は 84 対の合計が実測に一致するよう較正）、
母集団の模型を面数の昇順に 2 つずつ組んで（駆動と同じ）足しました。

| 方法 | 本体（約 1,360 対）の見込み |
|---|---:|
| 冪の当てはめ（$n^{1.42}$） | **29.8 時間** |
| 分位点の対応づけ（84 対の分布を引き伸ばす。母数を仮定しない） | **28.7 時間** |

**旧見積もり（§1.5 の 100.7 時間 / 2,200 対）との比較**: 1 対あたり 165 s → **79 s（2.1 倍）**、対の数 2,200 → 1,360。
**合わせて 3.4 倍**（内訳: 性能改善 2.1 倍 × 対の数 1.6 倍）。

#### ★ 飽和で区切る運用の目安（昇順に回すので、先頭 K 対の累積）

| 先頭 K 対 | 対の面数の上限 | 累積の見込み |
|---:|---:|---:|
| 100 | 3,036 | 0.03 h |
| 400 | 7,596 | 0.3 h |
| 800 | 25,594 | 2.1 h |
| 1,000 | 49,184 | 5.1 h |
| 1,200 | 102,374 | 13.1 h |
| **1,360（全部）** | 200,000 | **29.8 h** |

**最後の 160 対（12%）が全体の 56% を占めます。** 途中で打ち切る運用が効く形です。

> **★ ただし、飽和の判定を小さい対だけで下せません。** 84 対で失敗が出たのは**面数の上位 3 割**
> （分位点 0.68 と 0.79）でした。本体に写すと **922 対目（累積 3.6 h）と 1,068 対目（同 7.0 h）**に当たります。
> **「新しい失敗の種類が出なくなるまで」を判定するには、少なくとも 1,100 対（約 8 時間）が要ります**（推測）。

#### 見込みの弱点（先に書きます）

| 弱点 | 向き |
|---|---|
| 冪の当てはめが裾を過小に見る（上位 5 対の占める割合: 実測 42.6% 対 予測 35.4%） | **見込みは低め** |
| 84 対は 200 模型の標本。母集団の面数の分布は近いが同じではない（中央値 7,006 対 9,348） | 見込みは低め |
| 今日の機械は 09-10 の状態より 1.53 倍遅い（同じ旧コードの比） | 機械が速ければ短くなる |
| 量子化の通過率 84% は 200 模型の実測。母集団で同じとは限らない | 未確認 |

#### 実行の前に要るもの

**母集団 3,232 模型のうち、手元に `.kmesh` があるのは 200 だけです。3,032 模型の取得と量子化が要ります**
（`tests/thingi10k/fetch.py`）。取得の時間は見込みに入っていません。

### 取得の計画（2026-09-12。§5.10.14.43。**所有者の許可待ち**）

#### 1. CP2 の母集団は CP3 に完全に含まれます（metadata で確認。計算ゼロ）

| 母集団 | 模型 | 対 | 手元にある | 未取得 |
|---|---:|---:|---:|---:|
| **CP3**（帯 1,000〜100,000 面。性質は問わない） | 6,735 | 3,367 | 1,200 | **5,535** |
| **CP2**（同、自己交差あり） | 3,232 | 1,616 | 200 | 3,032 |
| CP2 ⊆ CP3 | **はい**（和集合 = 6,735） | | | |

**まとめて取れば 1 回で済みます**（仕様側の判断どおり）。CP2 だけ取ると、あとで CP3 のときに残りを取ることになります。

#### 2. 取得の速さ（実測。12 件。面数の分位点から等間隔に選んだ）

| 量 | 実測 |
|---|---|
| 1 件あたり | **2.33 s**（最小 1.60 / 最大 4.73。**大半は往復の待ち時間**。1,188 面で 1.84 s、72,064 面で 4.73 s） |
| 1 件あたりの `.npz` | 0.40 MB（実効 0.2 MB/s） |
| 変換（`.npz` → `.kmesh`） | 0.006 s / 件（無視できる） |

標本の平均面数 16,530 は、未取得 5,535 件の平均 16,828 とほぼ同じです（分位点から選んだため）。

#### 3. 見込み

| 範囲 | 件数 | 取得の時間 | 容量（npz + kmesh） |
|---|---:|---:|---:|
| **CP3 の全部（推奨）** | 5,535 | **3.6 時間** | **4.3 GB** |
| CP2 だけ | 3,032 | 2.0 時間 | 2.4 GB |

**空き容量は 193 GB** なので問題ありません。取得は逐次（1 件ずつ）で、`fetch.py` は取得済みを飛ばすので**中断と再開ができます**。

#### 4. 取得のときに記録すること（仕様側の指定）

- 取得できた件数と、できなかったものの理由（`fetch.py` は 1 件の失敗で止まらず、理由を出します）
- **量子化の通過率**（母集団での実測。既知は CP1 1,000 模型 → 500 対（100%）、CP2 200 模型 → 168（84%））
- 取得にかかった時間（見積もりに入っていなかった分）

#### 5. 許可を仰ぐこと

**外部（Hugging Face の Thingi10K）への 5,535 回の取得**です。既に 1,200 件を同じ経路で取っており、
データは Git に含めません（`.gitignore` 済み）。**所有者の許可が出てから実行します。**

### ★ 取得の方法を直しました — 1 件ずつではなく、公開されている書庫を 1 回（2026-09-12）

**所有者の 3 つの問いに、測って答えました。**

#### 問い 1: 3〜4 GB に数時間かかる原因は何か

**大きさではなく、往復の回数です。**

| 量 | 実測 |
|---|---|
| 1 件（`.npz` 0.40 MB）の取得 | **2.33 s** |
| うち転送に要る時間 | **0.19 s**（下の帯域から） |
| **帯域**（1 本の大きな流れで測定。書庫の先頭 67 MB） | **2.1 MB/s** |

**1 件あたり約 2.1 秒が待ち時間**（要求 1 回ごとの往復）で、5,535 件なら 3.2 時間がそこに消えます。
**転送そのものは 4.3 GB ÷ 2.1 MB/s = 34 分**です。

#### 問い 2: 大量の取得は不適切ではないか

**ご指摘のとおりで、方法を変えます。** Hugging Face のこのデータセットは、
**まとめ取り用の書庫を公開しています**（リポジトリの一覧を API で 1 回引いて確認）。

| ファイル | 大きさ |
|---|---:|
| **`Thingi10K_npz.tar.gz`** | **4.09 GB**（`npz/<file_id>.npz` の並び。9,998 件） |
| `Thingi10K.tar.gz` / `Thingi10K_renderings.tar.gz` ほか | 用途が違うので使いません |

**個別に 5,535 回叩くより、公開されている書庫を 1 回取るのが提供側の意図に沿います。**

#### 問い 3: 一度に全部取れば CP2 と CP3 が両方入るのではないか

**そのとおりです。** 書庫は Thingi10K の全 9,998 模型を含むので、**CP2（3,232）も CP3（6,735）も、その先も 1 回で賄えます。**

| 方法 | 要求の回数 | 時間 | 書き出す量 |
|---|---:|---:|---:|
| ~~1 件ずつ（`get`）~~ | ~~5,535~~ | ~~3.6 時間~~ | 4.3 GB（npz + kmesh） |
| **書庫を 1 回（`bundle`。採用）** | **1** | **約 30 分**（実測の進みから） | **2.2 GB**（`.kmesh` だけ） |

**`.npz` は残しません**（書庫を展開すると 14 GB。圧縮比 3.47 を実測）。**gzip は途中から読めない**ので書庫全体を流しますが、
**書き出すのは帯に入る 6,735 模型のうち未取得の 5,535 件だけ**です。中断したら変換済みを飛ばして流し直せます。

**`tests/thingi10k/fetch.py` に `bundle` を足しました**（`python3 tests/thingi10k/fetch.py bundle cp3`）。
理由（往復の回数・提供側への配慮・展開後の容量）はコードの説明に書いています。

### 取得の結果と、母集団の性質（2026-09-12）

#### 取得（書庫を 1 回。要求は 1 回）

| 量 | 実測 |
|---|---|
| 書庫 `Thingi10K_npz.tar.gz` | 4.09 GB。**帯域 4.7 MB/s**、取得 **14.5 分** |
| 取り出しと変換（手元の書庫から） | **139 秒**（9,999 件を通過し、3,490 件を変換） |
| 変換できた模型 | **6,734 / 6,735**（`.kmesh` 2.5 GB） |
| 失敗 | **1 件**（`613661`: 面が 4 列 = 四角形。三角形メッシュではない。データの性質で、取得の失敗ではない） |

**★ つまずき 2 件**（どちらも私の誤り）:
1. 最初の実行を `| head -2` に繋いだため、**パイプが閉じてプロセスが死に**、1,123 件で止まっていました
2. 流しながら変換する形は、**587 秒で接続が切れました**（`unexpected end of data`）。切れると帯域を払い直します

**対処**: `fetch.py bundle` を **2 段**にしました。(1) 書庫を `Range` で**切れたら続きから**手元に取る、
(2) 手元の書庫から要る模型だけ取り出す（何度でもやり直せる）。**書庫は残します**（再抽出が無料になる。4 GB。空き 207 GB）。

#### 母集団の性質（6,735 模型。量子化の後。`quantstat` と駆動の分類）

| 性質 | 件数 |
|---|---|
| **量子化の受け入れ** | **6,070（90.1%）→ 3,035 対** |
| 拒否: ∂S ≠ 0（元から） | 520 |
| 拒否: ∂S ≠ 0（量子化と除去のあと） | 144 |
| 拒否: 空（量子化で消えた） | 1 |
| 量子化で面が落ちた模型 | 1,222（18.1%。延べ 132,615 枚） |
| 量子化で頂点が併合された | 1,465（21.8%。延べ 125,714 点） |
| 辺多様体 / 頂点多様体 | 83.3% / 78.1% |
| 自己交差あり（量子化の後） | 3,207（47.6%） |
| 成分が 2 つ以上 | 2,334（34.7%） |
| 余分辺あり / 不足辺あり | 13.0% / 7.1% |

**CP3 の入口の検査は、この 6,735 のうち 665（9.9%）を弾きます**（∂S ≠ 0 と空）。
**残る 6,070 のうち 21.9% は辺か頂点が多様体でなく、34.7% は成分が 2 つ以上**です。

#### 通過率の実測で、CP2 の見込みを立て直しました

| | 前（200 模型の実測 84%） | **後（母集団の実測 82.5%）** |
|---|---:|---:|
| CP2 の本体 | 1,360 対 / 29.8 時間 | **1,334 対 / 29.6 時間** |

| 先頭 K 対 | 対の面数の上限 | 累積 |
|---:|---:|---:|
| 400 | 7,908 | 0.3 h |
| 800 | 27,138 | 2.2 h |
| 1,000 | 54,308 | 5.6 h |
| 1,200 | 112,738 | 14.6 h |
| **1,334（全部）** | 200,000 | **29.6 h** |

**通過率の仮定（84%）が実測（82.5%）とほぼ一致したので、見込みは変わりません。**

---

## CP2 本体の層化の設計（2026-09-12。`SPEC-phase5.md` §1.5.1.1。**設計だけ。実行していません**）

### 1. 対象

量子化を通った **2,668 模型を面数の昇順に 2 つずつ組んで 1,334 対**（駆動と同じ組み方）。
対の面数は 2,000〜200,000。時間の見込みは $T \approx k\,n^{1.42}$（84 対で較正）。

### 2. 層の数 — **5 層**（対の数で等分）

| 層の数 | 最上位の層が全体の時間に占める割合 | 最上位の層の 1 対 |
|---:|---:|---:|
| 4 | 81% | 258.9 s |
| **5** | **74%** | **295.7 s** |
| 6 | 68% | 325.4 s |

**層を増やすほど最上位が薄くなりますが、1 層あたりの対が減って打ち切りの判定が弱くなります。**
5 層にすると 1 層 267 対で、下の N = 59 を取っても層の 22% を見ることになります。**4 層では最上位が 81% を占め、6 層では 1 層 222 対で N の余地が減ります。**

| 層 | 面数の範囲 | 対 | 1 対の平均 | 層の合計 |
|---:|---|---:|---:|---:|
| 1 | 2,000〜5,128 | 266 | 1.9 s | 0.14 h |
| 2 | 5,170〜12,730 | 267 | 6.7 s | 0.49 h |
| 3 | 12,802〜27,138 | 267 | 21.3 s | 1.58 h |
| 4 | 27,290〜67,670 | 267 | 73.6 s | 5.46 h |
| **5** | **68,254〜200,000** | 267 | **295.7 s** | **21.93 h** |

### 3. 各層から何対取るか — **打ち切りの N から決めます**（実測から決められないので、判定したい発生率から導く）

**「N 対連続で新種が出なければ打ち切る」の N は、実測からは決まりません**（84 対の失敗は 1 種類で、
新種の間隔を測れない）。**判定したい発生率から導きます**（`CLAUDE.md`「閾値を実測から決められないときは、
判定したい差から導いてください」）。ある層で発生率 $p$ の種類を 1 度も見ない確率は $(1-p)^N$ なので、
**$N$ 対で新種が出なければ「その層では発生率 $p$ 以上の種類は無い」と 95% の確からしさで言えます**。

| N | 検出できる発生率 | 合計の対 | **見込みの時間** |
|---:|---|---:|---:|
| 29 | 9.8% 以上 | 145 | 3.2 h |
| **59** | **5.0% 以上** | **295** | **6.5 h** |
| 90 | 3.3% 以上 | 450 | 10.0 h |
| 135 | 2.2% 以上 | 675 | 15.0 h |
| 267（全部） | 1.1% 以上 | 1,330 | 29.6 h |

**実装側の推し: N = 59（6.5 時間）。** 根拠は 2 つ。**CP2 の 84 対で失敗は 2 / 84 = 2.4%** で、
5% を検出できれば「84 対で見えた水準の種類」は捕まります。そして **6.5 時間は 1 晩で回せる**（29.6 時間は 2 日弱）。
**判断は仕様側です。** N を上げるときの費用は上の表のとおりで、**費用のほぼ全部は層 5**（N = 59 で 4.85 h / 6.5 h）。

### 4. 取り方と流す順序

- **各層の中で等間隔に取る**（$i$ 番目は層の $(i + 0.5)/N$ 分位）。先頭から取ると層の下端に偏ります
- **層を横断して流す**（層 1 の 1 番目、層 2 の 1 番目、…、層 5 の 1 番目、層 1 の 2 番目、…）。
  **1 巡ごとに全層が進むので、途中で止めてもどの層も同じ割合まで見たことになります**

> **★ 駆動に 1 つ変更が要ります。** いまの `thingi_cp1` は**面数の昇順に回し、`_only.txt` は絞り込みにしか使いません**。
> このままだと層 5 が最後になり、途中で止めると大きい対を 1 つも見ていないことになります。
> **`_only.txt` に書いた順に回す**ようにしてください（数行）。**未実装です。**

### 5. 記録するもの（§1.5.1.1 の指定）

| 項目 | 出す形 |
|---|---|
| 失敗の種類の一覧 | 種類ごとに、**最初に出た対と層**、以後の件数。種類は駆動の `why`（辺多様体でない / 頂点多様体でない / 向きが整合しない / 退化三角形が残った / χ が奇数 / 体積の篩）+ `unresolved_post` の内訳 |
| 層ごとの対数 | **見た対 / 層の対**（例: 59 / 267） |
| 見ていない範囲 | 層ごとの**未実行の対数と面数の範囲**（N = 59 なら各層 207〜208 対が残る） |

**「小さい対から流すので、打ち切っても得たものは失われない」は層化では成り立ちません。**
**各層で何対見たかが、確かめた範囲そのものです。**

### CP2 本体（層化した 295 対）— 回す前の予測と、投入の条件（2026-09-12）

**設定**: `cp2b.txt`（2,668 模型）+ `cp2b_only.txt`（295 対 = 5 層 × 59、**層を横断する順**）、
深度 6、8 スレッド、$b = 21$、-O3、**検算 ON**、NSI 1、O3 = 2、7 例目 = 真。見込み **6.5 時間**。

| 層 | 層の面数の範囲 | 層の対 | 取る対 | 取った対の面数 | 見ない対 |
|---:|---|---:|---:|---|---:|
| 1 | 2,000〜5,128 | 266 | 59 | 2,016〜5,096 | 207 |
| 2 | 5,170〜12,730 | 267 | 59 | 5,220〜12,648 | 208 |
| 3 | 12,802〜27,138 | 267 | 59 | 12,866〜26,966 | 208 |
| 4 | 27,290〜67,670 | 267 | 59 | 27,518〜67,370 | 208 |
| 5 | 68,254〜200,000 | 267 | 59 | 68,816〜198,032 | 208 |

#### 予測（推測。外れ方の行き先つき）

| 量 | 予測 | 外れたら |
|---|---|---|
| 失敗の種類 | **1 種類**（頂点の水準 = 辺多様体でない + `unresolved_post` > 0） | 2 種類以上なら新しい配置。最小化してコーパスへ |
| 失敗の対の層 | **層 4 と 5 に偏る**（84 対では分位点 0.68 と 0.79） | 層 1〜3 に出たら、面数と無関係な原因 |
| 失敗の件数 | **5〜10 対**（84 対で 2 件 = 2.4%。295 対なら 7 件前後） | 20 件超なら、母集団の性質が 200 模型の標本と違う |
| `unresolved_post` の和 | **失敗の対の辺の数と一致**（`unsplit_edges` は 0） | 不一致なら `unsplit_edges` を確認（radial sort の回帰） |
| 時間 | **6.5 時間**（層 5 が 4.85 h） | 10 時間超なら、当てはめが裾を過小に見ている |

**「5% 以上の種類は無い」と言えるのは【層の中での発生率】です。** 層は対の数で等分しているので、
**層 5 で 5% は、本体 1,334 対の中では 1%** に当たります（仕様側の指摘）。

#### ★ 投入の条件と、計器の問題

**所有者の指定**: 「CPU 使用率が 50% を下回っている状態が 1 分以上続いたとき」。

**サンドボックスからは外（ホスト）の CPU 使用率が見えません**（`/proc/stat` は 1〜3% を示すだけ。
`CLAUDE.md` の「`/proc/loadavg` は外の負荷を見ない」と同じ盲点）。そこで**「実際に何コア使えるか」を測ります**。

| 計器 | 同じ仕事を 1 スレッドと 8 スレッドで回し、速度向上を出す（自己較正。基準値が要らない） |
|---|---|
| いまの読み | 1 スレッド 1.18〜1.25 s、**速度向上 4.85〜5.84 倍** |
| 対応（物理コアは 8。`CLAUDE.md`） | 速度向上 $S \approx 8(1-u)$。$u$ は外が使っている割合。**$S = 4$ が「使用率 50%」** |

**所有者の指定を文字どおり取ると $S \ge 4$ で、いまの読み（4.85〜5.84）は既に条件を満たします。**
**しかし所有者は「現在は 80〜98%」と見ており、読みが食い違います。** どちらが正しいか私には判定できません。

> **そこで、いまより明らかに空いたことが分かる水準にします: $S \ge 6.5$ が 1 分以上**
> （15 秒ごとに測り、4 回連続）。**待つ側に倒しています。**
> **この読み替えは私の判断です。** 食い違いの理由（ホストの表示と、サンドボックスから見た実効のコア数）は上のとおりです。
