# CLAUDE.md — Krisite プロジェクト

> **この文書はフェーズに依存しません。** フェーズが変わっても書き換え不要です。
> 現在どのフェーズかは `docs/ROADMAP.md` が唯一の情報源です。

---

## 言語

**このプロジェクトでは日本語でやりとりします。**

- 応答・説明・質問・提案はすべて日本語
- コミットメッセージ、Issue、PR 説明も日本語
- **ただし以下は英語**: 識別子、型名、関数名、ファイル名、`docs/` 配下の見出し以外の技術用語
- コードコメントは日本語可。ただし数式や論文からの引用は原文の記法を保つこと

---

## プロジェクト概要

`Krisite` は、3D データを扱う C++20 ライブラリです。点群圧縮・メッシュ化・
厳密ブール演算の統合を目指しています。

| 項目 | 値 |
|---|---|
| 名前空間 | `krisite::`（短縮別名 `namespace kri = krisite;` を提供してよい） |
| ヘッダ | `include/krisite/...` |
| CMake 接頭辞 | `KRISITE_` |
| ファイル拡張子 | `.kris` |
| マジックバイト | ASCII `"KRIS"` + `uint32` バージョン |

名前の由来は **kris**（短剣、かつ crystal の語幹）+ **-ite**（鉱物の接尾辞）です。
削ることと結晶をひとつの語に込めています。README のタグラインは下記を既定とします。

```
Krisite — exact, plane-based geometry for point clouds and meshes
```

---

## 作業を始める前に読むもの

**この順序で読んでください。**

1. **`docs/ROADMAP.md`** — 現在のフェーズと全体像。**ここが現在地の唯一の情報源です**
2. **`docs/SPEC-phase<N>.md`** — 現在のフェーズの仕様。**全文を読むこと**
3. **`docs/IMPL-phase<N-1>.md`** — 直前のフェーズの実装ノート。特に末尾の「申し送り」
4. `docs/STYLE.md` — 規約の細部
5. `docs/BENCH.md` — 既存の測定値

**仕様が正です。** 実装ノートは「なぜそう作ったか」であって、仕様ではありません。
両者が食い違ったら仕様に従い、食い違い自体を報告してください。

---

## 文書の役割分担

| 文書 | 書いてあること | 更新するのは |
|---|---|---|
| `CLAUDE.md`（本書） | **どう働くか。** 不変の規約 | まれ |
| `docs/ROADMAP.md` | **今どこにいるか。** フェーズの状態 | フェーズ移行時 |
| `docs/SPEC-phase<N>.md` | **何を作るか。** 仕様が正 | 仕様担当 |
| `docs/IMPL-phase<N>.md` | **なぜそう作ったか。** 判断と根拠、代案、つまずいた点 | 実装担当 |
| `docs/STYLE.md` | **どう書くか。** 規約が正 | まれ |
| `docs/BENCH.md` | **どれだけ速いか。** 測定値が正 | 計測ごと |

`IMPL-phase<N>.md` には **コードを読めば分かること・`git log` で分かることを書かないでください。**
数ヶ月後に「なぜこうなっているのか」を再導出する手間を省く情報だけを残します。

---

## ライセンス方針（最重要）

**このプロジェクトは MIT ライセンスで公開します。この制約は他のすべてに優先します。**

### 絶対に禁止

以下のコードを **参照・引用・移植・コピー** しないでください。ライセンス汚染は
後から取り返しがつきません。

| 対象 | ライセンス |
|---|---|
| CGAL | GPL |
| `MarcoAttene/Indirect_Predicates` | LGPL-3.0 |
| `MarcoAttene/TMesh_Kernel`, `CDT`, その他 Attene 氏のリポジトリ | LGPL / GPL |
| `gcherchi/*`（本体は MIT だが LGPL 依存を含む） | 実質 LGPL |
| `mangoleaves/OpenMeshCraft` | GPL-3.0 |
| MeshLab / VCGlib | GPL |
| LASlib（LAStools 同梱版） | LGPL |

**判断に迷ったら実装せず、必ず確認を求めてください。**

### 条件付きで許可

| 対象 | 条件 |
|---|---|
| GMP / MPFR | **テストのみ**。`KRISITE_BUILD_TESTS_WITH_GMP=ON`（既定 OFF）でのみ有効。ライブラリ本体に絶対にリンクしない |
| Manifold (Apache-2.0) | **テストの正解器としてのみ**。ライブラリ本体から呼ばない |
| Geogram (BSD-3) | **コードは使わない**。Levy24 論文は読んでよい |
| LASzip 単体 (Apache-2.0) | 可。**LASlib（LGPL）と混同しないこと** |

### 制限なく許可

- **論文からの再実装**。著作権は表現を保護し、アルゴリズムは保護しません
- Shewchuk の predicates（パブリックドメイン）
- 自分で導出した数式・アルゴリズム

### 依存を追加するとき

**新しい外部依存を入れる前に必ず確認を求めてください。** 勝手に追加しないこと。
確認時には以下を報告してください。

1. ライセンス名（LICENSE ファイルを実際に読んで確認したもの。README の記述を信用しない）
2. その依存の推移的依存とそのライセンス
3. ヘッダオンリーか、静的リンクか、動的リンクか
4. 自前実装した場合の工数見積もり

---

## ビルドとコマンド

```bash
# 構成（開発時の既定）
cmake -B build -G Ninja \
  -DCMAKE_BUILD_TYPE=RelWithDebInfo \
  -DKRISITE_CHECKED_ARITH=ON \
  -DKRISITE_BUILD_TESTS_WITH_GMP=ON

cmake --build build
ctest --test-dir build --output-on-failure
ctest --test-dir build -R fixed_int --output-on-failure   # 単一テスト

# ベンチマーク
cmake -B build-rel -G Ninja -DCMAKE_BUILD_TYPE=Release \
  -DKRISITE_CHECKED_ARITH=OFF -DKRISITE_BUILD_BENCH=ON
cmake --build build-rel
./build-rel/bench/pred_bench
```

### CMake オプション

| オプション | 既定 | 意味 |
|---|---|---|
| `KRISITE_COORD_BITS` | 21 | 入力座標のビット幅 `b` |
| `KRISITE_CHECKED_ARITH` | Debug で ON | 全演算のオーバーフロー検査 |
| `KRISITE_BUILD_TESTS` | ON | テストのビルド |
| `KRISITE_BUILD_TESTS_WITH_GMP` | **OFF** | GMP 差分テスト（LGPL、テスト専用） |
| `KRISITE_BUILD_BENCH` | OFF | ベンチマークのビルド |

フェーズ固有のオプションは各 `SPEC-phase<N>.md` を参照してください。

---

## コーディング規約

### C++ 標準

C++20。コンパイラは GCC 13+ / Clang 16+ / MSVC 2022+。

**MSVC には `unsigned __int128` がありません。** 可搬フォールバックを書くときは、
`<intrin.h>` の `_addcarry_u64` / `_umul128` を使う第3の経路が要ります。
**この制約はライブラリ本体だけでなくテストコードにも同じように効きます**
（Phase 0 で実際に落ちたのはテスト側でした）。

### 算術コードの制約（`include/krisite/arith/` 配下）

以下は **例外なく守ってください**。並列化と性能の前提が崩れます。

- **動的メモリ確保を行わない**（`new`, `malloc`, `std::vector`, `std::string` 禁止）
- **例外を投げない**（すべて `noexcept`）
- **グローバル変数・可変な静的変数を持たない**（スレッド安全性のため）
- **仮想関数を使わない**
- リム数はテンプレートパラメータで持ち、実行時に変えない

### 型でビット幅を表現する

**これが本プロジェクトの設計の要です。**

```cpp
// 良い: 幅の増加が型に現れる。オーバーフローがコンパイル時に防がれる
template <std::size_t N, std::size_t M>
fixed_int<N + M> mul(const fixed_int<N>&, const fixed_int<M>&) noexcept;

// 悪い: 実行時にしか壊れない
fixed_int<4> mul(const fixed_int<4>&, const fixed_int<4>&);
```

この設計のおかげで、Phase 0 の仕様バグ（`cmp_h` が 4 リムでは足りず 5 リム必要）は
定数の追加と戻り値型 1 箇所の変更だけで吸収でき、呼び出し側は 1 行も変わりませんでした。

### 命名

- 型: `PascalCase` または `snake_case`。統一されていればどちらでも
- 関数・変数: `snake_case`
- 定数: `kCamelCase` または `UPPER_SNAKE`。統一すること
- 名前空間: `krisite::arith`, `krisite::geom`

一度決めたら `.clang-format` と `docs/STYLE.md` に記録し、以降は機械的に従ってください。

**`clang-format -i` の対象は `*.hpp` / `*.cpp` に限定すること。** CMake ファイルに
かけると壊れます。

### コメント

論文の式を実装した箇所には、**必ず出典を書いてください。**

```cpp
// EMBER §4.2 式(7): 構成点の平面に対する側
// sign(w) * sign(a*x + b*y + c*z + d*w)
// ビット幅: 9b+20 → SPEC-phase0.md §3.1 参照
```

数ヶ月後に読み返したとき、論文のどこを見ればいいか分かることが重要です。

---

## 通しての約束

**フェーズをまたいで守る規律です。破ると後で高くつきます。**

### 厳密性

- **リム数の数値リテラルは `geom/widths.hpp` 以外に書かない**
- **述語を追加するときは、先に §3 と同じ手順でビット幅を導出し `widths.hpp` に定数を足す。**
  実装はその後
- **乱択の実測値でリム数を減らさない。** 乱択は真の最大に届きません。
  減らすのは、再導出した上界が下のリム境界を下回るときだけ
- **理論上界を実測が超えたら即座に報告。** これは設計の誤りです
- **浮動小数点の許容誤差を導入しない。** 厳密性がこのプロジェクトの存在理由です

### 検証

- **述語を実装する前に、その述語の差分テストを書く**
- **正解器は被検体と別経路で書く。** 同じ変形を使う正解器は正解器ではありません。
  同じ間違いをして両方が通ります
- **退化ケースを明示的に構成する。** 乱択は共平面・共線・重複頂点をほぼ生成しません。
  対象データ（CAD、量子化された点群）では退化は例外ではなく常態です
- **対称性の高い入力はバグを構造的に隠します。** 軸平行・直交・等間隔の入力は、
  一見すると退化ケースですが、退化の一部しか作れません。退化コーパスは
  「特殊な値」だけでなく **「非対称な配置」（斜め、非直交、非整列）** を必ず含めること。
  実例: 軸平行な立方体だけのコーパスは、4 平面同時交差・クリップの分岐バグ・
  変異テストの検出力の 3 つを同時に隠していました（`IMPL-phase1.md`）
- **変異テストはコーパスに依存します。** 「どのコーパスで検出されるか」を必ずセットで
  指定してください。**「変異テストがある」ことと「変異テストが効いている」ことは別です**
- **変異テストで検出力を確認する。** テストが本当に効いているかは壊してみないと分かりません。
  意図的に誤りを埋め、複数のテストが独立に検出することを確かめてください
- **プラットフォーム検証は CI が正。** 開発コンテナに GCC / MSVC を入れて代用しない。
  そのために `settings.json` の deny を緩めないこと

### 計測

- **単発の測定値の差に原因を当てない。** 実行ごとに ±15% 程度動きます。
  1 割前後の差に意味を持たせないこと
- **最適化の前に比率を測る。** 呼び出し比率を知らずに重い関数を最適化しても無駄になります

---

## 作業の進め方

### 早めに止まって確認を求める場面

以下に該当したら、進めずに報告してください。

- 仕様書に書かれていない設計判断が必要になったとき
- 新しい述語を追加する必要が生じたとき（ビット幅の再解析が必要）
- **理論上界を実測が超えたとき**（重大。即座に）
- **仕様書が定める中止条件に該当したとき**（止めて報告。続行しない）
- **仕様書が定めるチェックポイントに到達したとき**（都度報告。全部終えてからではなく）
- 外部依存を追加したくなったとき
- 仕様書の記述が実装上おかしいと気づいたとき（**仕様のバグかもしれません**）

**「たぶんこうだろう」で進めないでください。**

Phase 0 では実装側からの指摘で仕様のバグが 3 件見つかり、うち 1 件は字義どおり実装すると
符号が反転した点を返す実害のあるものでした。**仕様を疑うのは仕事のうちです。**

### やらないこと

**現在のフェーズの `SPEC-phase<N>.md` にある「非目標」を勝手に実装しないでください。**

非目標は「不要」という意味ではなく、多くは **「まだ判断材料がない」** という意味です。
先回りして入れると、判断のための計測ができなくなります。

「ついでに」のリファクタリングもしないこと。指示された範囲に留めてください。

### 仕様に書かれていない判断をしたとき

`IMPL-phase<N>.md` に **判断・根拠・採らなかった代案** を記録してください。
後から読む人が再導出せずに済むようにするためです。

---

## Git 運用

- ブランチ: `phase<N>/<topic>`（例: `phase1/vertex-identity`）
- コミットは論理単位で小さく
- コミットメッセージは日本語。1 行目は 50 字以内の要約。本文に仕様の参照節を書く
- **テストが通らない状態でコミットしない**
- `main` に直接コミットしない

```
fixed_int の乗算を実装

x86-64 では _mulx_u64、それ以外は __int128 にフォールバック。
GMP 差分テスト 10^7 件でパスを確認。

SPEC-phase0.md §5.2
```

**コミットの粒度について。** 「テスト追加と実装を分ける」ことが望ましいものの、
モジュールの新規導入時は**テストが実装なしにコンパイルできない**ため両立しません。
その場合は「実装 + 性質テスト」を 1 コミットにまとめ、差分テストや退化ケーステストを
別コミットに分けてください。**各コミットが単独でビルド・テストを通ること**が優先です。

---

## 環境について

開発コンテナにはツールチェーンが揃っていない場合があります。`sudo` / `apt` は
`settings.json` の deny により使えません。**これは意図した制約です。緩めないでください。**

Phase 0 では `pip` の cmake / ninja、`ziglang` 配布の clang、GMP は `.deb` を
`dpkg-deb -x` でホームに展開、という構成で立ち上げました。

**ヘッダを変更したのに挙動が変わらないときは、ninja が再ビルドを取りこぼしています。**
マウントの mtime 粒度が粗いためです。`touch` するか `--clean-first` を使ってください。
Phase 0 でこれにより誤った変異テスト結果を信じかけた事例があります。

---

## 参考文献

各 `SPEC-phase<N>.md` の参考文献表を参照してください。
論文 PDF は `docs/papers/` に置いてください（**Git には含めない**。`.gitignore` 済み）。
